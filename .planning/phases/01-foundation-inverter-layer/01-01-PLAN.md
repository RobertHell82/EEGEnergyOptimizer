---
phase: 01-foundation-inverter-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - custom_components/eeg_energy_optimizer/__init__.py
  - custom_components/eeg_energy_optimizer/const.py
  - custom_components/eeg_energy_optimizer/manifest.json
  - custom_components/eeg_energy_optimizer/inverter/__init__.py
  - custom_components/eeg_energy_optimizer/inverter/base.py
  - hacs.json
  - tests/conftest.py
  - tests/test_inverter_base.py
  - tests/test_inverter_factory.py
  - tests/test_manifest.py
  - pyproject.toml
autonomous: true
requirements:
  - INF-01
  - INF-03

must_haves:
  truths:
    - "Integration directory exists with valid manifest.json and hacs.json"
    - "Abstract InverterBase ABC defines all 3 write methods plus is_available property"
    - "Factory function creates inverter by type string and raises on unknown type"
    - "Test infrastructure runs and all tests pass"
  artifacts:
    - path: "custom_components/eeg_energy_optimizer/manifest.json"
      provides: "HA integration manifest"
      contains: '"domain": "eeg_energy_optimizer"'
    - path: "custom_components/eeg_energy_optimizer/inverter/base.py"
      provides: "Abstract inverter interface"
      exports: ["InverterBase"]
      min_lines: 30
    - path: "custom_components/eeg_energy_optimizer/inverter/__init__.py"
      provides: "Factory function"
      exports: ["create_inverter", "INVERTER_TYPES"]
    - path: "hacs.json"
      provides: "HACS manifest"
      contains: '"name": "EEG Energy Optimizer"'
    - path: "tests/test_inverter_base.py"
      provides: "ABC contract tests"
      min_lines: 20
  key_links:
    - from: "custom_components/eeg_energy_optimizer/inverter/__init__.py"
      to: "custom_components/eeg_energy_optimizer/inverter/base.py"
      via: "import InverterBase"
      pattern: "from .base import InverterBase"
    - from: "custom_components/eeg_energy_optimizer/__init__.py"
      to: "custom_components/eeg_energy_optimizer/inverter/__init__.py"
      via: "import create_inverter"
      pattern: "from .inverter import create_inverter"
---

<objective>
Create the HACS-compliant integration skeleton with abstract inverter interface, factory pattern, constants, and test infrastructure.

Purpose: Establish the foundational code structure that Plan 02 builds upon -- the inverter contract, HACS packaging, and test framework.
Output: A loadable (but not yet functional) HA integration with abstract inverter layer and passing tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-inverter-layer/01-CONTEXT.md
@.planning/phases/01-foundation-inverter-layer/01-RESEARCH.md
@.planning/phases/01-foundation-inverter-layer/01-VALIDATION.md

@custom_components/energieoptimierung/manifest.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create HACS skeleton, constants, abstract inverter interface, and factory</name>
  <files>
    custom_components/eeg_energy_optimizer/manifest.json,
    custom_components/eeg_energy_optimizer/const.py,
    custom_components/eeg_energy_optimizer/__init__.py,
    custom_components/eeg_energy_optimizer/inverter/__init__.py,
    custom_components/eeg_energy_optimizer/inverter/base.py,
    hacs.json
  </files>
  <read_first>
    custom_components/energieoptimierung/manifest.json,
    custom_components/energieoptimierung/const.py,
    custom_components/energieoptimierung/__init__.py,
    custom_components/energieoptimierung/fronius_api.py,
    .planning/phases/01-foundation-inverter-layer/01-RESEARCH.md
  </read_first>
  <behavior>
    - Test: InverterBase cannot be instantiated directly (TypeError)
    - Test: A subclass missing async_set_charge_limit raises TypeError on instantiation
    - Test: A subclass missing async_set_discharge raises TypeError on instantiation
    - Test: A subclass missing async_stop_forcible raises TypeError on instantiation
    - Test: A subclass missing is_available raises TypeError on instantiation
    - Test: A complete subclass implementing all 4 methods can be instantiated
    - Test: create_inverter("huawei_sun2000", hass, config) returns an InverterBase subclass (once huawei is registered)
    - Test: create_inverter("nonexistent", hass, config) raises ValueError with "Unknown inverter type"
    - Test: manifest.json contains all required fields: domain, name, version, config_flow, integration_type
    - Test: hacs.json contains name and homeassistant fields
  </behavior>
  <action>
    Create the full integration skeleton. All files are new (no existing files to modify).

    **custom_components/eeg_energy_optimizer/manifest.json:**
    ```json
    {
      "domain": "eeg_energy_optimizer",
      "name": "EEG Energy Optimizer",
      "version": "0.1.0",
      "documentation": "https://github.com/OWNER/eeg-energy-optimizer",
      "issue_tracker": "https://github.com/OWNER/eeg-energy-optimizer/issues",
      "dependencies": [],
      "after_dependencies": ["huawei_solar"],
      "codeowners": [],
      "iot_class": "local_push",
      "integration_type": "hub",
      "config_flow": true
    }
    ```

    **hacs.json** (repository root):
    ```json
    {
      "name": "EEG Energy Optimizer",
      "homeassistant": "2025.1.0",
      "render_readme": true
    }
    ```

    **custom_components/eeg_energy_optimizer/const.py:**
    ```python
    DOMAIN = "eeg_energy_optimizer"
    CONF_INVERTER_TYPE = "inverter_type"
    CONF_BATTERY_SOC_SENSOR = "battery_soc_sensor"
    CONF_BATTERY_CAPACITY_SENSOR = "battery_capacity_sensor"
    CONF_PV_POWER_SENSOR = "pv_power_sensor"
    CONF_HUAWEI_DEVICE_ID = "huawei_device_id"

    INVERTER_TYPE_HUAWEI = "huawei_sun2000"

    INVERTER_PREREQUISITES = {
        "huawei_sun2000": "huawei_solar",
    }
    ```

    **custom_components/eeg_energy_optimizer/inverter/base.py:**
    Create the abstract base class `InverterBase(ABC)` with:
    - `__init__(self, hass: HomeAssistant, config: dict) -> None` storing `self._hass` and `self._config`
    - `@abstractmethod async def async_set_charge_limit(self, power_kw: float) -> bool`
    - `@abstractmethod async def async_set_discharge(self, power_kw: float, target_soc: float | None = None) -> bool`
    - `@abstractmethod async def async_stop_forcible(self) -> bool`
    - `@property @abstractmethod def is_available(self) -> bool`
    Exact signatures and docstrings from 01-RESEARCH.md Pattern 1.

    **custom_components/eeg_energy_optimizer/inverter/__init__.py:**
    Create the factory module with:
    - `INVERTER_TYPES: dict[str, type[InverterBase]]` -- initially empty dict (Huawei added in Plan 02)
    - `def create_inverter(inverter_type: str, hass: HomeAssistant, config: dict) -> InverterBase`
    - Raises `ValueError(f"Unknown inverter type: {inverter_type}")` for unknown types
    Note: INVERTER_TYPES starts empty -- Plan 02 will register the Huawei class.

    **custom_components/eeg_energy_optimizer/__init__.py:**
    Minimal entry point with:
    - `async_setup_entry(hass, entry) -> bool` -- creates inverter via factory, stores in `hass.data[DOMAIN][entry.entry_id]`
    - `async_unload_entry(hass, entry) -> bool` -- removes from hass.data
    - `_async_update_listener(hass, entry)` -- reloads entry
    - `PLATFORMS = []` (no platforms in Phase 1)
    Follow the exact pattern from 01-RESEARCH.md "async_setup_entry Pattern" section.

    Also create empty `custom_components/eeg_energy_optimizer/__init__.py` for the package (the inverter subdirectory already gets its __init__.py from the factory file).
  </action>
  <verify>
    <automated>pytest tests/test_inverter_base.py tests/test_inverter_factory.py tests/test_manifest.py -x --timeout=10</automated>
  </verify>
  <acceptance_criteria>
    - custom_components/eeg_energy_optimizer/manifest.json contains "domain": "eeg_energy_optimizer"
    - custom_components/eeg_energy_optimizer/manifest.json contains "config_flow": true
    - custom_components/eeg_energy_optimizer/manifest.json contains "version": "0.1.0"
    - hacs.json contains "name": "EEG Energy Optimizer"
    - hacs.json contains "homeassistant": "2025.1.0"
    - custom_components/eeg_energy_optimizer/inverter/base.py contains "class InverterBase(ABC)"
    - custom_components/eeg_energy_optimizer/inverter/base.py contains "async def async_set_charge_limit"
    - custom_components/eeg_energy_optimizer/inverter/base.py contains "async def async_set_discharge"
    - custom_components/eeg_energy_optimizer/inverter/base.py contains "async def async_stop_forcible"
    - custom_components/eeg_energy_optimizer/inverter/base.py contains "def is_available"
    - custom_components/eeg_energy_optimizer/inverter/__init__.py contains "def create_inverter"
    - custom_components/eeg_energy_optimizer/inverter/__init__.py contains 'raise ValueError'
    - custom_components/eeg_energy_optimizer/const.py contains 'DOMAIN = "eeg_energy_optimizer"'
    - custom_components/eeg_energy_optimizer/const.py contains 'CONF_INVERTER_TYPE'
    - custom_components/eeg_energy_optimizer/const.py contains 'INVERTER_PREREQUISITES'
    - custom_components/eeg_energy_optimizer/__init__.py contains "async def async_setup_entry"
    - custom_components/eeg_energy_optimizer/__init__.py contains "async def async_unload_entry"
    - pytest tests/test_inverter_base.py tests/test_inverter_factory.py tests/test_manifest.py exits with code 0
  </acceptance_criteria>
  <done>
    All 6 production files created. manifest.json has all required HA fields. hacs.json is valid. InverterBase ABC enforces 4 abstract members. Factory function creates by type and raises on unknown. Entry setup/unload implemented. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create test infrastructure (conftest, pyproject.toml, test scaffolds)</name>
  <files>
    pyproject.toml,
    tests/__init__.py,
    tests/conftest.py,
    tests/test_inverter_base.py,
    tests/test_inverter_factory.py,
    tests/test_manifest.py
  </files>
  <read_first>
    .planning/phases/01-foundation-inverter-layer/01-VALIDATION.md,
    .planning/phases/01-foundation-inverter-layer/01-RESEARCH.md
  </read_first>
  <action>
    NOTE: This task should be executed FIRST (before Task 1 production code), since Task 1 is TDD. The test files are created first, then production code is written to make them pass.

    **pyproject.toml** (repository root, new file):
    ```toml
    [tool.pytest.ini_options]
    testpaths = ["tests"]
    asyncio_mode = "auto"
    timeout = 30
    ```

    **tests/__init__.py:** Empty file.

    **tests/conftest.py:**
    Create shared fixtures:
    - `mock_hass` fixture: returns a `MagicMock(spec=HomeAssistant)` with:
      - `mock_hass.config_entries.async_entries = MagicMock(return_value=[])`
      - `mock_hass.services.async_call = AsyncMock(return_value=None)`
      - `mock_hass.data = {}`
    - Use `unittest.mock.MagicMock` and `unittest.mock.AsyncMock` (no dependency on pytest-homeassistant-custom-component for Phase 1 -- keep it simple with standard mocks)

    **tests/test_inverter_base.py:**
    Tests for INF-01 ABC contract:
    - `test_cannot_instantiate_base_directly`: `InverterBase(mock_hass, {})` raises TypeError
    - `test_incomplete_subclass_missing_charge_limit`: subclass implementing only 3 of 4 methods raises TypeError
    - `test_incomplete_subclass_missing_discharge`: same pattern for missing async_set_discharge
    - `test_incomplete_subclass_missing_stop`: same pattern for missing async_stop_forcible
    - `test_incomplete_subclass_missing_is_available`: same pattern for missing is_available
    - `test_complete_subclass_instantiates`: subclass implementing all 4 members can be instantiated, verify it's an instance of InverterBase

    **tests/test_inverter_factory.py:**
    Tests for INF-01 factory:
    - `test_create_unknown_type_raises`: `create_inverter("nonexistent", mock_hass, {})` raises ValueError matching "Unknown inverter type"
    - `test_inverter_types_dict_exists`: `INVERTER_TYPES` is a dict
    - `test_create_registered_type`: manually register a mock class in INVERTER_TYPES, call create_inverter, verify instance returned

    **tests/test_manifest.py:**
    Tests for INF-03 packaging:
    - `test_manifest_required_fields`: load manifest.json, assert domain="eeg_energy_optimizer", name="EEG Energy Optimizer", version="0.1.0", config_flow=True, integration_type="hub"
    - `test_manifest_after_dependencies`: assert "huawei_solar" in after_dependencies
    - `test_hacs_json_valid`: load hacs.json, assert name and homeassistant fields present
    - `test_hacs_json_homeassistant_version`: assert homeassistant="2025.1.0"

    Use `pathlib.Path` to locate the JSON files relative to the test file:
    ```python
    INTEGRATION_DIR = Path(__file__).parent.parent / "custom_components" / "eeg_energy_optimizer"
    ```
  </action>
  <verify>
    <automated>python -c "import pytest; print(pytest.__version__)"</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml contains [tool.pytest.ini_options]
    - pyproject.toml contains testpaths = ["tests"]
    - tests/conftest.py contains "def mock_hass"
    - tests/conftest.py contains "MagicMock" or "AsyncMock"
    - tests/test_inverter_base.py contains "test_cannot_instantiate_base_directly"
    - tests/test_inverter_base.py contains "test_complete_subclass_instantiates"
    - tests/test_inverter_factory.py contains "test_create_unknown_type_raises"
    - tests/test_manifest.py contains "test_manifest_required_fields"
    - tests/test_manifest.py contains "test_hacs_json_valid"
    - tests/__init__.py exists
  </acceptance_criteria>
  <done>
    Test infrastructure bootstrapped. conftest.py has mock_hass fixture. 4 test files cover ABC contract (6 tests), factory (3 tests), and manifest validation (4 tests). pyproject.toml configures pytest with asyncio_mode=auto and timeout=30.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `pytest tests/ -x --timeout=10` -- all tests pass
2. `python -c "import json; d=json.load(open('custom_components/eeg_energy_optimizer/manifest.json')); assert d['domain']=='eeg_energy_optimizer'"` -- manifest valid
3. `python -c "import json; d=json.load(open('hacs.json')); assert d['name']=='EEG Energy Optimizer'"` -- hacs.json valid
</verification>

<success_criteria>
- All production files exist under custom_components/eeg_energy_optimizer/
- manifest.json and hacs.json are valid JSON with all required fields
- InverterBase ABC enforces 4 abstract members (3 methods + 1 property)
- Factory raises ValueError on unknown type
- Entry setup stores inverter in hass.data[DOMAIN][entry_id]
- All 13+ tests pass with pytest
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-inverter-layer/01-01-SUMMARY.md`
</output>
