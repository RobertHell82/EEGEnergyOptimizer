---
phase: 01-foundation-inverter-layer
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - custom_components/eeg_energy_optimizer/inverter/huawei.py
  - custom_components/eeg_energy_optimizer/inverter/__init__.py
  - custom_components/eeg_energy_optimizer/config_flow.py
  - custom_components/eeg_energy_optimizer/strings.json
  - custom_components/eeg_energy_optimizer/translations/de.json
  - custom_components/eeg_energy_optimizer/translations/en.json
  - tests/test_huawei_inverter.py
  - tests/test_config_flow.py
autonomous: false
requirements:
  - INF-02
  - INF-03

must_haves:
  truths:
    - "User can add the integration via HA config flow and select Huawei SUN2000"
    - "Config flow blocks setup if Huawei Solar integration is not installed"
    - "HuaweiInverter sends correct HA service calls for charge/discharge/stop"
    - "HuaweiInverter passes power as string to Huawei Solar services"
    - "Adding a second inverter type requires only a new file -- no changes to config flow or optimizer"
  artifacts:
    - path: "custom_components/eeg_energy_optimizer/inverter/huawei.py"
      provides: "Huawei SUN2000 inverter implementation"
      exports: ["HuaweiInverter"]
      contains: "class HuaweiInverter(InverterBase)"
    - path: "custom_components/eeg_energy_optimizer/config_flow.py"
      provides: "2-step config flow"
      contains: "class EegEnergyOptimizerConfigFlow"
      min_lines: 50
    - path: "custom_components/eeg_energy_optimizer/strings.json"
      provides: "German UI strings for config flow"
      contains: "Wechselrichter-Typ"
    - path: "custom_components/eeg_energy_optimizer/translations/en.json"
      provides: "English fallback translations"
      contains: "Inverter Type"
    - path: "tests/test_huawei_inverter.py"
      provides: "Huawei service call tests"
      min_lines: 40
    - path: "tests/test_config_flow.py"
      provides: "Config flow step tests"
      min_lines: 40
  key_links:
    - from: "custom_components/eeg_energy_optimizer/inverter/huawei.py"
      to: "custom_components/eeg_energy_optimizer/inverter/base.py"
      via: "inheritance"
      pattern: "class HuaweiInverter\\(InverterBase\\)"
    - from: "custom_components/eeg_energy_optimizer/inverter/__init__.py"
      to: "custom_components/eeg_energy_optimizer/inverter/huawei.py"
      via: "INVERTER_TYPES registration"
      pattern: '"huawei_sun2000": HuaweiInverter'
    - from: "custom_components/eeg_energy_optimizer/config_flow.py"
      to: "custom_components/eeg_energy_optimizer/const.py"
      via: "import constants"
      pattern: "from .const import"
---

<objective>
Implement the Huawei SUN2000 inverter class, 2-step config flow with prerequisite validation, and all UI translations.

Purpose: Make the integration functional -- users can set up via config flow, and the optimizer has a working Huawei inverter to send charge/discharge commands to.
Output: Complete, testable integration that can be added via HA's "Add Integration" flow.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-inverter-layer/01-CONTEXT.md
@.planning/phases/01-foundation-inverter-layer/01-RESEARCH.md
@.planning/phases/01-foundation-inverter-layer/01-UI-SPEC.md
@.planning/phases/01-foundation-inverter-layer/01-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 that this plan builds on -->

From custom_components/eeg_energy_optimizer/inverter/base.py:
```python
class InverterBase(ABC):
    def __init__(self, hass: HomeAssistant, config: dict) -> None: ...
    @abstractmethod
    async def async_set_charge_limit(self, power_kw: float) -> bool: ...
    @abstractmethod
    async def async_set_discharge(self, power_kw: float, target_soc: float | None = None) -> bool: ...
    @abstractmethod
    async def async_stop_forcible(self) -> bool: ...
    @property
    @abstractmethod
    def is_available(self) -> bool: ...
```

From custom_components/eeg_energy_optimizer/inverter/__init__.py:
```python
INVERTER_TYPES: dict[str, type[InverterBase]] = {}
def create_inverter(inverter_type: str, hass: HomeAssistant, config: dict) -> InverterBase: ...
```

From custom_components/eeg_energy_optimizer/const.py:
```python
DOMAIN = "eeg_energy_optimizer"
CONF_INVERTER_TYPE = "inverter_type"
CONF_BATTERY_SOC_SENSOR = "battery_soc_sensor"
CONF_BATTERY_CAPACITY_SENSOR = "battery_capacity_sensor"
CONF_PV_POWER_SENSOR = "pv_power_sensor"
CONF_HUAWEI_DEVICE_ID = "huawei_device_id"
INVERTER_TYPE_HUAWEI = "huawei_sun2000"
INVERTER_PREREQUISITES = {"huawei_sun2000": "huawei_solar"}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement HuaweiInverter and register in factory</name>
  <files>
    custom_components/eeg_energy_optimizer/inverter/huawei.py,
    custom_components/eeg_energy_optimizer/inverter/__init__.py,
    tests/test_huawei_inverter.py
  </files>
  <read_first>
    custom_components/eeg_energy_optimizer/inverter/base.py,
    custom_components/eeg_energy_optimizer/inverter/__init__.py,
    custom_components/eeg_energy_optimizer/const.py,
    custom_components/energieoptimierung/fronius_api.py,
    .planning/phases/01-foundation-inverter-layer/01-RESEARCH.md
  </read_first>
  <behavior>
    - Test: HuaweiInverter is an instance of InverterBase
    - Test: async_set_charge_limit(5.0) calls hass.services.async_call with domain="huawei_solar", service="forcible_charge_soc", data={"device_id": "test_device", "power": "5000", "target_soc": 100}, blocking=True
    - Test: async_set_discharge(3.0, target_soc=20) calls hass.services.async_call with domain="huawei_solar", service="forcible_discharge_soc", data={"device_id": "test_device", "power": "3000", "target_soc": 20}, blocking=True
    - Test: async_set_discharge(3.0) without target_soc defaults to soc=10
    - Test: async_stop_forcible() calls hass.services.async_call with domain="huawei_solar", service="stop_forcible_charge", data={"device_id": "test_device"}, blocking=True
    - Test: async_set_charge_limit returns True on success
    - Test: async_set_charge_limit returns False when service call raises Exception
    - Test: is_available returns True when huawei_solar has a loaded config entry
    - Test: is_available returns False when huawei_solar has no entries
    - Test: power parameter is always passed as string (str type check)
  </behavior>
  <action>
    **custom_components/eeg_energy_optimizer/inverter/huawei.py** (new file):
    Create `HuaweiInverter(InverterBase)` following the exact implementation from 01-RESEARCH.md Pattern 2:
    - `__init__` extracts `self._device_id` from `config["huawei_device_id"]`
    - `async_set_charge_limit(power_kw)`: converts to watts via `str(int(power_kw * 1000))`, calls `hass.services.async_call("huawei_solar", "forcible_charge_soc", {"device_id": self._device_id, "power": str(power_w), "target_soc": 100}, blocking=True)`. Returns True on success, False on Exception.
    - `async_set_discharge(power_kw, target_soc=None)`: converts to watts, soc defaults to 10 if None, calls `hass.services.async_call("huawei_solar", "forcible_discharge_soc", {"device_id": self._device_id, "power": str(power_w), "target_soc": soc}, blocking=True)`. Returns True/False.
    - `async_stop_forcible()`: calls `hass.services.async_call("huawei_solar", "stop_forcible_charge", {"device_id": self._device_id}, blocking=True)`. Returns True/False.
    - `is_available` property: checks `hass.config_entries.async_entries("huawei_solar")` for any entry with state.value == "loaded"
    - CRITICAL: power is ALWAYS passed as `str(int(power_kw * 1000))` (string, not int) per Pitfall 3 in RESEARCH.md

    **Update custom_components/eeg_energy_optimizer/inverter/__init__.py:**
    Register HuaweiInverter in INVERTER_TYPES:
    ```python
    from .huawei import HuaweiInverter
    INVERTER_TYPES = {
        "huawei_sun2000": HuaweiInverter,
    }
    ```

    **tests/test_huawei_inverter.py** (new file):
    Write tests using mock_hass fixture from conftest.py. Mock `hass.services.async_call` as AsyncMock. Create HuaweiInverter with config={"huawei_device_id": "test_device"}. Verify each service call's exact parameters. Verify power is str type. Verify returns True/False. Verify is_available checks config entries.
  </action>
  <verify>
    <automated>pytest tests/test_huawei_inverter.py -x --timeout=10</automated>
  </verify>
  <acceptance_criteria>
    - custom_components/eeg_energy_optimizer/inverter/huawei.py contains "class HuaweiInverter(InverterBase)"
    - custom_components/eeg_energy_optimizer/inverter/huawei.py contains 'HUAWEI_DOMAIN = "huawei_solar"'
    - custom_components/eeg_energy_optimizer/inverter/huawei.py contains '"forcible_charge_soc"'
    - custom_components/eeg_energy_optimizer/inverter/huawei.py contains '"forcible_discharge_soc"'
    - custom_components/eeg_energy_optimizer/inverter/huawei.py contains '"stop_forcible_charge"'
    - custom_components/eeg_energy_optimizer/inverter/huawei.py contains 'str(int(power_kw * 1000))' or equivalent str() wrapping
    - custom_components/eeg_energy_optimizer/inverter/__init__.py contains '"huawei_sun2000": HuaweiInverter'
    - tests/test_huawei_inverter.py contains at least 8 test functions (test_)
    - pytest tests/test_huawei_inverter.py exits with code 0
  </acceptance_criteria>
  <done>
    HuaweiInverter implements all 4 InverterBase members. Service calls use correct domain, service names, and parameter formats (power as string). Factory INVERTER_TYPES includes huawei_sun2000. All 10+ Huawei tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement config flow with prerequisite validation and translations</name>
  <files>
    custom_components/eeg_energy_optimizer/config_flow.py,
    custom_components/eeg_energy_optimizer/strings.json,
    custom_components/eeg_energy_optimizer/translations/de.json,
    custom_components/eeg_energy_optimizer/translations/en.json,
    tests/test_config_flow.py
  </files>
  <read_first>
    custom_components/eeg_energy_optimizer/const.py,
    custom_components/eeg_energy_optimizer/__init__.py,
    custom_components/energieoptimierung/config_flow.py,
    .planning/phases/01-foundation-inverter-layer/01-UI-SPEC.md,
    .planning/phases/01-foundation-inverter-layer/01-RESEARCH.md
  </read_first>
  <behavior>
    - Test: Step "user" shows form with inverter_type field
    - Test: Step "user" with huawei_sun2000 selected + huawei_solar loaded -> advances to step "sensors"
    - Test: Step "user" with huawei_sun2000 selected + huawei_solar NOT loaded -> returns error "prerequisite_not_installed"
    - Test: Step "sensors" with all fields filled -> creates config entry
    - Test: Config entry data contains inverter_type, battery_soc_sensor, battery_capacity_sensor, pv_power_sensor, huawei_device_id
    - Test: Flow aborts if already configured (unique_id = DOMAIN)
  </behavior>
  <action>
    **custom_components/eeg_energy_optimizer/config_flow.py** (new file):
    Create `EegEnergyOptimizerConfigFlow(ConfigFlow, domain=DOMAIN)`:

    - `async_step_user(self, user_input=None)`:
      - Schema: `vol.Schema({vol.Required(CONF_INVERTER_TYPE): SelectSelector(SelectSelectorConfig(options=[{"value": "huawei_sun2000", "label": "Huawei SUN2000"}], mode=SelectSelectorMode.DROPDOWN))})`
      - If user_input is None: show form
      - If user_input provided: check prerequisite via `hass.config_entries.async_entries(INVERTER_PREREQUISITES[inverter_type])` for any entry with state == ConfigEntryState.LOADED
      - If prerequisite not found: return form with `errors={"base": "prerequisite_not_installed"}`
      - If found: store data, call `await self.async_set_unique_id(DOMAIN)`, `self._abort_if_unique_id_configured()`, advance to async_step_sensors

    - `async_step_sensors(self, user_input=None)`:
      - Schema: `vol.Schema({vol.Required(CONF_BATTERY_SOC_SENSOR): EntitySelector(EntitySelectorConfig(domain="sensor", device_class="battery")), vol.Required(CONF_BATTERY_CAPACITY_SENSOR): EntitySelector(EntitySelectorConfig(domain="sensor", device_class="energy")), vol.Required(CONF_PV_POWER_SENSOR): EntitySelector(EntitySelectorConfig(domain="sensor", device_class="power")), vol.Required(CONF_HUAWEI_DEVICE_ID): DeviceSelector(DeviceSelectorConfig(integration="huawei_solar"))})`
      - If user_input is None: show form
      - If user_input provided: merge with step 1 data, create entry via `self.async_create_entry(title="EEG Energy Optimizer", data=all_data)`

    Import selectors from `homeassistant.helpers.selector`:
    - `SelectSelector`, `SelectSelectorConfig`, `SelectSelectorMode`
    - `EntitySelector`, `EntitySelectorConfig`
    - `DeviceSelector`, `DeviceSelectorConfig`

    **custom_components/eeg_energy_optimizer/strings.json** (new file):
    Exact content from 01-RESEARCH.md Code Examples section, with additions from 01-UI-SPEC.md:
    ```json
    {
      "config": {
        "step": {
          "user": {
            "title": "Wechselrichter-Typ",
            "description": "Waehle den Wechselrichter-Typ fuer die Batteriesteuerung.",
            "data": {
              "inverter_type": "Wechselrichter"
            }
          },
          "sensors": {
            "title": "Sensor-Zuordnung",
            "description": "Ordne die Sensoren fuer SOC, Kapazitaet und PV-Leistung zu.",
            "data": {
              "battery_soc_sensor": "Batterie-Ladezustand (SOC)",
              "battery_capacity_sensor": "Batterie-Kapazitaet",
              "pv_power_sensor": "PV-Leistung",
              "huawei_device_id": "Huawei Batterie-Geraet"
            }
          }
        },
        "error": {
          "prerequisite_not_installed": "Die {integration_name}-Integration muss installiert und geladen sein.",
          "unknown": "Unerwarteter Fehler. Bitte versuche es erneut."
        },
        "abort": {
          "already_configured": "EEG Energy Optimizer ist bereits eingerichtet."
        }
      }
    }
    ```

    **custom_components/eeg_energy_optimizer/translations/de.json:**
    Same content as strings.json (German is the primary language).

    **custom_components/eeg_energy_optimizer/translations/en.json:**
    English translations per 01-UI-SPEC.md Copywriting Contract:
    ```json
    {
      "config": {
        "step": {
          "user": {
            "title": "Inverter Type",
            "description": "Select the inverter type for battery control.",
            "data": {
              "inverter_type": "Inverter"
            }
          },
          "sensors": {
            "title": "Sensor Mapping",
            "description": "Map the sensors for SOC, capacity, and PV power.",
            "data": {
              "battery_soc_sensor": "Battery State of Charge (SOC)",
              "battery_capacity_sensor": "Battery Capacity",
              "pv_power_sensor": "PV Power",
              "huawei_device_id": "Huawei Battery Device"
            }
          }
        },
        "error": {
          "prerequisite_not_installed": "The {integration_name} integration must be installed and loaded.",
          "unknown": "Unexpected error. Please try again."
        },
        "abort": {
          "already_configured": "EEG Energy Optimizer is already configured."
        }
      }
    }
    ```

    **tests/test_config_flow.py** (new file):
    Mock-based tests for the config flow. Use `unittest.mock.patch` to mock:
    - `hass.config_entries.async_entries` to return loaded/empty entries
    - The flow's `async_set_unique_id` and `_abort_if_unique_id_configured`

    Tests must verify:
    - Step "user" returns form on first call (user_input=None)
    - Step "user" with valid prerequisite advances to "sensors"
    - Step "user" without prerequisite returns error "prerequisite_not_installed"
    - Step "sensors" creates entry with all data merged
    - Already configured abort works

    Since testing HA config flows without the full HA test framework is tricky, the tests should:
    - Import the config flow class directly
    - Mock `self.hass` on the flow instance
    - Call step methods directly
    - Assert return values (FlowResult dicts with type, step_id, errors, etc.)
  </action>
  <verify>
    <automated>pytest tests/test_config_flow.py tests/test_huawei_inverter.py -x --timeout=10</automated>
  </verify>
  <acceptance_criteria>
    - custom_components/eeg_energy_optimizer/config_flow.py contains "class EegEnergyOptimizerConfigFlow"
    - custom_components/eeg_energy_optimizer/config_flow.py contains "async_step_user"
    - custom_components/eeg_energy_optimizer/config_flow.py contains "async_step_sensors"
    - custom_components/eeg_energy_optimizer/config_flow.py contains "prerequisite_not_installed"
    - custom_components/eeg_energy_optimizer/config_flow.py contains "SelectSelector"
    - custom_components/eeg_energy_optimizer/config_flow.py contains "EntitySelector"
    - custom_components/eeg_energy_optimizer/config_flow.py contains "DeviceSelector"
    - custom_components/eeg_energy_optimizer/strings.json contains "Wechselrichter-Typ"
    - custom_components/eeg_energy_optimizer/strings.json contains "Sensor-Zuordnung"
    - custom_components/eeg_energy_optimizer/strings.json contains "prerequisite_not_installed"
    - custom_components/eeg_energy_optimizer/translations/en.json contains "Inverter Type"
    - custom_components/eeg_energy_optimizer/translations/en.json contains "Sensor Mapping"
    - custom_components/eeg_energy_optimizer/translations/de.json contains "Wechselrichter-Typ"
    - tests/test_config_flow.py contains at least 4 test functions
    - pytest tests/test_config_flow.py exits with code 0
  </acceptance_criteria>
  <done>
    Config flow implements 2-step setup (inverter type + sensor mapping). Prerequisite validation blocks setup when Huawei Solar not installed. strings.json and translations (de+en) match UI-SPEC copywriting contract. All config flow tests pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify integration loads in Home Assistant</name>
  <what-built>
    Complete EEG Energy Optimizer integration with:
    - HACS-compatible structure (manifest.json, hacs.json)
    - Abstract inverter interface with Huawei SUN2000 implementation
    - 2-step config flow (inverter selection + sensor mapping)
    - German and English translations
    - Full test suite (13+ tests passing)
  </what-built>
  <how-to-verify>
    1. Copy `custom_components/eeg_energy_optimizer/` to your HA instance's `custom_components/` directory
    2. Restart Home Assistant
    3. Go to Settings > Integrations > Add Integration
    4. Search for "EEG Energy Optimizer" -- it should appear
    5. Step 1: Select "Huawei SUN2000" -- if Huawei Solar is not installed, you should see the error "Die Huawei Solar-Integration muss installiert und geladen sein."
    6. If Huawei Solar IS installed: proceed to Step 2, verify entity pickers show filtered sensors (SOC, capacity, PV power) and device picker shows Huawei devices
    7. Check HA logs (Settings > System > Logs) for any errors from eeg_energy_optimizer
  </how-to-verify>
  <resume-signal>Type "approved" if integration loads and config flow works, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:
1. `pytest tests/ -x --timeout=10` -- full test suite passes
2. `python -c "import json; d=json.load(open('custom_components/eeg_energy_optimizer/manifest.json')); assert d['domain']=='eeg_energy_optimizer' and d['config_flow']==True"` -- manifest valid
3. `python -c "import json; d=json.load(open('custom_components/eeg_energy_optimizer/strings.json')); assert 'Wechselrichter-Typ' in str(d)"` -- strings present
4. `python -c "from custom_components.eeg_energy_optimizer.inverter.huawei import HuaweiInverter; from custom_components.eeg_energy_optimizer.inverter.base import InverterBase; assert issubclass(HuaweiInverter, InverterBase)"` -- inheritance verified
5. Human verification: integration loads in HA and config flow works
</verification>

<success_criteria>
- HuaweiInverter correctly calls all 3 Huawei Solar services with proper parameters (power as string)
- Config flow has 2 steps with prerequisite validation
- Config flow creates entry with all required data (inverter_type, sensors, device_id)
- German strings.json and both translation files match UI-SPEC copywriting contract
- Integration loads in HA without errors (human verified)
- All tests pass (15+ tests across 5 test files)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-inverter-layer/01-02-SUMMARY.md`
</output>
