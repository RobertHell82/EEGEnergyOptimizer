---
phase: 02-forecasting-consumption-profile
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - custom_components/eeg_energy_optimizer/config_flow.py
  - custom_components/eeg_energy_optimizer/strings.json
  - custom_components/eeg_energy_optimizer/translations/de.json
  - custom_components/eeg_energy_optimizer/translations/en.json
  - tests/test_config_flow.py
autonomous: true
requirements: [FCST-01, FCST-02]

must_haves:
  truths:
    - "User can select Solcast Solar or Forecast.Solar as PV forecast source in config flow"
    - "Config flow validates that selected forecast integration is installed and loaded"
    - "User can pick PV forecast entity IDs via EntitySelector (not hardcoded)"
    - "User can select consumption sensor with default sensor.power_meter_verbrauch"
    - "Lookback weeks and update intervals are configurable"
    - "All config flow text is in German"
  artifacts:
    - path: "custom_components/eeg_energy_optimizer/config_flow.py"
      provides: "4-step config flow (user, sensors, forecast, consumption)"
      contains: "async_step_forecast"
    - path: "custom_components/eeg_energy_optimizer/strings.json"
      provides: "German UI strings for all 4 config flow steps"
      contains: "forecast"
    - path: "custom_components/eeg_energy_optimizer/translations/de.json"
      provides: "German translations matching strings.json"
      contains: "Prognose-Quelle"
    - path: "tests/test_config_flow.py"
      provides: "Tests for new config flow steps"
  key_links:
    - from: "config_flow.py"
      to: "const.py"
      via: "CONF_FORECAST_SOURCE import"
      pattern: "from .const import.*CONF_FORECAST_SOURCE"
    - from: "config_flow.py"
      to: "hass.config_entries"
      via: "prerequisite validation"
      pattern: "async_entries.*forecast"
---

<objective>
Extend the config flow with two new steps: forecast source selection (Solcast/Forecast.Solar with prerequisite validation and entity selectors) and consumption sensor configuration (sensor, lookback weeks, update intervals). Update all translation files.

Purpose: Per D-05/D-21, config flow is the Phase 2 interim UI for configuring forecast and consumption settings (will move to Onboarding Panel in Phase 4).
Output: Extended config_flow.py with 4 steps, updated strings.json and translations
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-forecasting-consumption-profile/02-CONTEXT.md
@.planning/phases/02-forecasting-consumption-profile/02-RESEARCH.md
@.planning/phases/02-forecasting-consumption-profile/02-01-SUMMARY.md

@custom_components/eeg_energy_optimizer/config_flow.py
@custom_components/eeg_energy_optimizer/const.py
@custom_components/eeg_energy_optimizer/strings.json
@custom_components/eeg_energy_optimizer/translations/de.json
@custom_components/eeg_energy_optimizer/translations/en.json
@tests/test_config_flow.py

<interfaces>
<!-- Phase 2 config keys from Plan 01 -->
From custom_components/eeg_energy_optimizer/const.py:
```python
CONF_FORECAST_SOURCE = "forecast_source"
CONF_FORECAST_REMAINING_ENTITY = "forecast_remaining_entity"
CONF_FORECAST_TOMORROW_ENTITY = "forecast_tomorrow_entity"
CONF_CONSUMPTION_SENSOR = "consumption_sensor"
CONF_LOOKBACK_WEEKS = "lookback_weeks"
CONF_UPDATE_INTERVAL_FAST = "update_interval_fast_min"
CONF_UPDATE_INTERVAL_SLOW = "update_interval_slow_min"
FORECAST_SOURCE_SOLCAST = "solcast_solar"
FORECAST_SOURCE_FORECAST_SOLAR = "forecast_solar"
DEFAULT_CONSUMPTION_SENSOR = "sensor.power_meter_verbrauch"
DEFAULT_LOOKBACK_WEEKS = 8
DEFAULT_UPDATE_INTERVAL_FAST = 1
DEFAULT_UPDATE_INTERVAL_SLOW = 15
```

<!-- Existing config flow structure (2 steps) -->
From custom_components/eeg_energy_optimizer/config_flow.py:
```python
class EegEnergyOptimizerConfigFlow(ConfigFlow, domain=DOMAIN):
    VERSION = 1
    def __init__(self): self._data: dict[str, Any] = {}
    async def async_step_user(self, user_input) -> ConfigFlowResult:  # Step 1: inverter type
    async def async_step_sensors(self, user_input) -> ConfigFlowResult:  # Step 2: SOC/capacity/PV -> creates entry
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Config flow steps for forecast source + consumption sensor</name>
  <files>
    custom_components/eeg_energy_optimizer/config_flow.py,
    tests/test_config_flow.py
  </files>
  <read_first>
    custom_components/eeg_energy_optimizer/config_flow.py,
    custom_components/eeg_energy_optimizer/const.py,
    tests/test_config_flow.py
  </read_first>
  <action>
    **Modify the config flow** to add 2 new steps after the existing "sensors" step, making a 4-step flow:
    1. `user` (existing) -- inverter type selection
    2. `sensors` (existing) -- SOC, capacity, PV sensors. **Change**: instead of creating entry, proceed to step 3
    3. `forecast` (NEW) -- forecast source selection + entity selectors
    4. `consumption` (NEW) -- consumption sensor + lookback + intervals -> creates entry

    **Step 3: async_step_forecast** (per D-02, D-03, D-04, D-05):
    - SelectSelector for CONF_FORECAST_SOURCE with options: `[{"value": "solcast_solar", "label": "Solcast Solar"}, {"value": "forecast_solar", "label": "Forecast.Solar"}]`
    - On submit: validate selected integration is installed and loaded (same pattern as inverter prerequisite check):
      ```python
      entries = self.hass.config_entries.async_entries(forecast_source)
      loaded = [e for e in entries if e.state.value == "loaded"]
      if not loaded:
          errors["base"] = "forecast_not_installed"
      ```
    - If valid: show EntitySelector for CONF_FORECAST_REMAINING_ENTITY (domain="sensor") and CONF_FORECAST_TOMORROW_ENTITY (domain="sensor")
    - No defaults hardcoded -- user picks via EntitySelector (per research: entity IDs vary by installation)
    - Proceed to step 4 on success

    **Step 4: async_step_consumption** (per D-07, D-08, D-09, D-18, D-19, D-20):
    - EntitySelector for CONF_CONSUMPTION_SENSOR (domain="sensor"), default=DEFAULT_CONSUMPTION_SENSOR (`sensor.power_meter_verbrauch`)
    - NumberSelector for CONF_LOOKBACK_WEEKS: min=1, max=52, step=1, default=8, unit="Wochen"
    - NumberSelector for CONF_UPDATE_INTERVAL_FAST: min=1, max=60, step=1, default=1, unit="Minuten"
    - NumberSelector for CONF_UPDATE_INTERVAL_SLOW: min=5, max=120, step=5, default=15, unit="Minuten"
    - On submit: `self._data.update(user_input)` then `self.async_create_entry(title="EEG Energy Optimizer", data=self._data)`

    **Modify async_step_sensors**: Change the success path from `self.async_create_entry(...)` to `return await self.async_step_forecast()`. Remove the `async_create_entry` call from this method.

    **Add imports** for new const keys:
    ```python
    from .const import (
        # ... existing imports ...
        CONF_CONSUMPTION_SENSOR,
        CONF_FORECAST_REMAINING_ENTITY,
        CONF_FORECAST_SOURCE,
        CONF_FORECAST_TOMORROW_ENTITY,
        CONF_LOOKBACK_WEEKS,
        CONF_UPDATE_INTERVAL_FAST,
        CONF_UPDATE_INTERVAL_SLOW,
        DEFAULT_CONSUMPTION_SENSOR,
        DEFAULT_LOOKBACK_WEEKS,
        DEFAULT_UPDATE_INTERVAL_FAST,
        DEFAULT_UPDATE_INTERVAL_SLOW,
        FORECAST_SOURCE_FORECAST_SOLAR,
        FORECAST_SOURCE_SOLCAST,
    )
    ```

    **Bump VERSION** to 2 (data schema changed).

    **Update tests in test_config_flow.py:**
    - Update existing tests that expect `async_create_entry` after sensors step -- now it should proceed to forecast step
    - Add `test_forecast_step_solcast_valid`: mock solcast_solar loaded, submit -> proceeds to consumption
    - Add `test_forecast_step_not_installed`: mock no entries -> error "forecast_not_installed"
    - Add `test_consumption_step_creates_entry`: submit consumption config -> entry created with all 4 steps of data
    - Add `test_full_flow_4_steps`: user -> sensors -> forecast -> consumption -> entry created

    Run: `pytest tests/test_config_flow.py -x`
  </action>
  <verify>
    <automated>pytest tests/test_config_flow.py -x --timeout=10</automated>
  </verify>
  <acceptance_criteria>
    - config_flow.py contains `async def async_step_forecast(`
    - config_flow.py contains `async def async_step_consumption(`
    - config_flow.py contains `CONF_FORECAST_SOURCE`
    - config_flow.py contains `CONF_CONSUMPTION_SENSOR`
    - config_flow.py contains `forecast_not_installed` error key
    - config_flow.py contains `VERSION = 2`
    - config_flow.py `async_step_sensors` does NOT contain `async_create_entry`
    - config_flow.py `async_step_consumption` contains `async_create_entry`
    - test_config_flow.py contains `test_forecast_step`
    - test_config_flow.py contains `test_consumption_step`
    - `pytest tests/test_config_flow.py -x` exits 0
  </acceptance_criteria>
  <done>Config flow extended from 2 to 4 steps. Forecast source validated (integration must be installed). Entity selectors for PV forecast entities. Consumption sensor with lookback and interval config. Entry created at step 4.</done>
</task>

<task type="auto">
  <name>Task 2: Translation files for all config flow steps</name>
  <files>
    custom_components/eeg_energy_optimizer/strings.json,
    custom_components/eeg_energy_optimizer/translations/de.json,
    custom_components/eeg_energy_optimizer/translations/en.json
  </files>
  <read_first>
    custom_components/eeg_energy_optimizer/strings.json,
    custom_components/eeg_energy_optimizer/translations/de.json,
    custom_components/eeg_energy_optimizer/translations/en.json,
    custom_components/eeg_energy_optimizer/config_flow.py
  </read_first>
  <action>
    **Update all three translation files** (strings.json, de.json, en.json) to add the two new config flow steps. All files must have identical structure.

    **Add to `config.step`:**

    ```json
    "forecast": {
      "title": "Prognose-Quelle",
      "description": "Wähle die PV-Prognose-Quelle. Die gewählte Integration muss installiert und geladen sein.",
      "data": {
        "forecast_source": "Prognose-Integration",
        "forecast_remaining_entity": "PV Prognose verbleibend heute",
        "forecast_tomorrow_entity": "PV Prognose morgen"
      },
      "data_description": {
        "forecast_source": "Solcast Solar (HACS) oder Forecast.Solar (HA Core) als Quelle für die PV-Produktionsprognose.",
        "forecast_remaining_entity": "Sensor der die verbleibende PV-Produktion für heute in kWh anzeigt.",
        "forecast_tomorrow_entity": "Sensor der die prognostizierte PV-Produktion für morgen in kWh anzeigt."
      }
    },
    "consumption": {
      "title": "Verbrauchsmessung",
      "description": "Konfiguriere den Verbrauchssensor und die Berechnungsparameter.",
      "data": {
        "consumption_sensor": "Verbrauchssensor",
        "lookback_weeks": "Rückblick-Zeitraum",
        "update_interval_fast_min": "Schnelles Update-Intervall",
        "update_interval_slow_min": "Langsames Update-Intervall"
      },
      "data_description": {
        "consumption_sensor": "Sensor der den Gesamt-Stromverbrauch in kWh misst (total_increasing). Standard: Stromzähler Verbrauch.",
        "lookback_weeks": "Anzahl Wochen für den rollenden Verbrauchsdurchschnitt (Standard: 8 Wochen).",
        "update_interval_fast_min": "Update-Intervall für PV-Prognose und Batterie-Sensoren in Minuten (Standard: 1 Min).",
        "update_interval_slow_min": "Update-Intervall für das Verbrauchsprofil in Minuten (Standard: 15 Min)."
      }
    }
    ```

    **Add to `config.error`:**
    ```json
    "forecast_not_installed": "Die gewählte Prognose-Integration ist nicht installiert oder nicht geladen. Bitte zuerst installieren."
    ```

    **For en.json**, translate everything to English:
    - "forecast" step title: "Forecast Source"
    - "consumption" step title: "Consumption Measurement"
    - data labels and descriptions in English
    - Error: "The selected forecast integration is not installed or not loaded. Please install it first."

    **strings.json and de.json must be identical** (German is the primary language).
  </action>
  <verify>
    <automated>python -c "import json; s=json.load(open('custom_components/eeg_energy_optimizer/strings.json')); d=json.load(open('custom_components/eeg_energy_optimizer/translations/de.json')); assert s==d, 'strings.json != de.json'; assert 'forecast' in s['config']['step']; assert 'consumption' in s['config']['step']; assert 'forecast_not_installed' in s['config']['error']; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - strings.json contains `"forecast"` step with title `"Prognose-Quelle"`
    - strings.json contains `"consumption"` step with title `"Verbrauchsmessung"`
    - strings.json contains `"forecast_not_installed"` error
    - de.json is byte-identical to strings.json
    - en.json contains `"forecast"` step with title `"Forecast Source"`
    - en.json contains `"consumption"` step with title `"Consumption Measurement"`
    - All three files are valid JSON (python json.load succeeds)
    - `pytest tests/ -x --timeout=10` exits 0 (all tests including Phase 1)
  </acceptance_criteria>
  <done>All translation files updated with German and English strings for the 2 new config flow steps. strings.json and de.json are identical. Error messages for missing forecast integration included.</done>
</task>

</tasks>

<verification>
```bash
# Config flow tests pass
pytest tests/test_config_flow.py -x --timeout=10

# All tests pass
pytest tests/ -x --timeout=10

# Translations are valid and complete
python -c "
import json
s = json.load(open('custom_components/eeg_energy_optimizer/strings.json'))
d = json.load(open('custom_components/eeg_energy_optimizer/translations/de.json'))
e = json.load(open('custom_components/eeg_energy_optimizer/translations/en.json'))
assert s == d, 'strings.json and de.json differ'
assert set(s['config']['step'].keys()) == set(e['config']['step'].keys()), 'Step keys differ'
assert 'forecast' in s['config']['step']
assert 'consumption' in s['config']['step']
print('All translations valid')
"

# Config flow has 4 steps
grep -c "async_step_" custom_components/eeg_energy_optimizer/config_flow.py
# Expected: 4 (user, sensors, forecast, consumption)
```
</verification>

<success_criteria>
- Config flow has 4 steps: user -> sensors -> forecast -> consumption
- Forecast source validated (Solcast/Forecast.Solar must be installed)
- PV forecast entities selected via EntitySelector (no hardcoded IDs, per research pitfall 4)
- Consumption sensor defaults to sensor.power_meter_verbrauch (per D-08)
- Lookback weeks defaults to 8 (per D-09)
- All UI strings in German, English translation provided
- strings.json and de.json are identical
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-forecasting-consumption-profile/02-03-SUMMARY.md`
</output>
