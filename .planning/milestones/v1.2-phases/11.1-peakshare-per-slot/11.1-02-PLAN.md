---
phase: 11.1-peakshare-per-slot
plan: 02
type: execute
wave: 2
depends_on:
  - 11.1-01
files_modified:
  - custom_components/eeg_energy_optimizer/optimizer.py
  - tests/test_dual_window_integration.py
  - CHANGELOG.md
autonomous: true
requirements:
  - 11.1-R1
  - 11.1-R2
  - 11.1-R7
  - 11.1-R8
tags:
  - peakshare
  - markdown
  - activity-log
  - phase-11.1

must_haves:
  truths:
    - "`Decision.discharge_window_start`/`discharge_window_end` zeigen im Dual-Mode den Slot-spezifischen PeakShare-Plan: Slot-A-Plan wenn `discharge_active_slot=='A'`, Slot-B-Plan wenn `'B'`."
    - "`_discharge_detail_status` zeigt Slot-spezifische PeakShare-Times in der Status-Card (`start_time`-Feld) — nicht mehr hartkodiert Slot 'a'."
    - "Markdown-Renderer zeigt `(PeakShare HH:MM-HH:MM)`-Marker im Slot-Header wenn PeakShare-Plan aktiv ist; Slot-Marker `Aktiver Slot: A`/`B` bleibt."
    - "`naechste_aktion`-Text im Decision-State zeigt Slot-spezifischen PeakShare-Plan (Slot A: 'Abend-Entladung HH:MM-HH:MM (PeakShare)' / Slot B: 'Morgen-Entladung HH:MM-HH:MM (PeakShare)')."
    - "Activity-Log-Heartbeat enthaelt Slot+PeakShare-Kontext via existierendes `discharge_active_slot`-Feld + Decision.discharge_peakshare_active — keine neuen Felder."
    - "CHANGELOG dokumentiert Default-Wechsel `discharge_a_reserve_pct` 15->5 und neue PeakShare-Steuerung pro Slot mit Verhaltens-Hinweis fuer Bestands-User."
  artifacts:
    - path: "custom_components/eeg_energy_optimizer/optimizer.py"
      provides: "Slot-aware Markdown-Renderer + _discharge_detail_status + naechste_aktion"
      contains: "discharge_active_slot"
    - path: "tests/test_dual_window_integration.py"
      provides: "TestPeakShareSlotMarkdown Klasse + 24h-Sim-Tests fuer slot-aware PeakShare"
      contains: "class TestPeakShareSlotMarkdown"
    - path: "CHANGELOG.md"
      provides: "v1.2.0-dev Eintrag fuer Phase 11.1 mit Default-Wechsel-Hinweis"
      contains: "Phase 11.1"
  key_links:
    - from: "optimizer.py:_discharge_detail_status"
      to: "_peakshare._discharge_plan[active_slot]"
      via: "Slot-aware plan-lookup"
      pattern: "plan_dict\\.get\\((active_slot|slot_key)"
    - from: "optimizer.py:_build_markdown / naechste_aktion"
      to: "decision.discharge_active_slot + decision.discharge_window_start"
      via: "Slot-aware Markdown-Marker"
      pattern: "discharge_active_slot.*PeakShare\\|PeakShare.*discharge_active_slot"
---

<objective>
Plan 11.1-02 baut auf 11.1-01 auf und macht die Slot-A/B-PeakShare-Pipeline End-to-End sichtbar im UI und in der Telemetrie. Bisher (heute, nach 11.1-01) ist die Decision-Engine slot-spezifisch — aber `_discharge_detail_status` (optimizer.py:871-882) liest hartkodiert `_discharge_plan["a"]`, der Markdown-Renderer kennt keinen PeakShare-Marker pro Slot, und der `naechste_aktion`-Text greift nur fuer den Legacy-Pfad (optimizer.py:1558). Diese drei Stellen werden Slot-aware umgebaut. Plus: 24h-Sim-Integration-Tests fuer die End-to-End-Decision-Markdown-Kette + CHANGELOG-Eintrag fuer den 15->5-Default-Wechsel.

Purpose: User sehen im Panel und in Activity-Log-Heartbeats korrekte slot-spezifische PeakShare-Window-Times. Ohne diese Phase wuerde Plan 11.1-01 zwar funktional korrekt entscheiden, aber UI/Status-Karten zeigten weiterhin Slot-A-Daten waehrend Slot B aktiv ist (verwirrend fuer User).

Output: Slot-aware `_discharge_detail_status`, Slot-aware `naechste_aktion`, Slot-aware Markdown-Marker, neue Test-Klasse `TestPeakShareSlotMarkdown` mit ~5 Tests, CHANGELOG-Eintrag mit Verhaltensaenderung-Hinweis.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v1.2-ROADMAP.md
@.planning/milestones/v1.2-phases/11.1-peakshare-per-slot/11.1-CONTEXT.md
@.planning/milestones/v1.2-phases/11.1-peakshare-per-slot/11.1-RESEARCH.md
@.planning/milestones/v1.2-phases/11.1-peakshare-per-slot/11.1-01-PLAN.md
@CLAUDE.md

<interfaces>
<!-- Existierende Vertraege im Codebase, die der Executor unveraendert nutzt. -->

From custom_components/eeg_energy_optimizer/optimizer.py (nach Plan 11.1-01):
```python
# Decision-Felder (existing nach Phase 11):
@dataclass
class Decision:
    zustand: str
    entladung_aktiv: bool
    discharge_active_slot: str | None  # "A" | "B" | None
    discharge_peakshare_active: bool = False
    discharge_window_start: str | None = None  # "HH:MM"
    discharge_window_end: str | None = None    # "HH:MM"
    discharge_start_time: str = ""             # _discharge_detail_status liefert das
    naechste_aktion: str = ""
    markdown: str = ""
    # ... weitere Felder

# Funktionen (existing):
def _discharge_detail_status(self, snap, should_discharge, min_soc, blocked_by) -> dict:
    # liest heute hartkodiert _discharge_plan["a"] (Z. 879)
    # liefert dict mit "start_time": "HH:MM" oder "HH:MM-HH:MM (PeakShare)"

def _build_markdown(self, snap, decision) -> str:
    # heute: dynamischer Header Slot A vs Slot B, Slot-Marker, Slot-Konfig-Sektion
    # Phase 11.1-02 ergaenzt: PeakShare-Marker im Slot-Header
```

Activity-Log-Field (existing in __init__.py:1303):
```python
"discharge_active_slot": decision.discharge_active_slot,  # "A" | "B" | None
```

PeakShare-Plan-Lookup-Pattern (existing in optimizer.py:1620-1637 — NICHT aendern, schon Slot-aware):
```python
slot_key = "b" if active_slot == "B" else "a"
ps_plan = plan_dict.get(slot_key)
if ps_plan is not None:
    decision.discharge_peakshare_active = True
    decision.discharge_window_start = ps_plan[0].strftime("%H:%M")
    decision.discharge_window_end = ps_plan[1].strftime("%H:%M")
```
</interfaces>

@custom_components/eeg_energy_optimizer/optimizer.py
@custom_components/eeg_energy_optimizer/__init__.py
@tests/test_dual_window_integration.py
@CHANGELOG.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Slot-aware _discharge_detail_status + naechste_aktion + Markdown-PeakShare-Marker</name>
  <files>
    custom_components/eeg_energy_optimizer/optimizer.py,
    tests/test_dual_window_integration.py
  </files>
  <read_first>
    custom_components/eeg_energy_optimizer/optimizer.py (Zeilen 855-936 fuer _discharge_detail_status — heute hartkodiert auf Slot "a";
     Zeilen 1502-1572 fuer naechste_aktion-Block — heute Legacy-Single-Pfad;
     Zeilen 1620-1665 fuer Decision-Field-Population — heute schon Slot-aware (slot_key = "b" if active_slot == "B" else "a");
     Zeilen 1667-1748 fuer _build_markdown — Slot-Marker bereits da, PeakShare-Marker fehlt),
    tests/test_dual_window_integration.py (komplette Datei — 24h-Sim Pattern, _now-monkeypatch, _make_optimizer-Setup),
    .planning/milestones/v1.2-phases/11.1-peakshare-per-slot/11.1-RESEARCH.md (Sektion "Pitfall 7: _discharge_detail_status zeigt Slot-A-Plan auch bei aktivem Slot B" + "Reasons-Strategie")
  </read_first>
  <behavior>
    - Test M1 `test_discharge_detail_status_shows_slot_b_plan_when_b_active`: Mit `active_slot='B'` und sowohl Slot-A- als auch Slot-B-Plan in `_discharge_plan` liefert `_discharge_detail_status(...)["start_time"]` Slot-B-Plan-Times mit "(PeakShare)"-Suffix (nicht Slot-A-Times).
    - Test M2 `test_discharge_detail_status_shows_slot_a_plan_when_a_active`: `active_slot='A'` → Slot-A-Plan-Times.
    - Test M3 `test_discharge_detail_status_falls_back_to_fixed_time_without_plan`: `active_slot=None` und kein Plan → `start_time = "HH:MM"` (Fixzeit aus Config).
    - Test M4 `test_naechste_aktion_text_shows_slot_b_peakshare_window`: zustand=ABEND_ENTLADUNG, active_slot="B", Slot-B-Plan da → naechste_aktion enthaelt "Morgen-Entladung HH:MM-HH:MM (PeakShare)".
    - Test M5 `test_naechste_aktion_text_shows_slot_a_peakshare_window`: zustand=ABEND_ENTLADUNG, active_slot="A", Slot-A-Plan da → naechste_aktion enthaelt "Abend-Entladung HH:MM-HH:MM (PeakShare)".
    - Test M6 `test_markdown_includes_peakshare_marker_when_slot_a_active`: Markdown enthaelt eine Zeile wie "- PeakShare-Fenster: HH:MM-HH:MM" wenn Slot A aktiv mit Plan.
    - Test M7 `test_markdown_includes_peakshare_marker_when_slot_b_active`: Slot B analog.
    - Test M8 `test_markdown_no_peakshare_marker_when_no_plan`: Ohne Plan kein PeakShare-Marker im Markdown (Slot-Marker bleibt).
  </behavior>
  <action>
    Drei Aenderungen in `custom_components/eeg_energy_optimizer/optimizer.py`:

    **(A) `_discharge_detail_status` Slot-aware machen (Z. 857-935):**

    Die Funktion bekommt einen neuen Parameter `active_slot: str | None`. Heutige Signatur (Z. 857-859):
    ```python
    def _discharge_detail_status(
        self, snap: Snapshot, should_discharge: bool, min_soc: float,
        discharge_blocked_by: list[str]
    ) -> dict:
    ```

    Aenderung zu:
    ```python
    def _discharge_detail_status(
        self, snap: Snapshot, should_discharge: bool, min_soc: float,
        discharge_blocked_by: list[str], active_slot: str | None = None,
    ) -> dict:
    ```

    Im Body (Z. 871-883) den Plan-Lookup anpassen:
    ```python
    # ALT (Z. 871-883):
    ps_plan = None
    if self._enable_peakshare and self._peakshare is not None:
        ps_plan_date = getattr(self._peakshare, "_discharge_plan_date", None)
        if ps_plan_date == snap.now.strftime("%Y-%m-%d"):
            plan_dict = getattr(self._peakshare, "_discharge_plan", None) or {}
            if isinstance(plan_dict, dict):
                ps_plan = plan_dict.get("a")
    if ps_plan is not None:
        start_time_str = f"{ps_plan[0].strftime('%H:%M')}-{ps_plan[1].strftime('%H:%M')} (PeakShare)"
    else:
        start_time_str = f"{self._discharge_start_h:02d}:{self._discharge_start_m:02d}"
    ```

    Wird zu:
    ```python
    # Phase 11.1-02: Slot-aware Plan-Lookup. Wenn active_slot bekannt ist,
    # zeige den passenden Slot-Plan; sonst Slot "a" (Legacy + Slot-A-Default).
    #
    # Hinweis zur Cache-Architektur: Wir nutzen _discharge_plan_date (Day-Marker
    # für UI-Lookup), NICHT _discharge_plan_computed_dates[slot] (Per-Slot-Compute-
    # Tracking aus Plan 11.1-01). Beide tragen heute identische Datums-Werte —
    # für die UI-Sicht ("ist der Plan von heute?") reicht der Day-Marker, weil
    # plan_dict.get(slot_key) ein None liefert, wenn der Slot-Plan an dem Tag
    # noch nicht berechnet wurde. Die Per-Slot-Lifecycle-Steuerung gehört zur
    # Compute-Skip-Entscheidung in peakshare.py:get_discharge_plan und ist
    # absichtlich vom UI-Lookup getrennt (Single Responsibility).
    ps_plan = None
    if self._enable_peakshare and self._peakshare is not None:
        ps_plan_date = getattr(self._peakshare, "_discharge_plan_date", None)
        if ps_plan_date == snap.now.strftime("%Y-%m-%d"):
            plan_dict = getattr(self._peakshare, "_discharge_plan", None) or {}
            if isinstance(plan_dict, dict):
                slot_key = "b" if active_slot == "B" else "a"
                ps_plan = plan_dict.get(slot_key)
    if ps_plan is not None:
        start_time_str = (
            f"{ps_plan[0].strftime('%H:%M')}-"
            f"{ps_plan[1].strftime('%H:%M')} (PeakShare)"
        )
    else:
        # Phase 11.1-02: Slot-spezifische Fixzeit-Fallback. Im Dual-Mode
        # zeigt der Fallback die Slot-Startzeit, sonst Legacy discharge_start.
        if active_slot == "B":
            start_time_str = (
                f"{self._discharge_b_start_h:02d}:{self._discharge_b_start_m:02d}"
            )
        elif active_slot == "A" and self._enable_dual_discharge:
            start_time_str = (
                f"{self._discharge_a_start_h:02d}:{self._discharge_a_start_m:02d}"
            )
        else:
            start_time_str = (
                f"{self._discharge_start_h:02d}:{self._discharge_start_m:02d}"
            )
    ```

    Im Aufruf in `_evaluate` (suchen nach `discharge_info = self._discharge_detail_status(`, ist heute Z. 1576-1578) den neuen Parameter mit uebergeben:
    ```python
    # ALT:
    discharge_info = self._discharge_detail_status(
        snap, should_discharge, min_soc, dis_blocked_by_keys
    )
    # NEU:
    discharge_info = self._discharge_detail_status(
        snap, should_discharge, min_soc, dis_blocked_by_keys, active_slot=active_slot,
    )
    ```

    Wichtig: `active_slot` muss bereits BERECHNET sein, bevor `_discharge_detail_status` aufgerufen wird. Heute (Z. 1512-1517) wird `active_slot` aus den `dis_reasons_keys` abgeleitet. Pruefen, ob die Reihenfolge im _evaluate-Body so ist, dass active_slot vor dem _discharge_detail_status-Call gesetzt ist — wenn nicht, den Block (Z. 1512-1517) hochziehen, sodass active_slot vor dem _discharge_detail_status-Call existiert.

    **(B) `naechste_aktion` Slot-aware machen (Z. 1549-1572):**

    Heutiger Code (Z. 1554-1570):
    ```python
    elif zustand == STATE_ABEND_ENTLADUNG:
        # Show PeakShare window times if available.
        ps_plan = self._peakshare.get_discharge_plan(
            self._peakshare_community, 0, 0, None, snap.now
        ) if self._enable_peakshare and self._peakshare and self._peakshare._discharge_plan_date == snap.now.strftime("%Y-%m-%d") else None
        if ps_plan:
            naechste_aktion = (
                f"Abend-Entladung {ps_plan[0].strftime('%H:%M')}-"
                f"{ps_plan[1].strftime('%H:%M')} (PeakShare)"
            )
        else:
            naechste_aktion = (
                f"Abend-Entladung {self._discharge_start_h:02d}:"
                f"{self._discharge_start_m:02d}"
            )
    ```

    Wird zu:
    ```python
    elif zustand == STATE_ABEND_ENTLADUNG:
        # Phase 11.1-02: Slot-aware naechste_aktion. Slot-Plan-Lookup direkt
        # aus _discharge_plan dict statt get_discharge_plan(0, 0, ...) hack —
        # letzteres triggerte einen synthetischen Recompute-Aufruf, jetzt
        # ueberfluessig nach Per-Slot-Caching.
        slot_label = "Morgen-Entladung" if active_slot == "B" else "Abend-Entladung"
        ps_plan = None
        if (
            self._enable_peakshare
            and self._peakshare is not None
            and getattr(self._peakshare, "_discharge_plan_date", None)
            == snap.now.strftime("%Y-%m-%d")
        ):
            plan_dict = getattr(self._peakshare, "_discharge_plan", None) or {}
            if isinstance(plan_dict, dict):
                slot_key = "b" if active_slot == "B" else "a"
                ps_plan = plan_dict.get(slot_key)
        if ps_plan is not None:
            naechste_aktion = (
                f"{slot_label} {ps_plan[0].strftime('%H:%M')}-"
                f"{ps_plan[1].strftime('%H:%M')} (PeakShare)"
            )
        else:
            # Slot-spezifische Fixzeit-Fallback
            if active_slot == "B":
                naechste_aktion = (
                    f"{slot_label} {self._discharge_b_start_h:02d}:"
                    f"{self._discharge_b_start_m:02d}"
                )
            elif active_slot == "A" and self._enable_dual_discharge:
                naechste_aktion = (
                    f"{slot_label} {self._discharge_a_start_h:02d}:"
                    f"{self._discharge_a_start_m:02d}"
                )
            else:
                naechste_aktion = (
                    f"{slot_label} {self._discharge_start_h:02d}:"
                    f"{self._discharge_start_m:02d}"
                )
    ```

    Wichtig: Reihenfolge im _evaluate-Body sicherstellen — `active_slot` muss VOR `naechste_aktion` gesetzt sein. Heute steht der active_slot-Block (Z. 1512-1517) ABER VOR dem naechste_aktion-Block (Z. 1549-1572) — sollte also schon stimmen, aber explizit pruefen.

    **(C) Markdown-PeakShare-Marker im Slot-Header (Z. 1695-1718):**

    Heutiger Code (Z. 1695-1718):
    ```python
    if decision.entladung_aktiv:
        if decision.discharge_active_slot == "B":
            lines.append("### Morgen-Entladung")
        else:
            lines.append("### Abend-Entladung")
        if decision.discharge_active_slot == "A":
            lines.append("- Aktiver Slot: A")
        elif decision.discharge_active_slot == "B":
            lines.append("- Aktiver Slot: B")
        lines.append(
            f"- Startzeit: {self._discharge_start_h:02d}:{self._discharge_start_m:02d}"
        )
        # ... weitere lines.append
    ```

    Phase 11.1-02 ergaenzt einen PeakShare-Window-Marker, wenn ein Plan aktiv ist. Im selben `if decision.entladung_aktiv:`-Block, NACH dem Slot-Marker und VOR/STATT der bisherigen "Startzeit"-Zeile:

    ```python
    if decision.entladung_aktiv:
        if decision.discharge_active_slot == "B":
            lines.append("### Morgen-Entladung")
        else:
            lines.append("### Abend-Entladung")
        if decision.discharge_active_slot == "A":
            lines.append("- Aktiver Slot: A")
        elif decision.discharge_active_slot == "B":
            lines.append("- Aktiver Slot: B")
        # Phase 11.1-02: PeakShare-Window-Marker pro Slot
        if decision.discharge_peakshare_active and decision.discharge_window_start:
            lines.append(
                f"- PeakShare-Fenster: "
                f"{decision.discharge_window_start}-{decision.discharge_window_end}"
            )
        # Slot-spezifische Startzeit (Phase 11.1-02): zeige die echte Slot-Startzeit
        # statt der Legacy `_discharge_start_h/m`-Felder, die im Dual-Mode irrelevant sind.
        if decision.discharge_active_slot == "A":
            lines.append(
                f"- Startzeit: {self._discharge_a_start_h:02d}:"
                f"{self._discharge_a_start_m:02d}"
            )
        elif decision.discharge_active_slot == "B":
            lines.append(
                f"- Startzeit: {self._discharge_b_start_h:02d}:"
                f"{self._discharge_b_start_m:02d}"
            )
        else:
            lines.append(
                f"- Startzeit: {self._discharge_start_h:02d}:"
                f"{self._discharge_start_m:02d}"
            )
        # ... weitere bestehende lines.append (Leistung/Ziel-SOC/PV/Verbrauch — unveraendert)
    ```

    Die EXISTIERENDE Zeile `lines.append(f"- Startzeit: ...")` (heute Z. 1707-1709) wird durch das if-elif-else ersetzt; alle anderen lines.append-Zeilen (Leistung, Ziel-SOC, PV-Prognose, Verbrauchsprognose) bleiben unveraendert.

    **(D) `tests/test_dual_window_integration.py` neue Test-Klasse `TestPeakShareSlotMarkdown`:**

    Pattern wie bei den anderen Klassen in dieser Datei (24h-Sim mit `_now`-monkeypatch). Ich orientiere mich an `TestPeakShareSlotMarkdown` aus dem Plan-Brief (RESEARCH.md Sektion "Test-Layout"):

    ```python
    class TestPeakShareSlotMarkdown:
        """Phase 11.1-02: End-to-End-Test fuer Slot-aware PeakShare-UI-Output."""

        def test_discharge_detail_status_shows_slot_b_plan_when_b_active(
            self, mock_hass, mock_inverter, mock_coordinator, mock_provider,
        ): ...
        def test_discharge_detail_status_shows_slot_a_plan_when_a_active(...): ...
        def test_discharge_detail_status_falls_back_to_fixed_time_without_plan(...): ...
        def test_naechste_aktion_text_shows_slot_b_peakshare_window(...): ...
        def test_naechste_aktion_text_shows_slot_a_peakshare_window(...): ...
        def test_markdown_includes_peakshare_marker_when_slot_a_active(...): ...
        def test_markdown_includes_peakshare_marker_when_slot_b_active(...): ...
        def test_markdown_no_peakshare_marker_when_no_plan(...): ...
    ```

    Mock-Pattern: setze `opt._peakshare` mit MagicMock; `opt._peakshare._discharge_plan = {"a": (start_a, end_a), "b": (start_b, end_b)}`; `opt._peakshare._discharge_plan_date = snap.now.strftime("%Y-%m-%d")`. Dann ein Snapshot mit passenden Werten und Decision via `_evaluate` durchrechnen.

    Beispiel `test_naechste_aktion_text_shows_slot_b_peakshare_window`:
    ```python
    def test_naechste_aktion_text_shows_slot_b_peakshare_window(
        self, mock_hass, mock_inverter, mock_coordinator, mock_provider,
    ):
        cfg = _make_config(
            enable_dual_discharge=True,
            enable_slot_a=True, enable_slot_b=True,
            discharge_a_start_time="20:00",
            discharge_b_start_time="03:00",
            enable_peakshare=True,
        )
        opt = _make_optimizer(
            mock_hass, mock_inverter, mock_coordinator, mock_provider, config=cfg,
        )
        # Slot B aktiv: 03:30 — Plan 03:00-04:00
        now = datetime(2026, 12, 22, 3, 30, tzinfo=timezone.utc)
        plan_b = (
            datetime(2026, 12, 22, 3, 0, tzinfo=timezone.utc),
            datetime(2026, 12, 22, 4, 0, tzinfo=timezone.utc),
        )
        ps = MagicMock()
        ps._discharge_plan = {"a": None, "b": plan_b}
        ps._discharge_plan_date = "2026-12-22"
        ps.get_discharge_plan = MagicMock(return_value=plan_b)
        opt._peakshare = ps
        opt._enable_peakshare = True
        opt._peakshare_community = "Testgemeinde"
        opt._enable_slot_a = False  # nur Slot B aktiv im Test
        opt._slot_b_activated_date = None  # keine Reaktivierungs-Penalty

        snap = _make_snapshot(
            now=now, battery_soc=80.0, battery_capacity_kwh=10.0,
            sunrise=datetime(2026, 12, 22, 7, 30, tzinfo=timezone.utc),
            sunrise_today=datetime(2026, 12, 22, 7, 30, tzinfo=timezone.utc),
            consumption_overnight_kwh=2.0, pv_tomorrow_kwh=20.0,
        )
        decision = opt._evaluate(snap, mode=MODE_TEST)

        assert decision.discharge_active_slot == "B"
        assert "Morgen-Entladung" in decision.naechste_aktion
        assert "03:00-04:00 (PeakShare)" in decision.naechste_aktion
    ```

    Wichtig: Tests muessen die Snapshot-Felder so setzen, dass sowohl Slot-A- als auch Slot-B-Bedingungen je nach Test stimmen oder nicht stimmen. Pattern aus existing `TestDualWindow24hSimulation` als Vorlage.
  </action>
  <verify>
    <automated>pytest tests/test_dual_window_integration.py::TestPeakShareSlotMarkdown -x -q && pytest tests/test_dual_window_integration.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "active_slot=active_slot" custom_components/eeg_energy_optimizer/optimizer.py` liefert >=1 Treffer (Aufruf von _discharge_detail_status mit active_slot)
    - `grep -n "slot_key = \"b\" if active_slot == \"B\" else \"a\"" custom_components/eeg_energy_optimizer/optimizer.py` liefert >=2 Treffer (in _discharge_detail_status + in naechste_aktion-Block; existing einer in _evaluate Z. 1632 zaehlt zusaetzlich)
    - `grep -n "Morgen-Entladung\\|Abend-Entladung" custom_components/eeg_energy_optimizer/optimizer.py` liefert >=4 Treffer (Markdown-Header bestehend + naechste_aktion neu mit slot_label-Variable)
    - `grep -n "PeakShare-Fenster" custom_components/eeg_energy_optimizer/optimizer.py` liefert >=1 Treffer (Markdown-Marker)
    - `grep -n "_discharge_a_start_h\\|_discharge_b_start_h" custom_components/eeg_energy_optimizer/optimizer.py` liefert >=4 Treffer im _build_markdown- + naechste_aktion- + _discharge_detail_status-Bereich (slot-spezifische Startzeit-Zeile)
    - Pruefung: KEIN synthetischer `get_discharge_plan(self._peakshare_community, 0, 0, None, snap.now)`-Hack-Aufruf mehr — wurde durch direkten dict-Lookup ersetzt: `grep -n "get_discharge_plan(self._peakshare_community, 0, 0, None" custom_components/eeg_energy_optimizer/optimizer.py` liefert 0 Treffer
    - `pytest tests/test_dual_window_integration.py::TestPeakShareSlotMarkdown -x -q` ist gruen mit >=8 passing-Tests
    - `pytest tests/test_dual_window_integration.py -q` ist gruen (existierende ~10 Tests + 8 neue = ~18)
    - `pytest tests/ -q` zeigt keine NEUEN Failures gegenueber Phase-11-Baseline
    - Echte Umlaute in NEUEN Code-Zeilen: `git diff main -- custom_components/eeg_energy_optimizer/optimizer.py | grep -E "^\+" | grep -vE "^\+\+\+" | grep -nE "[a-zA-Z](ae|oe|ue|Ae|Oe|Ue)[a-zA-Z]"` liefert 0 Treffer (Whitelist-Kandidaten wie "blau", "grau", englische Wörter sind erlaubt — bei False-Positives ignorieren). Positiv-Check: `git diff main -- custom_components/eeg_energy_optimizer/optimizer.py | grep -E "^\+" | grep -E "ä|ö|ü"` liefert mindestens 1 Treffer (deutsche Strings im Diff)
  </acceptance_criteria>
  <done>
    `_discharge_detail_status` und `naechste_aktion` zeigen Slot-spezifische PeakShare-Plan-Times statt hartkodiert Slot "a". Markdown-Renderer zeigt einen "PeakShare-Fenster: HH:MM-HH:MM"-Marker im Slot-Header wenn ein Plan aktiv ist. Slot-spezifische Startzeit (a_start_time/b_start_time) statt Legacy `_discharge_start_h/m` im Dual-Mode. Test-Suite `TestPeakShareSlotMarkdown` mit ~8 Tests gruen. Decision.discharge_window_start/end (in optimizer.py:1620-1637, schon Slot-aware) bleibt unveraendert — Decision-Field-Population-Pattern wird wiederverwendet, nicht dupliziert.
  </done>
</task>

<task type="auto">
  <name>Task 2: CHANGELOG-Eintrag fuer Phase 11.1 + Verifikations-Sweep</name>
  <files>
    CHANGELOG.md
  </files>
  <read_first>
    CHANGELOG.md (komplette Datei — letzte v1.2.0-dev-Eintraege als Pattern; Phase 11 hat bereits einen "Verhaltensaenderung beim Update"-Abschnitt mit Format),
    .planning/milestones/v1.2-phases/11.1-peakshare-per-slot/11.1-CONTEXT.md (Locked Decision #2: 15 -> 5),
    .planning/milestones/v1.2-phases/11.1-peakshare-per-slot/11.1-RESEARCH.md (Sektion "CHANGELOG-Eintrag (User-sichtbar)" als Vorlage)
  </read_first>
  <action>
    In `CHANGELOG.md` einen neuen Eintrag fuer Phase 11.1 hinzufuegen. Den genauen Stil aus dem bestehenden v1.2.0-dev-Phase-11-Eintrag uebernehmen (gleiche Header-Struktur, gleicher "Verhaltensaenderung beim Update"-Abschnitt-Stil).

    Inhalt (Pflicht-Felder, deutsch, echte Umlaute):

    ```markdown
    ### Phase 11.1: PeakShare-Steuerung der Slot-A/B-Fenster

    **Neu:**
    - PeakShare optimiert jetzt auch im Dual-Window-Modus (Slot A + Slot B) das Entlade-Sub-Fenster INNERHALB der konfigurierten Slot-Zeiten. Beispiel: Slot A 20:00-03:00 -> PeakShare findet automatisch das beste Sub-Fenster (z.B. 22:00-00:00), wenn der EEG-Bedarf dort am hoechsten ist.
    - Slot A wird immer bevorzugt; Slot B nutzt die Reserve-Energie (siehe Default-Wechsel unten).
    - Pro Slot getrennt entscheidbar: Wenn PeakShare-Daten den Slot-Zeitraum nicht abdecken, faellt der betroffene Slot auf das Fixzeit-Verhalten zurueck. Der andere Slot kann separat PeakShare-gesteuert laufen.

    **Verhaltensaenderung beim Update:**
    - **Default fuer `discharge_a_reserve_pct` (Slot-A-Reserve fuer Slot B) wurde von 15 % auf 5 % gesenkt.** Bei aktiver PeakShare-Steuerung pro Slot bekommt Slot A das Hauptenergie-Budget; Slot B wird nur als kleine Morgen-Spitze bedient.
    - **Bestands-Setups behalten ihren bisher konfigurierten Wert** (kein Auto-Override; setdefault-Migration aendert nur Setups, die den Wert noch nicht explizit gesetzt haben).
    - User, die bewusst eine groessere Slot-B-Energiereserve wollen, koennen `discharge_a_reserve_pct` weiterhin im Panel auf bis zu 50 % setzen (Voluptuous-Range unveraendert).

    **Bug-Fix:**
    - PeakShare-Cache-Tageslock blockierte zuvor den zweiten Slot-Plan-Compute am selben Tag. Slot A und Slot B koennen jetzt unabhaengig PeakShare-Plaene berechnen (Per-Slot-Compute-Tracking via `_discharge_plan_computed_dates`).

    **Schliesst Spec-Luecke aus Phase 11:**
    - Phase 11 SPEC §"In scope" Z. 75 hatte "PeakShare-Integration für Dual-Mode (zwei separate Sliding-Window-Suchen, eine pro Slot, mit slot-spezifischem `available_kwh`)" zugesagt. Plan 11-02 hatte das Cache-Schema (dict[a/b]) vorbereitet, aber den eigentlichen PeakShare-Aufruf nie nachgereicht. Phase 11.1 schliesst diese Luecke.

    **UI-Anzeige:**
    - Decision-Markdown zeigt im aktiven Slot den PeakShare-Window-Marker (z.B. "- PeakShare-Fenster: 22:00-00:00").
    - "Naechste Aktion"-Text zeigt slot-spezifische PeakShare-Window-Times.
    - Status-Card-Startzeit ist slot-aware (Slot-A-Plan vs Slot-B-Plan abhaengig vom aktiven Slot).
    ```

    Position des Eintrags: NACH dem bestehenden Phase-11-Eintrag, VOR aelteren Versionen. Genaue Position basiert auf der bestehenden CHANGELOG-Struktur — typisch direkt unter dem `## v1.2.0-dev` Header oder als naechster Untereintrag nach Phase 11.

    KEINE Versionserhoehung. Der Eintrag bleibt unter dem aktuellen `v1.2.0-dev` (oder dem bestehenden v1.2.0-dev-X-Sub-Tag). User entscheidet ueber Versionsbumps separat.
  </action>
  <verify>
    <automated>grep -c "Phase 11.1" CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 11.1" CHANGELOG.md` liefert >=1 (mindestens ein Treffer)
    - `grep -c "Verhaltensaenderung beim Update" CHANGELOG.md` liefert >=2 (Phase 11 bereits + neuer Phase 11.1-Eintrag)
    - `grep -c "PeakShare" CHANGELOG.md` liefert >=2 (vermutlich schon mehr — alte Eintraege)
    - Positiv-Check Block-basiert: `awk '/^## \[1.2.0-dev/{flag=1} /^## \[1.1/{flag=0} flag' CHANGELOG.md | grep -A 60 "Phase 11.1\|11.1-peakshare\|PeakShare-Steuerung der Slot" | grep -E "ä|ö|ü|Ä|Ö|Ü"` liefert mindestens 1 Treffer (echte Umlaute IM NEUEN Phase-11.1-Block)
    - Negativ-Check Block-basiert: `awk '/^## \[1.2.0-dev/{flag=1} /^## \[1.1/{flag=0} flag' CHANGELOG.md | grep -A 60 "Phase 11.1\|11.1-peakshare\|PeakShare-Steuerung der Slot" | grep -nE "[a-zA-Z](ae|oe|ue|Ae|Oe|Ue)[a-zA-Z]"` liefert 0 Treffer — bzw. ausschließlich Whitelist-Kandidaten (blau, grau, Auer, Bauer, englische Begriffe). Das Konstrukt isoliert den NEUEN v1.2.0-dev-Block und filtert Phase 11.1-Sektion heraus, sodass der Bestands-Phase-11-Eintrag NICHT mit verglichen wird.
    - Manuelles Pruefen: Eintrag enthaelt die drei Pflicht-Sektionen "Neu / Verhaltensaenderung beim Update / Bug-Fix"
    - Manuelles Pruefen: Eintrag verweist auf den 15->5-Default-Wechsel UND erwaehnt explizit "Bestands-Setups behalten ihren Wert"
  </acceptance_criteria>
  <done>
    CHANGELOG.md hat einen Phase-11.1-Eintrag im selben Stil wie der Phase-11-Eintrag, mit Sektionen Neu / Verhaltensaenderung beim Update / Bug-Fix / Schliesst Spec-Luecke / UI-Anzeige. Default-Wechsel 15->5 ist klar erklaert mit Hinweis auf Bestands-Schutz. Echte Umlaute, keine ASCII-Substitution. Keine Versionserhoehung.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Decision → Activity-Log/Markdown | In-process Boundary; Phase 11.1-02 fuegt KEINE neuen Felder hinzu, nutzt nur existierende `discharge_active_slot` + `discharge_window_start/end` |
| Markdown-String → Frontend-Sensor-State | Cross-process Boundary (HA-State-API); Markdown-Inhalt ist user-visible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.1-07 | Information Disclosure | PeakShare-Window-Times im Markdown + naechste_aktion | accept | Bereits in Phase 8 + 11 akzeptiert (PeakShare-community-name + window-times werden in Activity-Log und Decision-Markdown angezeigt). Phase 11.1-02 macht die Anzeige slot-aware, fuegt aber keine NEUEN Felder hinzu. |
| T-11.1-08 | Tampering | Slot-Marker-Inkonsistenz: `discharge_active_slot=='A'` aber Markdown zeigt Slot-B-Plan | mitigate | Slot-aware Plan-Lookup nutzt `slot_key = "b" if active_slot == "B" else "a"` konsistent in 3 Stellen (`_discharge_detail_status`, `naechste_aktion`, Decision-Field-Population in Z. 1632). Tests M1-M3 + bestehende `TestPeakShareSlotIntegration` decken dies ab. |
| T-11.1-09 | Tampering | Synthetischer get_discharge_plan(0, 0, None, snap.now)-Hack-Aufruf in naechste_aktion-Block fuehrte zu unerwarteter Re-Compute-Logik | mitigate | Ersetzt durch direkten dict-Lookup. KEIN Recompute mehr im Markdown-Pfad. Verifikation via grep-Negativcheck. |
</threat_model>

<verification>
- `pytest tests/ -q` ist gruen (412/413 oder besser; Pre-existing OOS-Failure bleibt akzeptiert)
- `pytest tests/test_dual_window.py tests/test_dual_window_integration.py -q` ist gruen
- Manuelle Code-Review: `_discharge_detail_status` UND `naechste_aktion`-Block UND `_build_markdown` nutzen alle das gleiche `slot_key`-Pattern
- Manuelle Code-Review: KEIN synthetischer `get_discharge_plan(..., 0, 0, None, ...)`-Aufruf mehr in optimizer.py (war ein Phase-11-Workaround, jetzt durch dict-Lookup ersetzt)
- CHANGELOG-Eintrag enthaelt 15 -> 5 Default-Wechsel-Hinweis mit Bestands-User-Schutz
</verification>

<success_criteria>
- `_discharge_detail_status` ist Slot-aware (neuer `active_slot=` Parameter, Slot-spezifischer Plan-Lookup)
- `naechste_aktion`-Text ist Slot-aware (zeigt Morgen-Entladung vs Abend-Entladung + Slot-spezifische PeakShare-Times)
- `_build_markdown` zeigt einen "PeakShare-Fenster: HH:MM-HH:MM"-Marker im Slot-Header wenn Plan aktiv
- Slot-spezifische Startzeit-Anzeige im Markdown (a_start_time/b_start_time statt Legacy discharge_start_time im Dual-Mode)
- 8 neue Tests in `TestPeakShareSlotMarkdown` gruen
- CHANGELOG.md hat Phase-11.1-Eintrag mit allen Pflicht-Sektionen, echte Umlaute, ohne Versionsbump
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.2-phases/11.1-peakshare-per-slot/11.1-02-SUMMARY.md` mit:
- Was tatsaechlich gebaut wurde (slot-aware UI/Markdown/Activity-Log)
- Test-Coverage (8 neue Tests fuer Slot-aware-Output)
- CHANGELOG-Eintrag (textauszug)
- Phase-11.1-Gesamt-Bilanz: Welche Spec-Luecke geschlossen, welche Locked Decisions umgesetzt
- Naechster Schritt: `/gsd-verify-phase 11.1` -> nach gruenen Tests `/gsd-uat-phase 11.1` fuer 7-Tage-Beobachtung
</output>