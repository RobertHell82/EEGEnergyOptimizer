---
phase: 11
plan: 04
type: execute
wave: 3
depends_on: [02]
files_modified:
  - custom_components/eeg_energy_optimizer/optimizer.py
  - custom_components/eeg_energy_optimizer/__init__.py
  - custom_components/eeg_energy_optimizer/frontend/eeg-optimizer-panel.js
  - tests/test_dual_window_integration.py
  - CHANGELOG.md
autonomous: true
requirements: [SPEC-7, SPEC-8]
tags: [dual-window, markdown, activity-log, changelog]

must_haves:
  truths:
    - "_build_markdown rendert bei aktiver Abend-Entladung den aktiven Slot ('Aktiver Slot: A' oder 'Aktiver Slot: B') in der Markdown-Status-Card"
    - "Markdown bleibt rückwärtskompatibel: Legacy-Pfad ohne discharge_active_slot rendert wie bisher (kein Slot-Marker)"
    - "Activity-Log-Entry-Dict trägt das Feld discharge_active_slot ('A' | 'B' | None) bei jedem Log-Eintrag (D-09 — Slot-Kontext für Telemetrie und Frontend)"
    - "Frontend-Aktivitäts-Timeline (eeg-optimizer-panel.js ~Zeile 4013) zeigt bei Abend-Entladung-Einträgen den Slot-Suffix (z.B. 'Abend-Entladung (Slot A)')"
    - "End-to-End 24h-Decision-Sequenz-Test prüft die Slot-Marker-Persistenz im _evaluate-Pfad"
    - "CHANGELOG.md hat einen v1.2.0-dev Eintrag mit prominentem 'Verhaltensänderung beim Update'-Abschnitt"
    - "Test-Datei tests/test_dual_window_integration.py existiert getrennt von tests/test_dual_window.py (Wave-3-Kollision mit 11-03 vermieden)"
  artifacts:
    - path: custom_components/eeg_energy_optimizer/optimizer.py
      provides: "_build_markdown-Erweiterung mit Slot-Marker + Slot-A/B-Konfig-Status"
      contains: "Aktiver Slot:"
    - path: custom_components/eeg_energy_optimizer/__init__.py
      provides: "Activity-Log-Entry-Dict mit discharge_active_slot-Feld (D-09)"
      contains: "\"discharge_active_slot\":"
    - path: custom_components/eeg_energy_optimizer/frontend/eeg-optimizer-panel.js
      provides: "Aktivitäts-Timeline-Render zeigt Slot-Suffix bei Abend-Entladung"
      contains: "discharge_active_slot"
    - path: tests/test_dual_window_integration.py
      provides: "TestMarkdownRendering + TestEvaluate24hSlotMarkerPersistence + TestActivityLogSlotContext (separate Datei zur Vermeidung der Wave-3-Kollision mit 11-03)"
      contains: "class TestActivityLogSlotContext"
    - path: CHANGELOG.md
      provides: "v1.2.0-dev Eintrag mit Dual-Window-Sektion + Verhaltensänderung-Hinweis"
      contains: "Verhaltensänderung beim Update"
  key_links:
    - from: "optimizer.py:_build_markdown"
      to: "Decision.discharge_active_slot (aus Plan 11-01 + 11-02)"
      via: "Markdown-Renderer liest decision.discharge_active_slot und rendert es"
      pattern: "decision\\.discharge_active_slot"
    - from: "__init__.py:_log_activity"
      to: "Decision.discharge_active_slot"
      via: "Activity-Log-Entry-Dict serialisiert discharge_active_slot direkt aus decision (D-09)"
      pattern: "\"discharge_active_slot\": decision\\.discharge_active_slot"
    - from: "__init__.py:_log_activity entry_data"
      to: "frontend/eeg-optimizer-panel.js Aktivitäts-Timeline-Render (~Zeile 4013)"
      via: "hass.bus.async_fire('eeg_optimizer_activity', entry_data) → WebSocket → Panel liest e.discharge_active_slot"
      pattern: "e\\.discharge_active_slot"
---

<objective>
Phase 11 Wave 3 (parallel zu 11-03): Markdown-Renderer-Erweiterung, Activity-Log-Slot-Kontext (D-09), Frontend-Activity-Timeline-Slot-Suffix, integriertes 24h-_evaluate-Sequenz-Testing, und CHANGELOG-Eintrag.

Dieser Plan macht die Slot-Information für Endnutzer sichtbar (Decision-Card im HA-Dashboard rendert Markdown vom Sensor "Entscheidung" aus optimizer.py:_build_markdown), führt den Slot-Kontext im Activity-Log fort (gemäß D-09 verpflichtend, sowohl für Frontend-Anzeige als auch für künftige Telemetrie), und schreibt einen sauberen 24h-Integration-Test der den vollen _evaluate-Pfad inkl. Slot-Marker-Persistenz validiert. Das CHANGELOG dokumentiert die Verhaltensänderung im "Verhaltensänderung beim Update"-Stil (D-04 fordert prominente Kommunikation).

**Wave-3-Kollisions-Fix:** Tests dieses Plans landen in der NEUEN Datei `tests/test_dual_window_integration.py` (NICHT `tests/test_dual_window.py`), weil 11-03 parallel in Wave 3 läuft und ebenfalls in `tests/test_dual_window.py` schreibt (TestSolarEdgeXOR + TestInverterRaceValidation). Trennung verhindert Merge-Konflikte ohne Wave-Sequenzialisierung.

Adressiert SPEC-Requirements:
- §7 Telemetry-Reasons pro Slot (Activity-Log nutzt Reasons + Decision.discharge_active_slot konsistent — D-09)
- §8 Independent Slot-Aktivierung (24h-_evaluate-Test prüft Slot-Marker-Persistenz im vollen Cycle)

Adressiert User-Decisions:
- D-08 Status-Anzeige (Decision-Card) zeigt discharge_active_slot als visuelle Markierung
- D-09 Slot-Kontext explizit im Activity-Log (verpflichtend, NICHT optional)
- D-04 Default-Wechsel intendiert: CHANGELOG-Eintrag mit "Verhaltensänderung beim Update"-Sektion

Purpose: Endnutzer sehen den aktiven Slot im HA-Dashboard und Activity-Log; CHANGELOG kommuniziert die Verhaltensänderung beim Update prominent — Voraussetzung für die 7-Tage-UAT, in der User entscheiden "gute Idee oder nicht".
Output: Markdown-Renderer-Update, Activity-Log-Update mit discharge_active_slot-Feld, Frontend-Render-Update, +3 neue Test-Klassen mit ~10 Test-Methoden in NEUER Datei `tests/test_dual_window_integration.py`, CHANGELOG-Eintrag.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/milestones/v1.2-phases/11-dual-window-discharge/11-SPEC.md
@.planning/milestones/v1.2-phases/11-dual-window-discharge/11-CONTEXT.md
@.planning/milestones/v1.2-phases/11-dual-window-discharge/11-RESEARCH.md
@.planning/milestones/v1.2-phases/11-dual-window-discharge/11-PATTERNS.md
@.planning/phases/11-dual-window-discharge/11-01-SUMMARY.md
@.planning/phases/11-dual-window-discharge/11-02-SUMMARY.md

<interfaces>
<!-- Bestehende Patterns aus dem Code -->

Aus optimizer.py:1232-1298 (_build_markdown — heutige Struktur):
```python
def _build_markdown(self, snap: Snapshot, decision: Decision) -> str:
    lines = []
    lines.append(f"**Status:** {decision.zustand}")
    lines.append("")
    if decision.entladung_aktiv:
        lines.append("### Abend-Entladung")
        lines.append(f"- Startzeit: {self._discharge_start_h:02d}:{self._discharge_start_m:02d}")
        lines.append(f"- Leistung: {decision.entladeleistung_kw:.1f} kW")
        lines.append(f"- Ziel-SOC: {decision.min_soc_berechnet:.0f}%")
        ...
    if decision.reasons:
        lines.append("### Diagnose (Gründe)")
        for key in decision.reasons:
            lines.append(f"- {REASON_LABELS_DE.get(key, key)}")
    ...
```

Aus __init__.py:1255-1275 (_log_activity — Activity-Log-Entry-Builder, exakte Stelle für D-09-Erweiterung):
```python
def _log_activity(decision, reason):
    """Append an activity entry and fire a HA event."""
    snap_dict = decision.snapshot if isinstance(decision.snapshot, dict) else {}
    soc_val = snap_dict.get("soc_pct")
    entry_data = {
        "timestamp": decision.timestamp,
        "zustand": decision.zustand,
        "reason": reason,
        "soc": soc_val,
        "min_soc": round(decision.min_soc_berechnet, 1),
        "pv_today": round(decision.morning_pv_today_kwh, 1),
        "pv_tomorrow": round(decision.discharge_pv_tomorrow_kwh, 1),
        "bedarf": round(decision.energiebedarf_kwh, 1),
        "discharge_bedarf": round(decision.discharge_demand_total_kwh, 1),
        "discharge_pv": round(decision.discharge_pv_tomorrow_kwh, 1),
        "ausführung": decision.ausführung,
    }
    activity_log.append(entry_data)
    hass.bus.async_fire("eeg_optimizer_activity", entry_data)
    hass.async_create_task(_save_activity_log())
```

→ **D-09-Erweiterung in 11-04 Task 1:** Füge `"discharge_active_slot": decision.discharge_active_slot` als zusätzliches Feld in `entry_data` ein. Frontend-Render in `frontend/eeg-optimizer-panel.js` (~Zeile 4013) liest `e.discharge_active_slot` und zeigt im Status-Text " (Slot A)" / " (Slot B)" bei `e.zustand === "Abend-Entladung"`.

Aus frontend/eeg-optimizer-panel.js:4005-4015 (Aktivitäts-Timeline-Render — Erweiterungs-Stelle):
```javascript
const reason = e.reason === "Heartbeat" ? `<span style="opacity:0.5">${e.zustand}</span>` : `<strong>${e.zustand}</strong>`;
const changeBadge = e.reason === "Heartbeat" ? "" : `<span class="activity-badge" ...>Änderung</span>`;
...
return `<div class="activity-entry">
    <div class="activity-time">${dateStr}<br>${timeStr}</div>
    <div class="activity-dot" style="background:${color}">${icon}</div>
    <div class="activity-content">
      <div class="activity-header">${reason} ${changeBadge} ${testBadge}</div>
      <div class="activity-details">SOC ${e.soc != null ? e.soc + "%" : "—"}${e.zustand === "Abend-Entladung" ? ` &rarr; Ziel-SOC ${fmtDe(e.min_soc, 0)}%` : ""} ...
```

→ **D-09-Frontend-Erweiterung:** `${e.zustand}` im `reason`-Template um Slot-Suffix erweitern: bei `e.zustand === "Abend-Entladung"` und `e.discharge_active_slot in ("A","B")` wird "Abend-Entladung (Slot A)" oder "Abend-Entladung (Slot B)" gerendert.

Existierende `decision.discharge_active_slot` aus Plan 11-01 (Default None) wird in 11-02 in `_evaluate` auf "A" oder "B" gesetzt.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _build_markdown + Activity-Log discharge_active_slot (D-09) + Frontend-Slot-Suffix + 24h-_evaluate-Integration-Test</name>
  <files>
    custom_components/eeg_energy_optimizer/optimizer.py,
    custom_components/eeg_energy_optimizer/__init__.py,
    custom_components/eeg_energy_optimizer/frontend/eeg-optimizer-panel.js,
    tests/test_dual_window_integration.py
  </files>
  <read_first>
    custom_components/eeg_energy_optimizer/optimizer.py (Zeilen 1232-1298 für _build_markdown KOMPLETT; Zeilen 1057-1230 für _evaluate-Pfad inkl. der Stellen wo Decision befüllt wird; Plan-11-02-Erweiterungen bereits gemerged),
    custom_components/eeg_energy_optimizer/__init__.py (Zeilen 1240-1280 für die _log_activity-Funktion und das entry_data-Dict; das ist die EXAKTE Stelle, an der `discharge_active_slot` als zusätzliches Feld serialisiert werden MUSS — D-09 fordert Slot-Kontext explizit für Activity-Log und Telemetrie. Suchpattern: `def _log_activity`, `entry_data = {`, `hass.bus.async_fire("eeg_optimizer_activity"`),
    custom_components/eeg_energy_optimizer/__init__.py (Zeilen 1390-1410 für den State-Change-Trigger im _optimizer_cycle, der `_log_activity(decision, reason)` aufruft — der `decision`-Parameter trägt bereits `discharge_active_slot` aus Plan 11-02; KEINE Code-Änderung am Aufruf nötig, nur am Dict-Builder),
    custom_components/eeg_energy_optimizer/frontend/eeg-optimizer-panel.js (Zeilen 3990-4030 für die Aktivitäts-Timeline-Render-Funktion; Suchpattern: `e.zustand === "Abend-Entladung"` rund um Zeile 4013 — das ist die Stelle für den Slot-Suffix-Render),
    .planning/milestones/v1.2-phases/11-dual-window-discharge/11-PATTERNS.md (Sektion "_build_markdown Slot-Rendering"),
    .planning/milestones/v1.2-phases/11-dual-window-discharge/11-RESEARCH.md (Open-Question 3 zu Activity-Log-Texten — Status RESOLVED),
    .planning/milestones/v1.2-phases/11-dual-window-discharge/11-CONTEXT.md (D-09 — Slot-Kontext im Activity-Log VERPFLICHTEND),
    .planning/phases/11-dual-window-discharge/11-02-SUMMARY.md
  </read_first>
  <behavior>
    - _build_markdown: bei decision.entladung_aktiv UND decision.discharge_active_slot in {"A","B"} → zusätzliche Markdown-Zeile "- Aktiver Slot: A (Abend-Entladung)" oder "- Aktiver Slot: B (Morgen-Entladung)"
    - _build_markdown: bei discharge_active_slot=None (Legacy-Pfad oder keine Aktivität) → keine Slot-Zeile, Markdown bleibt rückwärtskompatibel
    - _build_markdown: zusätzliche Sektion "### Slot-Konfiguration" wenn enable_dual_discharge=True, listet Slot A und Slot B Konfig-Werte
    - _log_activity in __init__.py: das von der Funktion gebaute `entry_data`-Dict enthält das zusätzliche Feld `"discharge_active_slot": decision.discharge_active_slot` (Wert "A", "B" oder None — direkt aus Decision-Dataclass durchgereicht). Das Feld ist auch im `eeg_optimizer_activity`-Bus-Event sichtbar (per `hass.bus.async_fire`).
    - frontend/eeg-optimizer-panel.js Aktivitäts-Timeline (~Zeile 4013): bei `e.zustand === "Abend-Entladung"` UND `e.discharge_active_slot in ("A","B")` wird der Status-Text um " (Slot A)" oder " (Slot B)" erweitert; bei `discharge_active_slot=null/undefined` bleibt das Render-Verhalten identisch zur Legacy-Version (rückwärtskompatibel mit alten Activity-Log-Einträgen aus dem persistenten Store).
    - 24h-_evaluate-Integration-Test: ruft _evaluate (NICHT _should_discharge direkt) über eine Sequenz von Stunden auf, prüft dass _slot_a_activated_date und _slot_b_activated_date korrekt gesetzt werden und decision.discharge_active_slot in jedem Cycle korrekt ist
    - Activity-Log-Slot-Kontext-Test: ruft `_log_activity` (oder reproduziert die Logik durch direktes Bauen des entry_data-Dicts via Decision-Mock) auf und prüft, dass `entry_data["discharge_active_slot"]` mit `decision.discharge_active_slot` übereinstimmt — KEIN pytest.mark.skip mehr.
  </behavior>
  <action>
    **A. optimizer.py — _build_markdown-Erweiterung (Zeilen 1232-1298 lokalisieren und ergänzen):**

    Suche nach `if decision.entladung_aktiv:` Block. Innerhalb des Blocks, **direkt nach der "Startzeit"-Zeile** (oder vor "- Leistung:"), füge ein:

    ```python
    if decision.entladung_aktiv:
        # Phase 11: dynamischer Header für Slot B (Morgen-Entladung)
        if decision.discharge_active_slot == "B":
            lines.append("### Morgen-Entladung")
        else:
            lines.append("### Abend-Entladung")
        # Phase 11: Slot-Marker
        if decision.discharge_active_slot == "A":
            lines.append("- Aktiver Slot: A")
        elif decision.discharge_active_slot == "B":
            lines.append("- Aktiver Slot: B")
        # Bestehende Zeilen unverändert:
        lines.append(
            f"- Startzeit: {self._discharge_start_h:02d}:{self._discharge_start_m:02d}"
        )
        ...
    ```

    **B. optimizer.py — neue Markdown-Sektion "### Slot-Konfiguration" am Ende des _build_markdown (vor "### Diagnose (Gründe)" oder am Ende):**

    ```python
    # Phase 11: Slot-Konfigurations-Übersicht (nur wenn dual aktiviert)
    if self._enable_dual_discharge and not self._is_solaredge:
        lines.append("### Slot-Konfiguration")
        lines.append(
            f"- Slot A: {'aktiv' if self._enable_slot_a else 'deaktiviert'} "
            f"({self._discharge_a_start_h:02d}:{self._discharge_a_start_m:02d}, "
            f"Reserve {self._discharge_a_reserve_pct}%)"
        )
        lines.append(
            f"- Slot B: {'aktiv' if self._enable_slot_b else 'deaktiviert'} "
            f"({self._discharge_b_start_h:02d}:{self._discharge_b_start_m:02d}, "
            f"Cap {self._discharge_b_end_cap})"
        )
        lines.append("")
    ```

    Echte Umlaute überall (z.B. in dynamischer Header-Logik, Slot-Konfigurations-Block).

    **C. __init__.py — Activity-Log-Slot-Kontext (D-09 — VERPFLICHTEND, NICHT optional):**

    Lokalisiere die `_log_activity`-Funktion (`__init__.py:1255-1275`). Innerhalb des `entry_data`-Dict-Builders, füge das zusätzliche Feld `"discharge_active_slot"` direkt aus `decision.discharge_active_slot` ein. Das Feld wird über `hass.bus.async_fire("eeg_optimizer_activity", entry_data)` an Frontend-Listener und ggf. Telemetrie-Sinks weitergegeben.

    Konkrete Änderung (an die exakte Einrückung des bestehenden `_log_activity`-Bodies anpassen):
    ```python
    def _log_activity(decision, reason):
        """Append an activity entry and fire a HA event."""
        snap_dict = decision.snapshot if isinstance(decision.snapshot, dict) else {}
        soc_val = snap_dict.get("soc_pct")
        entry_data = {
            "timestamp": decision.timestamp,
            "zustand": decision.zustand,
            "reason": reason,
            "soc": soc_val,
            "min_soc": round(decision.min_soc_berechnet, 1),
            "pv_today": round(decision.morning_pv_today_kwh, 1),
            "pv_tomorrow": round(decision.discharge_pv_tomorrow_kwh, 1),
            "bedarf": round(decision.energiebedarf_kwh, 1),
            "discharge_bedarf": round(decision.discharge_demand_total_kwh, 1),
            "discharge_pv": round(decision.discharge_pv_tomorrow_kwh, 1),
            "ausführung": decision.ausführung,
            # Phase 11 (D-09): Slot-Kontext für Frontend-Anzeige + Telemetrie
            "discharge_active_slot": decision.discharge_active_slot,
        }
        activity_log.append(entry_data)
        hass.bus.async_fire("eeg_optimizer_activity", entry_data)
        hass.async_create_task(_save_activity_log())
    ```

    **Hinweis zur Rückwärtskompatibilität:** Bestehende persistierte Activity-Log-Einträge (aus dem `Store`) haben das Feld nicht; beim Re-Load werden sie unverändert geladen, das Feld fehlt einfach (Frontend muss `e.discharge_active_slot ?? null` defensiv handhaben — siehe D nachfolgend). Kein Migrations-Bedarf für den persistenten Log-Buffer.

    **D. frontend/eeg-optimizer-panel.js — Aktivitäts-Timeline-Slot-Suffix (D-09 — Frontend-Render):**

    Lokalisiere die Render-Funktion rund um `eeg-optimizer-panel.js:4005-4015`. Aktuell:
    ```javascript
    const reason = e.reason === "Heartbeat" ? `<span style="opacity:0.5">${e.zustand}</span>` : `<strong>${e.zustand}</strong>`;
    ```

    Erweitere zu:
    ```javascript
    // Phase 11 (D-09): Slot-Suffix bei Abend-Entladung
    const slotMarker = (e.zustand === "Abend-Entladung" && (e.discharge_active_slot === "A" || e.discharge_active_slot === "B"))
      ? ` (Slot ${e.discharge_active_slot})`
      : "";
    const zustandLabel = `${e.zustand}${slotMarker}`;
    const reason = e.reason === "Heartbeat"
      ? `<span style="opacity:0.5">${zustandLabel}</span>`
      : `<strong>${zustandLabel}</strong>`;
    ```

    Falls bei Slot B der `decision.zustand` weiterhin `"Abend-Entladung"` ist (Phase 11 hat den State-String NICHT umbenannt, nur den Markdown-Header dynamisch — siehe Plan 11-02), bleibt der Suffix-Trigger korrekt. Bei `e.discharge_active_slot=null/undefined` (alte Log-Einträge aus dem Store oder nicht-Dual-Mode) ist `slotMarker = ""`, Render-Verhalten ist Legacy-kompatibel.

    Echte Umlaute prüfen — `Slot A`/`Slot B` haben keine, aber falls der erweiterte Block weitere deutsche Texte berührt, KEINE ASCII-Substitution.

    **E. tests/test_dual_window_integration.py — NEUE Datei mit drei Test-Klassen:**

    Datei-Anfang (analog zu `tests/test_dual_window.py` aus Plan 11-01, mit eigenen Imports):
    ```python
    """Phase 11: Dual-Window-Integration — Markdown + Activity-Log-Slot-Kontext + 24h-_evaluate-Sequenz.

    Diese Datei läuft parallel zu tests/test_dual_window.py (Plan 11-03 schreibt
    dort TestSolarEdgeXOR + TestInverterRaceValidation). Trennung verhindert
    Wave-3-Merge-Konflikte.
    """
    from __future__ import annotations

    from datetime import datetime, timezone
    from unittest.mock import MagicMock

    import pytest

    from custom_components.eeg_energy_optimizer.optimizer import Decision
    from tests.conftest import _make_config, _make_optimizer, _make_snapshot
    ```

    **TestMarkdownRendering-Klasse:**
    ```python
    class TestMarkdownRendering:
        def test_markdown_shows_slot_a_marker(
            self, mock_hass, mock_inverter, mock_coordinator, mock_provider,
        ):
            cfg = _make_config(
                enable_dual_discharge=True,
                enable_slot_a=True, enable_slot_b=True,
            )
            opt = _make_optimizer(mock_hass, mock_inverter, mock_coordinator, mock_provider, config=cfg)
            snap = _make_snapshot(
                now=datetime(2026, 6, 15, 21, 0, tzinfo=timezone.utc),
                battery_soc=80.0,
            )
            decision = Decision()
            decision.zustand = "Abend-Entladung"
            decision.entladung_aktiv = True
            decision.discharge_active_slot = "A"
            decision.entladeleistung_kw = 5.0
            decision.min_soc_berechnet = 25.0
            md = opt._build_markdown(snap, decision)
            assert "Aktiver Slot: A" in md

        def test_markdown_shows_slot_b_marker(
            self, mock_hass, mock_inverter, mock_coordinator, mock_provider,
        ):
            cfg = _make_config(enable_dual_discharge=True, enable_slot_a=True, enable_slot_b=True)
            opt = _make_optimizer(mock_hass, mock_inverter, mock_coordinator, mock_provider, config=cfg)
            snap = _make_snapshot(now=datetime(2026, 6, 15, 4, 0, tzinfo=timezone.utc), battery_soc=70.0)
            decision = Decision()
            decision.zustand = "Abend-Entladung"
            decision.entladung_aktiv = True
            decision.discharge_active_slot = "B"
            decision.entladeleistung_kw = 5.0
            decision.min_soc_berechnet = 25.0
            md = opt._build_markdown(snap, decision)
            assert "Aktiver Slot: B" in md
            assert "Morgen-Entladung" in md  # Slot B Header

        def test_markdown_no_slot_marker_for_legacy(
            self, mock_hass, mock_inverter, mock_coordinator, mock_provider,
        ):
            cfg = _make_config(enable_dual_discharge=False)
            opt = _make_optimizer(mock_hass, mock_inverter, mock_coordinator, mock_provider, config=cfg)
            snap = _make_snapshot(now=datetime(2026, 6, 15, 1, 30, tzinfo=timezone.utc), battery_soc=80.0)
            decision = Decision()
            decision.zustand = "Abend-Entladung"
            decision.entladung_aktiv = True
            decision.discharge_active_slot = None  # Legacy
            decision.entladeleistung_kw = 5.0
            decision.min_soc_berechnet = 25.0
            md = opt._build_markdown(snap, decision)
            assert "Aktiver Slot:" not in md
            assert "### Abend-Entladung" in md

        def test_markdown_shows_slot_config_when_dual_enabled(
            self, mock_hass, mock_inverter, mock_coordinator, mock_provider,
        ):
            cfg = _make_config(
                enable_dual_discharge=True,
                enable_slot_a=True, enable_slot_b=True,
                discharge_a_start_time="20:00",
                discharge_b_start_time="03:00",
                discharge_b_end_cap="07:00",
                discharge_a_reserve_pct=15,
            )
            opt = _make_optimizer(mock_hass, mock_inverter, mock_coordinator, mock_provider, config=cfg)
            snap = _make_snapshot(now=datetime(2026, 6, 15, 12, 0, tzinfo=timezone.utc))
            decision = Decision()
            decision.zustand = "Normal"
            md = opt._build_markdown(snap, decision)
            assert "### Slot-Konfiguration" in md
            assert "Slot A: aktiv" in md
            assert "Slot B: aktiv" in md
            assert "20:00" in md
            assert "03:00" in md
            assert "Cap 07:00" in md

        def test_markdown_no_slot_config_when_legacy(
            self, mock_hass, mock_inverter, mock_coordinator, mock_provider,
        ):
            cfg = _make_config(enable_dual_discharge=False)
            opt = _make_optimizer(mock_hass, mock_inverter, mock_coordinator, mock_provider, config=cfg)
            snap = _make_snapshot()
            decision = Decision()
            decision.zustand = "Normal"
            md = opt._build_markdown(snap, decision)
            assert "### Slot-Konfiguration" not in md
    ```

    **TestEvaluate24hSlotMarkerPersistence-Klasse:**
    ```python
    class TestEvaluate24hSlotMarkerPersistence:
        """Voller _evaluate-Pfad: Slot-Aktivierungs-Datum wird gesetzt + Reset.

        Diese Tests laufen via _evaluate (async-Funktion → asyncio.run oder pytest.mark.asyncio).
        """
        @pytest.mark.asyncio
        async def test_slot_a_activated_date_set_on_first_activation(
            self, mock_hass, mock_inverter, mock_coordinator, mock_provider,
        ):
            cfg = _make_config(
                enable_dual_discharge=True, enable_slot_a=True, enable_slot_b=True,
                discharge_a_start_time="20:00", discharge_b_start_time="03:00",
                min_soc=20,
            )
            opt = _make_optimizer(mock_hass, mock_inverter, mock_coordinator, mock_provider, config=cfg)
            assert opt._slot_a_activated_date is None
            snap = _make_snapshot(
                now=datetime(2026, 6, 15, 21, 0, tzinfo=timezone.utc),
                battery_soc=80.0,
                sunrise=datetime(2026, 6, 16, 4, 52, tzinfo=timezone.utc),
                sunrise_today=datetime(2026, 6, 15, 4, 52, tzinfo=timezone.utc),
                pv_tomorrow_kwh=20.0,
                consumption_tomorrow_daylight_kwh=10.0,
            )
            decision = await opt._evaluate(snap, mode="Test")
            assert decision.discharge_active_slot == "A"
            assert opt._slot_a_activated_date == "2026-06-15"
            assert opt._last_active_slot == "A"

        @pytest.mark.asyncio
        async def test_slot_b_activated_date_set_independently_from_a(
            self, mock_hass, mock_inverter, mock_coordinator, mock_provider,
        ):
            cfg = _make_config(
                enable_dual_discharge=True, enable_slot_a=False, enable_slot_b=True,
                discharge_b_start_time="03:00", discharge_b_end_cap="07:00",
                min_soc=20,
            )
            opt = _make_optimizer(mock_hass, mock_inverter, mock_coordinator, mock_provider, config=cfg)
            snap = _make_snapshot(
                now=datetime(2026, 12, 21, 4, 0, tzinfo=timezone.utc),
                battery_soc=80.0,
                sunrise=datetime(2026, 12, 21, 7, 30, tzinfo=timezone.utc),
                sunrise_today=datetime(2026, 12, 20, 7, 30, tzinfo=timezone.utc),
                pv_tomorrow_kwh=20.0,
                consumption_tomorrow_daylight_kwh=10.0,
            )
            decision = await opt._evaluate(snap, mode="Test")
            assert decision.discharge_active_slot == "B"
            assert opt._slot_b_activated_date == "2026-12-21"
            assert opt._slot_a_activated_date is None
            assert opt._last_active_slot == "B"

        @pytest.mark.asyncio
        async def test_slot_a_date_reset_after_sunrise(
            self, mock_hass, mock_inverter, mock_coordinator, mock_provider,
        ):
            cfg = _make_config(enable_dual_discharge=True, enable_slot_a=True, enable_slot_b=True)
            opt = _make_optimizer(mock_hass, mock_inverter, mock_coordinator, mock_provider, config=cfg)
            opt._slot_a_activated_date = "2026-06-14"
            snap = _make_snapshot(
                now=datetime(2026, 6, 15, 12, 0, tzinfo=timezone.utc),
                battery_soc=80.0,
                sunrise=datetime(2026, 6, 16, 4, 52, tzinfo=timezone.utc),
                sunrise_today=datetime(2026, 6, 15, 4, 52, tzinfo=timezone.utc),
                pv_tomorrow_kwh=20.0,
                consumption_tomorrow_daylight_kwh=10.0,
            )
            await opt._evaluate(snap, mode="Test")
            assert opt._slot_a_activated_date is None
    ```

    **TestActivityLogSlotContext-Klasse (D-09 — KEIN Skip mehr):**

    Da `_log_activity` als geschlossene Funktion innerhalb von `async_setup_entry` definiert ist (Closure über `activity_log`/`hass`/`activity_dirty`), ist sie nicht direkt importierbar. Pragmatischer Ansatz: wir bauen den Dict-Builder-Teil als Test-Erwartung nach und prüfen, dass `decision.discharge_active_slot` direkt im erwarteten Feld liegt — ergänzt durch einen Source-Pattern-Check, der die Linie im `__init__.py` verifiziert. Falls der Closure-Test in der Test-Umgebung instabil ist, fallen wir auf den Source-Pattern-Test zurück (er ist deterministisch und ausreichend für D-09-Verifikation).

    ```python
    class TestActivityLogSlotContext:
        """D-09: discharge_active_slot ist Pflichtfeld im Activity-Log-Entry-Dict
        und im eeg_optimizer_activity-Bus-Event.
        """

        def test_log_entry_carries_slot_marker(self):
            """Verifiziere, dass _log_activity das Feld discharge_active_slot
            in entry_data setzt — über den Source-Pattern-Check, weil die Funktion
            ein Closure ist und nicht direkt importiert werden kann.
            """
            import pathlib
            init_path = pathlib.Path(
                "custom_components/eeg_energy_optimizer/__init__.py"
            )
            src = init_path.read_text(encoding="utf-8")
            # Pattern muss in _log_activity-Body stehen
            assert '"discharge_active_slot": decision.discharge_active_slot' in src, (
                "D-09: _log_activity MUSS discharge_active_slot ins entry_data-Dict "
                "serialisieren. Erwartetes Pattern fehlt in __init__.py."
            )

        def test_log_entry_dict_shape_includes_slot_field(self):
            """Reproduziere die Dict-Builder-Logik in _log_activity und prüfe,
            dass das Schlüsselfeld bei A/B/None korrekt durchgereicht wird.
            """
            for slot in ("A", "B", None):
                decision = Decision()
                decision.zustand = "Abend-Entladung"
                decision.discharge_active_slot = slot
                decision.timestamp = "2026-06-15T20:00:00+00:00"
                decision.min_soc_berechnet = 25.0
                decision.morning_pv_today_kwh = 10.0
                decision.discharge_pv_tomorrow_kwh = 15.0
                decision.energiebedarf_kwh = 12.0
                decision.discharge_demand_total_kwh = 14.0
                decision.ausführung = True
                decision.snapshot = {"soc_pct": 75.0}

                # Reproduktion der entry_data-Builder-Logik aus _log_activity:
                snap_dict = decision.snapshot if isinstance(decision.snapshot, dict) else {}
                soc_val = snap_dict.get("soc_pct")
                entry_data = {
                    "timestamp": decision.timestamp,
                    "zustand": decision.zustand,
                    "reason": "test",
                    "soc": soc_val,
                    "min_soc": round(decision.min_soc_berechnet, 1),
                    "pv_today": round(decision.morning_pv_today_kwh, 1),
                    "pv_tomorrow": round(decision.discharge_pv_tomorrow_kwh, 1),
                    "bedarf": round(decision.energiebedarf_kwh, 1),
                    "discharge_bedarf": round(decision.discharge_demand_total_kwh, 1),
                    "discharge_pv": round(decision.discharge_pv_tomorrow_kwh, 1),
                    "ausführung": decision.ausführung,
                    "discharge_active_slot": decision.discharge_active_slot,
                }
                assert "discharge_active_slot" in entry_data
                assert entry_data["discharge_active_slot"] == slot
    ```

    **HINWEIS zur _evaluate-Async-Signatur:** Falls die Methode synchron ist (kein `async def _evaluate`), entferne `@pytest.mark.asyncio` und `async/await` aus den Tests. Read der bestehenden Funktionssignatur in `optimizer.py:1057` klärt das beim Implementieren.

    **Falls der Activity-Log-Code-Pfad in `__init__.py` keine direkte Aufnahme von `discharge_active_slot` ohne breaking change erlaubt** (z.B. weil das Dict-Schema strikt validiert wird oder ein Storage-Migrations-Hop nötig wäre), dokumentiere das EXPLIZIT als Markdown-only-Begründung im SUMMARY. Aber: Der Read der Datei (siehe `<read_first>`) zeigt klar, dass `entry_data` ein freies Dict ist — additives Hinzufügen eines Felds ist ohne Migration möglich. D-09 muss daher umgesetzt werden, **nicht** als Skip-Test markiert.
  </action>
  <verify>
    <automated>pytest tests/test_dual_window_integration.py::TestMarkdownRendering -x -q</automated>
    <automated>pytest tests/test_dual_window_integration.py::TestEvaluate24hSlotMarkerPersistence -x -q</automated>
    <automated>pytest tests/test_dual_window_integration.py::TestActivityLogSlotContext -x -q</automated>
    <automated>pytest tests/test_dual_window_integration.py -x -q</automated>
    <automated>pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Aktiver Slot:" custom_components/eeg_energy_optimizer/optimizer.py` liefert mindestens einen Treffer (Markdown-Zeile)
    - `grep -n "Morgen-Entladung" custom_components/eeg_energy_optimizer/optimizer.py` liefert mindestens einen Treffer (dynamischer Header für Slot B)
    - `grep -n "Slot-Konfiguration" custom_components/eeg_energy_optimizer/optimizer.py` liefert mindestens einen Treffer
    - `grep -n "decision.discharge_active_slot" custom_components/eeg_energy_optimizer/optimizer.py` liefert mindestens 4 Treffer (in _build_markdown + in _evaluate aus Plan 11-02)
    - `grep -c '"discharge_active_slot": decision.discharge_active_slot' custom_components/eeg_energy_optimizer/__init__.py` liefert genau 1 (D-09 Activity-Log-Feld)
    - `grep -c "discharge_active_slot" custom_components/eeg_energy_optimizer/frontend/eeg-optimizer-panel.js` liefert mindestens 2 (Slot-Suffix-Logik in Aktivitäts-Timeline)
    - `grep -c "Slot \${e.discharge_active_slot}" custom_components/eeg_energy_optimizer/frontend/eeg-optimizer-panel.js` liefert mindestens 1 (Frontend-Render des Suffix)
    - `pytest tests/test_dual_window_integration.py::TestMarkdownRendering -x -q` exit-code 0 (5 Tests grün)
    - `pytest tests/test_dual_window_integration.py::TestEvaluate24hSlotMarkerPersistence -x -q` exit-code 0 (3 Tests grün)
    - `pytest tests/test_dual_window_integration.py::TestActivityLogSlotContext -x -q` exit-code 0 (2 Tests grün, KEIN Skip)
    - `pytest tests/test_dual_window_integration.py -x -q` exit-code 0 (gesamte neue Datei grün)
    - `pytest tests/test_dual_window.py -x -q` exit-code 0 (Plan-11-03-Tests bleiben unberührt — kein Wave-3-Konflikt)
    - `pytest tests/ -q` exit-code 0 (volle Suite grün, keine Regression)
  </acceptance_criteria>
  <done>
    Markdown-Renderer zeigt aktiven Slot + Slot-Konfigurations-Übersicht. _log_activity in __init__.py serialisiert discharge_active_slot ins entry_data-Dict (D-09). Frontend-Aktivitäts-Timeline zeigt "(Slot A)"/"(Slot B)"-Suffix bei Abend-Entladung-Einträgen. _evaluate-Integration-Tests bestätigen korrekte Slot-Marker-Persistenz und Reset-Trigger. TestActivityLogSlotContext ohne Skip — beide Tests grün. Alle Tests in der neuen separaten Datei tests/test_dual_window_integration.py — keine Wave-3-Kollision mit 11-03.
  </done>
</task>

<task type="auto">
  <name>Task 2: CHANGELOG.md — v1.2.0-dev Eintrag mit "Verhaltensänderung beim Update"-Sektion</name>
  <files>
    CHANGELOG.md
  </files>
  <read_first>
    CHANGELOG.md (komplett, um Version-Header-Pattern und Sektionen-Struktur zu sehen),
    .planning/milestones/v1.2-phases/11-dual-window-discharge/11-CONTEXT.md (D-04 — Verhaltensänderung-Kommunikation),
    .planning/milestones/v1.2-phases/11-dual-window-discharge/11-RESEARCH.md (Sektion "Migration & Versioning" für Defaults),
    .planning/milestones/v1.2-phases/11-dual-window-discharge/11-PATTERNS.md (Sektion "CHANGELOG.md")
  </read_first>
  <action>
    **A. CHANGELOG.md — neuen v1.2.0-dev-Eintrag oben einfügen (oberhalb des aktuellsten v1.1.x-Eintrags):**

    Wenn das Header-Pattern `## [1.1.3] - 2026-MM-DD` ist (oder ähnlich), füge ein:

    ```markdown
    ## [1.2.0-dev] - YYYY-MM-DD

    > User selbst macht Releases — dieser Eintrag ist DEV-Repo-only und wird beim Release-Sync übernommen.

    ### Verhaltensänderung beim Update

    Mit Phase 11 wird die **Dual-Window-Entladung** zum Standard für alle Wechselrichter außer SolarEdge. Bestands-Anlagen werden beim Update automatisch auf das neue Modell migriert (Config-Entry-Version-Bump v14 → v15).

    **Was sich ändert:**
    - Die Abend-Entladung läuft nun in **zwei unabhängigen Slots** statt einem einzigen Fenster:
      - **Slot A — Abend** (Default 20:00 bis 5min vor Slot-B-Start): Adressiert den EEG-Abendpeak (18:00–23:00)
      - **Slot B — Morgen** (Default 03:00 bis spätestens 07:00 oder Sonnenaufgang−5min): Adressiert den EEG-Morgenpeak und Wintermorgen
    - **Pro-Slot-Hysterese:** Slot A und Slot B haben jeweils unabhängige Reaktivierungs-Schwellen (+5% SOC bei Reaktivierung).
    - **Energie-Reserve:** Slot A endet bei `min_soc + 15%` (Default), damit Slot B genug Energie übrig hat.

    **Mitigation gegen unerwünschtes Entladen:**
    - Pro-Slot-Hysterese verhindert oszillierende Aktivierung.
    - PV-Tomorrow-Garantie: Beide Slots prüfen weiterhin, dass die PV-Prognose für morgen den Bedarf inkl. Sicherheitspuffer deckt.
    - Konfiguration jederzeit umstellbar: Im Onboarding-Panel → Einstellungen → Abend-Entladung kann jeder User `Dual-Window-Entladung` deaktivieren und auf das alte Single-Window-Verhalten zurückkehren.

    **SolarEdge-Sonderfall:**
    SolarEdge-Wechselrichter schreiben Entlade-Kommandos in NVRAM-Speicher mit begrenzten Schreibzyklen. Daher ist Dual-Window auf SolarEdge **nicht verfügbar**. Stattdessen gibt es ein Radio-Auswahl-Feld "Slot A — Abend (Default) | Slot B — Morgen" im Panel; pro Tag läuft genau einer der beiden Slots.

    ### Added
    - **Dual-Window-Entladung** mit Slot A (Abend) und Slot B (Morgen) — neue Konfigurationskeys `enable_dual_discharge`, `enable_slot_a`, `enable_slot_b`, `discharge_a_start_time`, `discharge_b_start_time`, `discharge_b_end_cap`, `discharge_a_reserve_pct`.
    - Funktion `compute_b_window_end()` für adaptives Slot-B-Ende vor Sonnenaufgang.
    - Acht neue Telemetrie-Reasons für Slot-A-/Slot-B-Phasen: `before_slot_a`, `slot_a_active`, `slot_a_reserve_reached`, `between_slots`, `before_slot_b`, `slot_b_active`, `slot_b_window_expired`, `slot_b_pre_sunrise_cutoff`.
    - `Decision.discharge_active_slot`-Feld (Werte "A", "B" oder None).
    - Activity-Log-Feld `discharge_active_slot` für Slot-Kontext im Aktivitätsverlauf (D-09).
    - SolarEdge-XOR-Radio im Onboarding-Panel mit Tooltip-Erklärung "NVRAM-Verschleiß: nur ein Slot pro Tag möglich".

    ### Changed
    - **Default-Verhalten:** Bestands-Anlagen erhalten beim Update Dual-Window automatisch (Config v14 → v15). SolarEdge-Bestände bekommen Slot-A-only (XOR-Konfiguration). Siehe "Verhaltensänderung beim Update" oben.
    - `_should_discharge` ist nun ein Dispatcher mit drei Pfaden (Legacy / Slot-A / Slot-B), gemeinsame Guards in `_check_common_guards`. Legacy-Pfad bleibt 1:1 erhalten für Setups mit `enable_dual_discharge=False`.
    - PeakShare-Cache `_discharge_plan` ist nun ein slot-indiziertes Dict (`{"a": ..., "b": ...}`) statt single tuple. Alte Cache-Form wird beim Update verworfen und neu berechnet.
    - WebSocket-Save-Path validiert SolarEdge-XOR und Inverter-Race (b_start ≥ a_start+30min+5min) mit Auto-Korrektur.

    ### Migration
    - Config-Entry-Version: 14 → 15.
    - Migration ist additiv: bestehende User-Werte bleiben erhalten (`setdefault`).
    ```

    **B. Echte Umlaute überall** — `Verhaltensänderung`, `Sonnenaufgang`, `unabhängig`, `Schreibzyklen`, `verfügbar`, `Auswahl-Feld`, etc. KEINE ASCII-Substitution.

    **C. Datum YYYY-MM-DD durch heutiges Datum 2026-05-04 ersetzen** (oder leer lassen für späteren Release-Schritt — User macht Releases laut Memory `feedback_release_process.md`).
  </action>
  <verify>
    <automated>echo "CHANGELOG-Edit: pure docs, kein automatisierter Test"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "1.2.0-dev" CHANGELOG.md` liefert mindestens einen Treffer (neue Version-Section)
    - `grep -c "Verhaltensänderung" CHANGELOG.md` liefert mindestens 1 (echte Umlaute)
    - `grep -c "Schreibzyklen\|Sonnenaufgang\|verfügbar\|unabhängig" CHANGELOG.md` liefert mindestens 3 (positive Match-Set für echte Umlaute in Schlüsselwörtern)
    - `grep -c "Verhaltensaenderung\|Schreibzyklenverschleiss\|verfuegbar\|unabhaengig" CHANGELOG.md` liefert genau 0 (Negativliste — KEINE ASCII-Substitution erlaubt)
    - `grep -c "## .*Verhaltensänderung beim Update" CHANGELOG.md` liefert mindestens 1 (Sektions-Header existiert)
    - `grep -n "Dual-Window" CHANGELOG.md` liefert mindestens 3 Treffer (Header, Beschreibung, Added)
    - `grep -n "Slot A" CHANGELOG.md` liefert mindestens 3 Treffer (Bezeichnung in Behaviors-Sektion)
    - `grep -n "Slot B" CHANGELOG.md` liefert mindestens 3 Treffer
    - `grep -n "NVRAM-Verschleiß" CHANGELOG.md` liefert mindestens einen Treffer (echte Umlaute, SolarEdge-Sonderfall)
    - `grep -n "v14 → v15" CHANGELOG.md` liefert mindestens einen Treffer (Migrations-Hinweis)
    - `grep -n "Pro-Slot-Hysterese" CHANGELOG.md` liefert mindestens einen Treffer
    - `grep -n "discharge_active_slot" CHANGELOG.md` liefert mindestens einen Treffer (D-09 Added-Eintrag)
  </acceptance_criteria>
  <done>
    CHANGELOG.md enthält v1.2.0-dev-Eintrag mit prominenter "Verhaltensänderung beim Update"-Sektion, Added-/Changed-/Migration-Sektionen, allen relevanten Konfigurations-Keys, SolarEdge-Sonderfall-Erklärung, alle Texte mit echten Umlauten, KEINE ASCII-Substitution. Eintrag ist Release-ready.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Decision.discharge_active_slot → Markdown | Renderer-Schritt; kein User-Input, nur State-Reflektion |
| Decision.discharge_active_slot → Activity-Log entry_data | Additives Feld im Dict; persistiert via Storage; Frontend liest via Bus-Event und Storage-Replay |
| _evaluate → Hysterese-Felder | _evaluate setzt _slot_*_activated_date — Test-Verifikation in Task 1 |
| Activity-Log-entry_data → Frontend-Render | Defensives Render bei `e.discharge_active_slot=null/undefined` für Legacy-Storage-Einträge |
| CHANGELOG → Release-Notes | User liest CHANGELOG vor Update — UI/UX-kritisch für Verhaltensänderungs-Kommunikation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04-01 | Information Disclosure | Markdown leakt Slot-Info an unauthenticated Dashboard-Besucher | accept | Sensor "Entscheidung" hat dieselbe Sichtbarkeit wie alle anderen Optimizer-Sensoren — keine neue Disclosure |
| T-11-04-02 | Tampering | _evaluate setzt _slot_*_activated_date inkorrekt → Hysterese hebt sich auf | mitigate | Integration-Test `test_slot_a_activated_date_set_on_first_activation` validiert Set-Logik. Reset-Test `test_slot_a_date_reset_after_sunrise` validiert Reset. |
| T-11-04-03 | Repudiation | User behauptet "Dual-Window war nicht angekündigt" | mitigate | CHANGELOG-Eintrag mit "Verhaltensänderung beim Update"-Sektion ist explizit und prominent. Sektion erklärt Mitigation und Opt-Out-Pfad. |
| T-11-04-04 | DoS | _build_markdown crashed bei discharge_active_slot=ungültiger Wert | accept | Markdown-Renderer prüft `== "A"` / `== "B"` strikt; alle anderen Werte (None, "", andere Strings) liefern keine Slot-Zeile, Markdown bleibt sicher |
| T-11-04-05 | Tampering | Frontend rendert "(Slot null)" bei Legacy-Storage-Einträgen | mitigate | Frontend-Check prüft strikt `e.discharge_active_slot === "A" \|\| e.discharge_active_slot === "B"`; bei `null/undefined` ist `slotMarker = ""`, Render-Verhalten ist Legacy-kompatibel |

Severity: alle LOW. Markdown-, Activity-Log-, Frontend- und CHANGELOG-Änderungen sind Read-only-Reflektionen des bestehenden State.
</threat_model>

<verification>
**Automated:**
```bash
pytest tests/test_dual_window_integration.py::TestMarkdownRendering -x -q
pytest tests/test_dual_window_integration.py::TestEvaluate24hSlotMarkerPersistence -x -q
pytest tests/test_dual_window_integration.py::TestActivityLogSlotContext -x -q
pytest tests/test_dual_window_integration.py -x -q
pytest tests/test_dual_window.py -x -q  # Plan-11-03-Tests bleiben grün
pytest tests/ -q
```

**Manual (vor Release):**
1. CHANGELOG-Section optisch prüfen (Markdown-Render in GitHub/HACS)
2. HA-Update auf v1.2.0-dev → Decision-Card im Dashboard zeigt Slot-Info bei aktiver Entladung
3. Activity-Log-Eintrag bei State-Change zu Abend-Entladung — Slot-Suffix "(Slot A)"/"(Slot B)" sichtbar im Panel-Aktivitätsverlauf

**Pattern checks:**
```bash
grep -c "Aktiver Slot:" custom_components/eeg_energy_optimizer/optimizer.py  # mind. 1
grep -c "Slot-Konfiguration" custom_components/eeg_energy_optimizer/optimizer.py  # mind. 1
grep -c '"discharge_active_slot": decision.discharge_active_slot' custom_components/eeg_energy_optimizer/__init__.py  # 1
grep -c "discharge_active_slot" custom_components/eeg_energy_optimizer/frontend/eeg-optimizer-panel.js  # mind. 2
grep -c "Verhaltensänderung beim Update" CHANGELOG.md  # 1
```
</verification>

<success_criteria>
- _build_markdown rendert "Aktiver Slot: A/B" und dynamischen Header (Abend-Entladung / Morgen-Entladung)
- _build_markdown zeigt "### Slot-Konfiguration" wenn dual aktiviert
- __init__.py:_log_activity serialisiert discharge_active_slot ins entry_data-Dict (D-09)
- frontend/eeg-optimizer-panel.js Aktivitäts-Timeline zeigt "(Slot A)"/"(Slot B)"-Suffix bei Abend-Entladung-Einträgen
- 24h-_evaluate-Integration-Tests bestätigen _slot_*_activated_date Set + Reset
- TestActivityLogSlotContext: 2 Tests grün, KEIN Skip
- Tests in NEUER Datei tests/test_dual_window_integration.py (kein Wave-3-Konflikt mit 11-03)
- CHANGELOG.md hat v1.2.0-dev-Eintrag mit prominenter "Verhaltensänderung beim Update"-Sektion
- Echte Umlaute durchgängig, KEINE ASCII-Substitution (verifiziert via positive + Negativ-grep)
- Volle Test-Suite grün
</success_criteria>

<output>
Nach Abschluss: erstelle `.planning/phases/11-dual-window-discharge/11-04-SUMMARY.md` mit:
- Markdown-Renderer-Erweiterungen (Slot-Marker + Slot-Konfigurations-Sektion)
- Activity-Log-discharge_active_slot-Feld in __init__.py:_log_activity (D-09 erfüllt)
- Frontend-Aktivitäts-Timeline-Slot-Suffix in eeg-optimizer-panel.js (~Zeile 4013)
- _evaluate-Integration-Test-Status (Slot-Marker-Persistenz + Reset-Trigger)
- TestActivityLogSlotContext-Status (2 Tests, KEIN Skip)
- Test-Datei-Trennung: tests/test_dual_window_integration.py (separat von tests/test_dual_window.py — Wave-3-Kollision mit 11-03 vermieden)
- CHANGELOG-Eintrag-Übersicht (Sektionen + Schlüssel-Statements + ASCII-Substitution-Negativcheck)
- Phase-11-Gesamtstatus (alle 4 Plans abgeschlossen → 7-Tage-UAT-Trigger)
- UAT-Hinweis: Manuelle 7-Tage-Beobachtung an Huawei (192.168.1.211) und/oder Fronius (192.168.100.211) mit User-Bewertung "akzeptiert/verworfen"
</output>
</content>
</invoke>