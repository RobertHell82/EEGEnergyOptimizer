---
phase: 11
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - custom_components/eeg_energy_optimizer/frontend/eeg-optimizer-panel.js
  - custom_components/eeg_energy_optimizer/websocket_api.py
  - tests/test_dual_window.py
  - tests/test_websocket_api.py
autonomous: true
requirements: [SPEC-1, SPEC-6, SPEC-9]
tags: [dual-window, panel-ui, save-path, solaredge-xor, inverter-race]

must_haves:
  truths:
    - "Onboarding-Panel zeigt Master-Toggle enable_dual_discharge in der Discharge-Sektion (sowohl Wizard als auch Settings)"
    - "Bei aktivem Master-Toggle werden Slot-A- und Slot-B-Sub-Bereiche eingeblendet (jeweils mit eigenem Toggle + Zeit-/Reserve-Feldern)"
    - "Bei deaktivem Master-Toggle bleiben die Legacy-Felder (discharge_start_time) sichtbar"
    - "Bei SolarEdge-Wechselrichter wird der Master-Toggle versteckt; stattdessen erscheint Radio Slot-A | Slot-B (Default Slot A) mit Tooltip 'NVRAM-Verschleiß'"
    - "websocket_api.ws_save_config setzt enable_dual_discharge=False bei SolarEdge (Auto-Korrektur mit _LOGGER.warning)"
    - "websocket_api.ws_save_config korrigiert XOR-Konflikt bei SolarEdge (slot_a + slot_b beide True → slot_b auf False)"
    - "websocket_api.ws_save_config setzt slot_a auf True wenn beide Slots False bei SolarEdge (Fallback)"
    - "websocket_api.ws_save_config validiert Inverter-Race: b_start ≥ a_start + 30min + 5min, sonst Auto-Korrektur b_start"
    - "_parse_hhmm-Helper im websocket_api.py akzeptiert 'HH:MM' und liefert int (Minuten seit Mitternacht)"
  artifacts:
    - path: custom_components/eeg_energy_optimizer/frontend/eeg-optimizer-panel.js
      provides: "Wizard Step 4 + Settings-Tab evening Erweiterung mit Master-Toggle, Slot-A/B-Sub-Bereichen, SolarEdge-XOR-Radio"
      contains: "data-field=\"enable_dual_discharge\""
    - path: custom_components/eeg_energy_optimizer/websocket_api.py
      provides: "ws_save_config-Erweiterung: SolarEdge-XOR-Block + Inverter-Race-Validation + _parse_hhmm-Helper"
      contains: "def _parse_hhmm("
    - path: tests/test_dual_window.py
      provides: "TestSolarEdgeXOR-Klasse + TestInverterRaceValidation-Klasse"
      contains: "class TestSolarEdgeXOR"
    - path: tests/test_websocket_api.py
      provides: "WebSocket-Save-Path-Tests für SolarEdge-XOR + Inverter-Race"
      contains: "test_save_config_solaredge"
  key_links:
    - from: "frontend/eeg-optimizer-panel.js"
      to: "websocket_api.py:ws_save_config"
      via: "WS-Command eeg_optimizer/save_config mit data-Payload"
      pattern: "eeg_optimizer/save_config"
    - from: "websocket_api.py:ws_save_config"
      to: "Migration v14→v15 (Plan 11-01) + Optimizer-Runtime-Force (Plan 11-02)"
      via: "Defense-in-depth: 3 Layer für SolarEdge-XOR"
      pattern: "enable_dual_discharge.*False"
    - from: "frontend/eeg-optimizer-panel.js (SolarEdge-Branch)"
      to: "frontend/eeg-optimizer-panel.js (Standard-Branch)"
      via: "Conditional Render basierend auf inverter_type"
      pattern: "inverter_type === \"solaredge_storedge\""
---

<objective>
Phase 11 Wave 3 (parallel zu 11-04): Panel-UI für Dual-Window-Konfiguration + Server-Side-Validation im Save-Path.

Dieser Plan implementiert die User-facing Schicht von Phase 11. Onboarding-Panel zeigt den Master-Toggle, die Slot-A/Slot-B-Sub-Bereiche und das SolarEdge-XOR-Radio. Im Save-Path werden alle drei SolarEdge-XOR-Konfliktfälle abgefangen (dual=True bei SolarEdge / beide Slots aktiv / kein Slot aktiv), und die Inverter-Race-Validation korrigiert b_start automatisch (Auto-Korrektur statt Hard-Reject — konsistent mit dem SolarEdge-5kW-Clamp-Pattern).

Adressiert SPEC-Requirements:
- §1 Konfigurations-Schema (UI-Schicht für die in 11-01 eingeführten Keys)
- §6 SolarEdge-Sperre (Panel-Hinweis + Save-Path-Erzwingung — Defense-in-depth Layer 2)
- §9 Inverter-Race-Schutz (Save-Path-Validation mit Auto-Korrektur)

Adressiert User-Decisions:
- D-06 Inline-Erweiterung der Discharge-Sektion mit Master-Toggle + Sub-Bereichen
- D-07 SolarEdge-Sonderfall: Master-Toggle deaktiviert, stattdessen XOR-Radio mit Default Slot A + Tooltip
- Claude's Discretion: Inverter-Race als Auto-Korrektur (NICHT Hard-Reject) — siehe RESEARCH-Empfehlung

Purpose: User können Dual-Window-Konfiguration im Wizard und Settings vornehmen; SolarEdge-User sehen ein Radio statt fehlender Optionen mit Erklärung. Save-Path stellt sicher, dass auch bei UI-Bugs niemals SolarEdge mit dual=True persistiert wird.
Output: Erweiterte Panel-JS, erweiterte ws_save_config, +5 Tests in test_dual_window.py + test_websocket_api.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/milestones/v1.2-phases/11-dual-window-discharge/11-SPEC.md
@.planning/milestones/v1.2-phases/11-dual-window-discharge/11-CONTEXT.md
@.planning/milestones/v1.2-phases/11-dual-window-discharge/11-RESEARCH.md
@.planning/milestones/v1.2-phases/11-dual-window-discharge/11-PATTERNS.md
@.planning/phases/11-dual-window-discharge/11-01-SUMMARY.md
@.planning/phases/11-dual-window-discharge/11-02-SUMMARY.md

<interfaces>
<!-- Bestehende Patterns aus dem Code, die exakt übernommen werden -->

Aus frontend/eeg-optimizer-panel.js:2995-3027 (Wizard Discharge-Section, "feature-params"-Pattern mit `data-field`):
```javascript
const dischargeFields = nDischarge ? `
  <div class="feature-params">
    <label style="display:flex;align-items:center;gap:8px;cursor:pointer;margin-bottom:12px">
      <input type="checkbox" data-field="enable_peakshare" ${peakshare ? "checked" : ""}>
      <div>
        <div style="font-weight:500">PeakShare-Bedarfssteuerung</div>
        <div class="help-text">Entladezeitpunkt wird automatisch nach dem Bedarf der Energiegemeinschaft optimiert.</div>
      </div>
    </label>
    ...
    <div class="field-group">
      <label>Frühester Entladestart</label>
      <input type="text" data-field="discharge_start_time" placeholder="HH:MM" pattern="[0-2][0-9]:[0-5][0-9]" maxlength="5"
             value="${this._wizardData.discharge_start_time}" style="width:80px">
      <div class="help-text">...</div>
    </div>
    ...
  </div>` : "";
```

Settings-Tab nutzt `settings_*`-Präfix für `data-field` (z.B. `data-field="settings_discharge_start_time"`).

SolarEdge-Detection im Panel (eeg-optimizer-panel.js:3017):
```javascript
${this._wizardData.inverter_type === "solaredge_storedge" ? "..." : "..."}
```

Aus websocket_api.py:410-414 (SolarEdge-5kW-Clamp-Pattern):
```python
if new_data.get("inverter_type") == INVERTER_TYPE_SOLAREDGE:
    discharge_kw = new_data.get("discharge_power_kw")
    if discharge_kw is not None and float(discharge_kw) < 5.0:
        new_data["discharge_power_kw"] = 5.0
```

Aus websocket_api.py:420-441 (Fronius Hard-Reject — NICHT übernehmen, dient als Anti-Pattern):
```python
if new_data.get("inverter_type") == INVERTER_TYPE_FRONIUS:
    host = new_data.get("fronius_modbus_host", "")
    if not isinstance(host, str) or not host.strip():
        connection.send_error(msg["id"], "invalid_config", "...")
        return
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: websocket_api.py — SolarEdge-XOR + Inverter-Race-Validation + _parse_hhmm-Helper</name>
  <files>
    custom_components/eeg_energy_optimizer/websocket_api.py,
    tests/test_websocket_api.py,
    tests/test_dual_window.py
  </files>
  <read_first>
    custom_components/eeg_energy_optimizer/websocket_api.py (Zeilen 1-50 für Imports + Zeilen 380-460 für ws_save_config-Body inkl. SolarEdge-Block 410-414 und Fronius-Block 420-441),
    custom_components/eeg_energy_optimizer/const.py (Zeilen 1-80 für INVERTER_TYPE_*-Konstanten),
    .planning/milestones/v1.2-phases/11-dual-window-discharge/11-PATTERNS.md (Sektion "websocket_api.py Save-Path"),
    .planning/milestones/v1.2-phases/11-dual-window-discharge/11-RESEARCH.md (Sektion "Inverter-Race-Validation" mit konkretem Code),
    tests/test_websocket_api.py (für bestehende Test-Pattern, falls Datei existiert)
  </read_first>
  <behavior>
    - _parse_hhmm("20:00") liefert 1200 (Minuten seit Mitternacht)
    - _parse_hhmm("invalid") wirft ValueError oder liefert konsistenten Default; Save-Path nutzt _parse_hhmm sicher mit try/except
    - SolarEdge + enable_dual_discharge=True → Auto-Korrektur auf False, _LOGGER.warning, KEIN connection.send_error
    - SolarEdge + slot_a=True + slot_b=True → slot_b auf False, _LOGGER.warning
    - SolarEdge + slot_a=False + slot_b=False → slot_a auf True (Fallback)
    - Dual-Mode mit a_start=20:00 + b_start=20:25 (Konflikt) → b_start auf 20:35 angehoben (a_min_end=20:30 + 5min Pause)
    - Dual-Mode mit a_start=20:00 + b_start=03:00 (kein Konflikt) → keine Änderung
    - WS-Save-Pfad sendet KEIN send_error für Dual-Window-Konfigurationsfehler (Auto-Korrektur)
  </behavior>
  <action>
    **A. websocket_api.py — _parse_hhmm-Helper am Modulanfang (oder direkt vor ws_save_config):**

    ```python
    def _parse_hhmm(s: str) -> int:
        """Parse 'HH:MM' to minutes since midnight.

        Strict format: 'HH:MM' (zero-padded). Raises ValueError on malformed input.
        """
        h_str, m_str = s.split(":")
        return int(h_str) * 60 + int(m_str)
    ```

    **B. websocket_api.py — ws_save_config-Erweiterung im SolarEdge-Block (Z. 410-414 erweitern):**

    Lokalisiere den bestehenden SolarEdge-Block:
    ```python
    if new_data.get("inverter_type") == INVERTER_TYPE_SOLAREDGE:
        discharge_kw = new_data.get("discharge_power_kw")
        if discharge_kw is not None and float(discharge_kw) < 5.0:
            new_data["discharge_power_kw"] = 5.0
    ```

    Erweitere innerhalb desselben if-Blocks (nach dem 5kW-Clamp):
    ```python
    if new_data.get("inverter_type") == INVERTER_TYPE_SOLAREDGE:
        discharge_kw = new_data.get("discharge_power_kw")
        if discharge_kw is not None and float(discharge_kw) < 5.0:
            new_data["discharge_power_kw"] = 5.0
        # Phase 11: SolarEdge bleibt Single-Slot (NVRAM-Verschleiß)
        if new_data.get("enable_dual_discharge"):
            _LOGGER.warning(
                "SolarEdge: enable_dual_discharge=True nicht erlaubt — auf False gesetzt"
            )
            new_data["enable_dual_discharge"] = False
        if new_data.get("enable_slot_a") and new_data.get("enable_slot_b"):
            _LOGGER.warning(
                "SolarEdge: nur ein Slot erlaubt — Slot A bevorzugt"
            )
            new_data["enable_slot_b"] = False
        if not new_data.get("enable_slot_a") and not new_data.get("enable_slot_b"):
            # Fallback: A aktivieren wenn beide aus
            new_data["enable_slot_a"] = True
    ```

    **C. websocket_api.py — Inverter-Race-Validation als separater Block NACH den Inverter-Type-Blöcken, VOR async_update_entry:**

    ```python
    # Phase 11: Inverter-Race-Schutz (≥5min zwischen Slot-A-Min-Ende und Slot-B-Start)
    # Auto-Korrektur (NICHT Hard-Reject) — konsistent mit SolarEdge-5kW-Clamp-Pattern.
    if (
        new_data.get("enable_dual_discharge")
        and new_data.get("enable_slot_a")
        and new_data.get("enable_slot_b")
    ):
        try:
            a_start_min = _parse_hhmm(
                new_data.get("discharge_a_start_time", "20:00")
            )
            b_start_min = _parse_hhmm(
                new_data.get("discharge_b_start_time", "03:00")
            )
        except (ValueError, AttributeError):
            # Malformed Time-String — vertraue auf Voluptuous-Schema-Validation
            # weiter oben (oder Default-Fallback durch Migration). Skip.
            a_start_min = b_start_min = None

        if a_start_min is not None and b_start_min is not None:
            # 30min Mindest-Slot-A-Dauer + 5min Pause = a_start + 35
            a_min_end_total = (a_start_min + 30) % (24 * 60)
            # Auf "Tagesachse mit a abends, b nach Mitternacht" abbilden
            if b_start_min < 12 * 60:
                b_on_tomorrow = b_start_min + 24 * 60
            else:
                b_on_tomorrow = b_start_min
            if a_min_end_total < 12 * 60:
                a_on_tomorrow = a_min_end_total + 24 * 60
            else:
                a_on_tomorrow = a_min_end_total
            if b_on_tomorrow < a_on_tomorrow + 5:
                new_b_min = (a_on_tomorrow + 5) % (24 * 60)
                new_b_start_str = f"{new_b_min // 60:02d}:{new_b_min % 60:02d}"
                _LOGGER.warning(
                    "Dual-Window: b_start %s zu nah an a_start+30min — auf %s angehoben",
                    new_data.get("discharge_b_start_time"),
                    new_b_start_str,
                )
                new_data["discharge_b_start_time"] = new_b_start_str
    ```

    Stelle sicher, dass dieser Block VOR `await hass.config_entries.async_update_entry(...)` steht — die Auto-Korrektur muss persistiert werden.

    **D. tests/test_dual_window.py — TestSolarEdgeXOR + TestInverterRaceValidation:**

    ```python
    class TestSolarEdgeXOR:
        @pytest.mark.asyncio
        async def test_save_config_solaredge_disables_dual(self):
            """SolarEdge + enable_dual_discharge=True → Auto-Korrektur auf False."""
            from custom_components.eeg_energy_optimizer.websocket_api import (
                ws_save_config,
            )
            hass = MagicMock()
            entry = MagicMock()
            entry.entry_id = "test_entry"
            entry.data = {
                "inverter_type": "solaredge_storedge",
                "enable_dual_discharge": True,
            }
            hass.config_entries.async_get_entry = MagicMock(return_value=entry)
            hass.config_entries.async_update_entry = MagicMock()
            connection = MagicMock()
            connection.send_result = MagicMock()
            connection.send_error = MagicMock()
            msg = {
                "id": 1,
                "entry_id": "test_entry",
                "data": {
                    "inverter_type": "solaredge_storedge",
                    "enable_dual_discharge": True,
                    "enable_slot_a": True,
                    "enable_slot_b": False,
                },
            }
            await ws_save_config(hass, connection, msg)
            # Save-Path-Auto-Korrektur muss greifen
            assert hass.config_entries.async_update_entry.called
            args, kwargs = hass.config_entries.async_update_entry.call_args
            new_data = kwargs.get("data") or args[1]
            assert new_data["enable_dual_discharge"] is False
            # KEIN send_error
            assert not connection.send_error.called

        @pytest.mark.asyncio
        async def test_save_config_solaredge_two_slots_falls_back_a(self):
            """SolarEdge + slot_a=True + slot_b=True → slot_b auf False."""
            from custom_components.eeg_energy_optimizer.websocket_api import (
                ws_save_config,
            )
            hass = MagicMock()
            entry = MagicMock()
            entry.entry_id = "test_entry"
            entry.data = {"inverter_type": "solaredge_storedge"}
            hass.config_entries.async_get_entry = MagicMock(return_value=entry)
            hass.config_entries.async_update_entry = MagicMock()
            connection = MagicMock()
            msg = {
                "id": 1,
                "entry_id": "test_entry",
                "data": {
                    "inverter_type": "solaredge_storedge",
                    "enable_dual_discharge": False,
                    "enable_slot_a": True,
                    "enable_slot_b": True,
                },
            }
            await ws_save_config(hass, connection, msg)
            args, kwargs = hass.config_entries.async_update_entry.call_args
            new_data = kwargs.get("data") or args[1]
            assert new_data["enable_slot_a"] is True
            assert new_data["enable_slot_b"] is False

        @pytest.mark.asyncio
        async def test_save_config_solaredge_no_slot_falls_back_to_a(self):
            """SolarEdge + beide Slots False → slot_a True (Fallback)."""
            from custom_components.eeg_energy_optimizer.websocket_api import (
                ws_save_config,
            )
            hass = MagicMock()
            entry = MagicMock()
            entry.entry_id = "test_entry"
            entry.data = {"inverter_type": "solaredge_storedge"}
            hass.config_entries.async_get_entry = MagicMock(return_value=entry)
            hass.config_entries.async_update_entry = MagicMock()
            connection = MagicMock()
            msg = {
                "id": 1,
                "entry_id": "test_entry",
                "data": {
                    "inverter_type": "solaredge_storedge",
                    "enable_dual_discharge": False,
                    "enable_slot_a": False,
                    "enable_slot_b": False,
                },
            }
            await ws_save_config(hass, connection, msg)
            args, kwargs = hass.config_entries.async_update_entry.call_args
            new_data = kwargs.get("data") or args[1]
            assert new_data["enable_slot_a"] is True


    class TestInverterRaceValidation:
        @pytest.mark.asyncio
        async def test_b_start_too_close_auto_bumped(self):
            """Dual + a_start=20:00 + b_start=20:25 → b_start auf 20:35."""
            from custom_components.eeg_energy_optimizer.websocket_api import (
                ws_save_config,
            )
            hass = MagicMock()
            entry = MagicMock()
            entry.entry_id = "test_entry"
            entry.data = {"inverter_type": "huawei_sun2000"}
            hass.config_entries.async_get_entry = MagicMock(return_value=entry)
            hass.config_entries.async_update_entry = MagicMock()
            connection = MagicMock()
            msg = {
                "id": 1,
                "entry_id": "test_entry",
                "data": {
                    "inverter_type": "huawei_sun2000",
                    "enable_dual_discharge": True,
                    "enable_slot_a": True,
                    "enable_slot_b": True,
                    "discharge_a_start_time": "20:00",
                    "discharge_b_start_time": "20:25",  # zu nah an a_start+30min=20:30
                },
            }
            await ws_save_config(hass, connection, msg)
            args, kwargs = hass.config_entries.async_update_entry.call_args
            new_data = kwargs.get("data") or args[1]
            # b_start auf a_start + 30min + 5min = 20:35 angehoben
            assert new_data["discharge_b_start_time"] == "20:35"

        @pytest.mark.asyncio
        async def test_default_dual_config_no_correction(self):
            """Default a=20:00 + b=03:00 → keine Änderung."""
            from custom_components.eeg_energy_optimizer.websocket_api import (
                ws_save_config,
            )
            hass = MagicMock()
            entry = MagicMock()
            entry.entry_id = "test_entry"
            entry.data = {"inverter_type": "huawei_sun2000"}
            hass.config_entries.async_get_entry = MagicMock(return_value=entry)
            hass.config_entries.async_update_entry = MagicMock()
            connection = MagicMock()
            msg = {
                "id": 1,
                "entry_id": "test_entry",
                "data": {
                    "inverter_type": "huawei_sun2000",
                    "enable_dual_discharge": True,
                    "enable_slot_a": True,
                    "enable_slot_b": True,
                    "discharge_a_start_time": "20:00",
                    "discharge_b_start_time": "03:00",
                },
            }
            await ws_save_config(hass, connection, msg)
            args, kwargs = hass.config_entries.async_update_entry.call_args
            new_data = kwargs.get("data") or args[1]
            assert new_data["discharge_b_start_time"] == "03:00"

        def test_parse_hhmm_basic(self):
            from custom_components.eeg_energy_optimizer.websocket_api import (
                _parse_hhmm,
            )
            assert _parse_hhmm("20:00") == 1200
            assert _parse_hhmm("03:30") == 210
            assert _parse_hhmm("00:00") == 0
            assert _parse_hhmm("23:59") == 1439
    ```

    **HINWEIS:** Falls `tests/test_websocket_api.py` noch nicht existiert oder andere Test-Helper benutzt, die obigen Tests in `tests/test_dual_window.py` lassen. Falls bestehende Tests in `tests/test_websocket_api.py` ein anderes WS-Connection-Mock-Pattern verwenden (z.B. `MockHomeAssistant`), passe die Mocks an.

    **Falls ws_save_config einen anderen Aufruf-Stil hat** (z.B. `decorators` für WS-Command-Schema), prüfe Read-Output und passe den Test-Setup an: ggf. ist die Funktion über einen `@websocket_api.async_response`-Decorator gewrappt und muss unwrapped aufgerufen werden, oder der Test arbeitet via `hass.bus.async_fire` mit dem Schema. Die obigen Tests arbeiten mit direktem Aufruf — falls das nicht funktioniert, lies `tests/test_websocket_api.py` für das exakte Pattern.
  </action>
  <verify>
    <automated>pytest tests/test_dual_window.py::TestSolarEdgeXOR -x -q</automated>
    <automated>pytest tests/test_dual_window.py::TestInverterRaceValidation -x -q</automated>
    <automated>pytest tests/test_websocket_api.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def _parse_hhmm(" custom_components/eeg_energy_optimizer/websocket_api.py` liefert genau einen Treffer
    - `grep -n "enable_dual_discharge=True nicht erlaubt" custom_components/eeg_energy_optimizer/websocket_api.py` liefert genau einen Treffer (Warning-Text)
    - `grep -n "nur ein Slot erlaubt" custom_components/eeg_energy_optimizer/websocket_api.py` liefert genau einen Treffer
    - `grep -n "b_start.*zu nah an a_start" custom_components/eeg_energy_optimizer/websocket_api.py` liefert genau einen Treffer (Race-Validation-Warning)
    - `grep -n "discharge_b_start_time.*new_b_start_str" custom_components/eeg_energy_optimizer/websocket_api.py` liefert genau einen Treffer (Auto-Korrektur)
    - `pytest tests/test_dual_window.py::TestSolarEdgeXOR -x -q` exit-code 0 (3 Tests grün)
    - `pytest tests/test_dual_window.py::TestInverterRaceValidation -x -q` exit-code 0 (3 Tests grün)
    - `pytest tests/test_websocket_api.py -x -q` exit-code 0 (keine Regression in bestehenden Tests)
  </acceptance_criteria>
  <done>
    websocket_api.ws_save_config bietet 3-fach SolarEdge-XOR-Auto-Korrektur + Inverter-Race-Validation mit Auto-Bump. _parse_hhmm-Helper verfügbar. Alle 6 neuen Tests grün, keine Regression.
  </done>
</task>

<task type="auto">
  <name>Task 2: Panel-UI — Master-Toggle + Slot-A/B-Sub-Bereiche + SolarEdge-XOR-Radio (Wizard + Settings)</name>
  <files>
    custom_components/eeg_energy_optimizer/frontend/eeg-optimizer-panel.js
  </files>
  <read_first>
    custom_components/eeg_energy_optimizer/frontend/eeg-optimizer-panel.js (Zeilen 1-150 für Klassen-Struktur und _wizardData/_settingsData-Initialisierung; Zeilen 2950-3070 für Wizard Step 4 Discharge-Section; Zeilen 3200-3340 für Settings-Tab evening Discharge-Section; Zeilen 3500-3700 für Save-Path/Read-Pfad falls nötig),
    .planning/milestones/v1.2-phases/11-dual-window-discharge/11-PATTERNS.md (Sektion "frontend/eeg-optimizer-panel.js Wizard Step 4 + Settings evening Tab"),
    .planning/milestones/v1.2-phases/11-dual-window-discharge/11-RESEARCH.md (Sektion "SolarEdge-XOR Panel-Erkennungs-Text"),
    .planning/milestones/v1.2-phases/11-dual-window-discharge/11-CONTEXT.md (D-06, D-07 — Panel-Layout-Entscheidungen)
  </read_first>
  <action>
    **A. _wizardData/_settingsData — neue Default-Werte:**

    Lies die Initialisierungs-Stellen für `_wizardData` und `_settingsData` (irgendwo in der Klassen-Definition, oft als Properties oder im Constructor). Ergänze die neuen Keys (sieben Stück) mit denselben Defaults wie const.py:

    ```javascript
    // Phase 11: Dual-Window
    enable_dual_discharge: true,
    enable_slot_a: true,
    enable_slot_b: true,
    discharge_a_start_time: "20:00",
    discharge_b_start_time: "03:00",
    discharge_b_end_cap: "07:00",
    discharge_a_reserve_pct: 15,
    ```

    Mache das sowohl im Wizard-Default-State als auch im Settings-Default-State.

    **B. Wizard Step 4 — Discharge-Section erweitern (Zeilen ~2960-3061):**

    Lokalisiere die Stelle wo `dischargeFields` (oder ähnlich benannt) als Template-Literal definiert wird. Vor dem PeakShare-Toggle ergänze:

    ```javascript
    const isSolarEdge = this._wizardData.inverter_type === "solaredge_storedge";
    const dualOn = !!this._wizardData.enable_dual_discharge;
    const slotA = !!this._wizardData.enable_slot_a;
    const slotB = !!this._wizardData.enable_slot_b;

    const dualMasterToggle = isSolarEdge ? "" : `
      <label style="display:flex;align-items:center;gap:8px;cursor:pointer;margin-bottom:12px">
        <input type="checkbox" data-field="enable_dual_discharge" ${dualOn ? "checked" : ""}>
        <div>
          <div style="font-weight:500">Dual-Window-Entladung</div>
          <div class="help-text" style="margin-top:2px">
            Zwei unabhängige Entladefenster: Slot A abends (~20:00 bis nach Mitternacht) und Slot B morgens (~03:00 bis vor Sonnenaufgang). Maximiert EEG-Bedarfsabdeckung in beiden Tagesperioden.
          </div>
        </div>
      </label>
    `;

    const solarEdgeXorRadio = isSolarEdge ? `
      <div class="field-group" title="NVRAM-Verschleiß: nur ein Slot pro Tag möglich">
        <label style="font-weight:500">Welcher Slot soll laufen?</label>
        <div class="help-text" style="margin-bottom:8px">
          Auf SolarEdge-Wechselrichtern können nicht beide Slots gleichzeitig laufen. Grund: SolarEdge schreibt Entlade-Kommandos in NVRAM-Speicher, der nur eine begrenzte Zahl Schreibzyklen zulässt. Wähle daher genau einen Slot:
        </div>
        <label style="display:block;margin-bottom:6px">
          <input type="radio" name="solaredge_slot" data-field-radio="slot_a" ${slotA && !slotB ? "checked" : ""}>
          Slot A — Abend (Default)
        </label>
        <label style="display:block">
          <input type="radio" name="solaredge_slot" data-field-radio="slot_b" ${slotB && !slotA ? "checked" : ""}>
          Slot B — Morgen
        </label>
      </div>
    ` : "";

    const slotAFields = (dualOn && !isSolarEdge) ? `
      <div class="feature-params" style="border-left:3px solid var(--primary-color);padding-left:12px;margin-bottom:12px">
        <label style="display:flex;align-items:center;gap:8px;cursor:pointer;margin-bottom:8px">
          <input type="checkbox" data-field="enable_slot_a" ${slotA ? "checked" : ""}>
          <span style="font-weight:500">Slot A — Abend</span>
        </label>
        <div class="field-group">
          <label>Slot-A-Start</label>
          <input type="text" data-field="discharge_a_start_time" placeholder="HH:MM" pattern="[0-2][0-9]:[0-5][0-9]" maxlength="5"
                 value="${this._wizardData.discharge_a_start_time || "20:00"}" style="width:80px">
          <div class="help-text">Startzeit der Abend-Entladung. Empfehlung 20:00 (EEG-Abendpeak 18:00–23:00).</div>
        </div>
        <div class="field-group">
          <label>Reserve für Slot B (%)</label>
          <input type="number" data-field="discharge_a_reserve_pct" min="0" max="50" step="1"
                 value="${this._wizardData.discharge_a_reserve_pct ?? 15}" style="width:80px">
          <div class="help-text">SOC-Reserve in Prozent, die Slot A für Slot B aufhebt. Default 15%.</div>
        </div>
      </div>
    ` : "";

    const slotBFields = (dualOn && !isSolarEdge) ? `
      <div class="feature-params" style="border-left:3px solid var(--accent-color);padding-left:12px;margin-bottom:12px">
        <label style="display:flex;align-items:center;gap:8px;cursor:pointer;margin-bottom:8px">
          <input type="checkbox" data-field="enable_slot_b" ${slotB ? "checked" : ""}>
          <span style="font-weight:500">Slot B — Morgen</span>
        </label>
        <div class="field-group">
          <label>Slot-B-Start</label>
          <input type="text" data-field="discharge_b_start_time" placeholder="HH:MM" pattern="[0-2][0-9]:[0-5][0-9]" maxlength="5"
                 value="${this._wizardData.discharge_b_start_time || "03:00"}" style="width:80px">
          <div class="help-text">Startzeit der Morgen-Entladung. Empfehlung 03:00.</div>
        </div>
        <div class="field-group">
          <label>Spätestes Slot-B-Ende</label>
          <input type="text" data-field="discharge_b_end_cap" placeholder="HH:MM" pattern="[0-2][0-9]:[0-5][0-9]" maxlength="5"
                 value="${this._wizardData.discharge_b_end_cap || "07:00"}" style="width:80px">
          <div class="help-text">Maximales Ende der Morgen-Entladung. Wird automatisch auf Sonnenaufgang−5min gekürzt, falls SA früher liegt. Default 07:00.</div>
        </div>
      </div>
    ` : "";
    ```

    Im bestehenden `dischargeFields`-Template (innerhalb des `nDischarge ? \`...\` : ""`-Blocks), füge die neuen Sub-Templates VOR dem PeakShare-Toggle ein:

    ```javascript
    const dischargeFields = nDischarge ? `
      <div class="feature-params">
        ${dualMasterToggle}
        ${solarEdgeXorRadio}
        ${slotAFields}
        ${slotBFields}
        <!-- bestehende PeakShare + Legacy discharge_start_time + discharge_power_kw Felder ... -->
        ...
      </div>
    ` : "";
    ```

    **WICHTIG für Conditional-Render des Legacy-`discharge_start_time`-Feldes:** Das bestehende `discharge_start_time`-Feld (Zeile ~3007) sollte nur noch sichtbar sein wenn dualOn=false ODER bei SolarEdge. Bei dualOn=true und nicht-SolarEdge wird stattdessen das Slot-A-Field sichtbar. **Pragmatisch:** Lasse das bestehende `discharge_start_time`-Feld bei dualOn=true einfach unverändert sichtbar — es bleibt als Legacy-Anker und Save-Path schreibt beide Werte (Backwards-Compat). User kann ignorieren oder Wert anpassen. Ein voller Visual-Switch zwischen Modi kann in einer späteren Phase ergänzt werden.

    **C. Settings-Tab evening — analog zu Wizard, aber mit `settings_*`-Präfix für `data-field`:**

    Lokalisiere den Settings-Tab evening (Zeilen ~3200-3340). Wiederhole die obigen Sub-Template-Definitionen mit `settings_`-Präfix:
    - `data-field="settings_enable_dual_discharge"`
    - `data-field="settings_enable_slot_a"` etc.
    - `data-field-radio="settings_slot_a"` und `settings_slot_b` für SolarEdge-Radio
    - Werte aus `this._settingsData.*` statt `this._wizardData.*`

    Füge analog zwischen Master-Toggle und PeakShare-Toggle ein.

    **D. Save-Path JS — keine Änderung nötig:**

    Bestehender Save-Path im Panel sammelt `data-field`-Attribute → schickt sie an WS `eeg_optimizer/save_config`. Da die neuen Keys denselben Mechanismus nutzen, sollte das automatisch funktionieren. Prüfe trotzdem nach dem Read, ob `_collectFormData()` (oder ähnlich) explizit nur bekannte Keys zulässt — falls ja, neue Keys whitelisten.

    **E. Radio-Button-Save-Logik (SolarEdge-XOR):**

    Das `data-field-radio`-Attribut ist neu. Im Save-Path-Sammeln muss zunächst ausgewertet werden, welcher Radio-Button selected ist:
    - Wenn `slot_a` selected → `enable_slot_a=true`, `enable_slot_b=false`
    - Wenn `slot_b` selected → `enable_slot_a=false`, `enable_slot_b=true`

    Falls `_collectFormData()` Radio-Buttons nicht standardmäßig handhabt, ergänze einen Special-Case:
    ```javascript
    // Phase 11: SolarEdge-XOR-Radio
    const radio = this.shadowRoot.querySelector('input[name="solaredge_slot"]:checked');
    if (radio) {
        const which = radio.getAttribute("data-field-radio");
        if (which === "slot_a" || which === "settings_slot_a") {
            data["enable_slot_a"] = true;
            data["enable_slot_b"] = false;
        } else if (which === "slot_b" || which === "settings_slot_b") {
            data["enable_slot_a"] = false;
            data["enable_slot_b"] = true;
        }
        data["enable_dual_discharge"] = false;
    }
    ```

    Diesen Block direkt nach dem allgemeinen `data-field`-Sammeln einfügen, damit er die generischen Werte überschreibt.

    **F. Echte Umlaute überall — KEINE ASCII-Substitution.** Validate via grep nach Compile.
  </action>
  <verify>
    <automated>pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'data-field="enable_dual_discharge"' custom_components/eeg_energy_optimizer/frontend/eeg-optimizer-panel.js` liefert mindestens einen Treffer (Wizard)
    - `grep -n 'data-field="settings_enable_dual_discharge"' custom_components/eeg_energy_optimizer/frontend/eeg-optimizer-panel.js` liefert mindestens einen Treffer (Settings)
    - `grep -n 'data-field="discharge_a_start_time"' custom_components/eeg_energy_optimizer/frontend/eeg-optimizer-panel.js` liefert mindestens einen Treffer
    - `grep -n 'data-field="discharge_b_start_time"' custom_components/eeg_energy_optimizer/frontend/eeg-optimizer-panel.js` liefert mindestens einen Treffer
    - `grep -n 'data-field="discharge_b_end_cap"' custom_components/eeg_energy_optimizer/frontend/eeg-optimizer-panel.js` liefert mindestens einen Treffer
    - `grep -n 'data-field="discharge_a_reserve_pct"' custom_components/eeg_energy_optimizer/frontend/eeg-optimizer-panel.js` liefert mindestens einen Treffer
    - `grep -n 'name="solaredge_slot"' custom_components/eeg_energy_optimizer/frontend/eeg-optimizer-panel.js` liefert mindestens 2 Treffer (Wizard + Settings)
    - `grep -n 'NVRAM-Verschleiß' custom_components/eeg_energy_optimizer/frontend/eeg-optimizer-panel.js` liefert mindestens einen Treffer (Tooltip + Hilfetext, ECHTE Umlaute)
    - `grep -n 'inverter_type === "solaredge_storedge"' custom_components/eeg_energy_optimizer/frontend/eeg-optimizer-panel.js` liefert mindestens einen Treffer (Conditional-Render)
    - `grep -n 'Slot A — Abend' custom_components/eeg_energy_optimizer/frontend/eeg-optimizer-panel.js` liefert mindestens einen Treffer (Radio-Label)
    - `grep -n 'Slot B — Morgen' custom_components/eeg_energy_optimizer/frontend/eeg-optimizer-panel.js` liefert mindestens einen Treffer
    - `grep -n 'Sonnenaufgang' custom_components/eeg_energy_optimizer/frontend/eeg-optimizer-panel.js` liefert mindestens einen Treffer (echte Umlaute, Slot-B-Hilfetext)
    - `pytest tests/ -q` exit-code 0 (Backend-Suite weiter grün; Panel-JS hat keine Backend-Tests, aber WS-Save-Tests aus Task 1 sichern Save-Path)
  </acceptance_criteria>
  <done>
    Panel zeigt Master-Toggle + Slot-A/B-Sub-Bereiche im Wizard und Settings-Tab. SolarEdge-Setups sehen XOR-Radio mit Default Slot A + Tooltip "NVRAM-Verschleiß: nur ein Slot pro Tag möglich". Save-Path sammelt neue data-field-Keys + Radio-Special-Case. Backend-Tests bleiben grün. Visuelle Verifikation erfolgt manuell im Rahmen der 7-Tage-UAT (siehe 11-VALIDATION.md).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Panel-JS → WebSocket-API | User-Input wird über `eeg_optimizer/save_config` gesendet; Server-side-Validation ist autoritativ |
| Save-Path → entry.data | Auto-Korrekturen mutieren entry.data vor async_update_entry |
| Radio-Button → enable_slot_a/b | XOR-Mapping muss korrekt sein (kein Drift zwischen Radio und Bool-Werten) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03-01 | Tampering | Frontend-Bypass: User schickt SolarEdge + dual=True direkt an WS | mitigate | Save-Path-Defense (Task 1) korrigiert Auto auf False mit _LOGGER.warning. Test `test_save_config_solaredge_disables_dual`. |
| T-11-03-02 | Tampering | Inverter-Race durch zu kleinen b_start | mitigate | Auto-Korrektur b_start auf a_min_end+5min (Task 1). Test `test_b_start_too_close_auto_bumped`. |
| T-11-03-03 | Tampering | Malformed Time-String aus Panel | mitigate | _parse_hhmm wirft ValueError; Save-Path skipt Race-Validation bei Parse-Fehler (vertraut auf Voluptuous-Schema) |
| T-11-03-04 | Information Disclosure | _LOGGER.warning leakt Pfad/Werte | accept | Warnings sind technisch und enthalten keine PII (nur Inverter-Type-Name + Time-Strings). Akzeptabel. |
| T-11-03-05 | DoS | Panel-Render mit ungültigen Defaults crashed Wizard | mitigate | Defaults im Panel-State (`||`-Fallback in Templates: `value="${this._wizardData.discharge_a_start_time || "20:00"}"`). Falls _wizardData-Keys fehlen, leerer State greift Fallback. |

Severity: alle LOW. Defense-in-depth (Migration-Default + Save-Path + Runtime-Force) macht jeden einzelnen Layer-Bypass folgenlos.
</threat_model>

<verification>
**Automated:**
```bash
pytest tests/test_dual_window.py::TestSolarEdgeXOR -x -q
pytest tests/test_dual_window.py::TestInverterRaceValidation -x -q
pytest tests/test_websocket_api.py -x -q
pytest tests/ -q
```

**Pattern checks:**
```bash
grep -c "data-field=\"enable_dual_discharge\"\|data-field=\"settings_enable_dual_discharge\"" custom_components/eeg_energy_optimizer/frontend/eeg-optimizer-panel.js  # mind. 2
grep -c "name=\"solaredge_slot\"" custom_components/eeg_energy_optimizer/frontend/eeg-optimizer-panel.js  # mind. 2 (Wizard + Settings)
grep -c "def _parse_hhmm" custom_components/eeg_energy_optimizer/websocket_api.py  # 1
```

**Manual (7-Tage-UAT, siehe 11-VALIDATION.md "Manual-Only Verifications"):**
1. SolarEdge-Setup im Wizard: XOR-Radio sichtbar mit Default Slot A, Master-Toggle versteckt, Tooltip "NVRAM-Verschleiß"
2. Huawei/Fronius/SolaX-Setup: Master-Toggle sichtbar, Slot-A/B-Sub-Bereiche bei Aktivierung
3. Settings-Tab evening: Spiegelt Wizard-Layout 1:1
</verification>

<success_criteria>
- _parse_hhmm-Helper in websocket_api.py
- ws_save_config: 3-fach SolarEdge-XOR-Auto-Korrektur (dual=True, beide Slots an, kein Slot an)
- ws_save_config: Inverter-Race-Auto-Korrektur (b_start auf a_min_end+5min)
- Panel: Master-Toggle enable_dual_discharge in Wizard + Settings (versteckt bei SolarEdge)
- Panel: Slot-A-Sub-Bereich (Toggle + a_start + reserve_pct) bei dual=True
- Panel: Slot-B-Sub-Bereich (Toggle + b_start + b_end_cap) bei dual=True
- Panel: SolarEdge-XOR-Radio mit Default Slot A + Tooltip "NVRAM-Verschleiß: nur ein Slot pro Tag möglich"
- Echte Umlaute in allen UI-Texten
- Alle Save-Path-Tests grün (3 SolarEdge + 3 Race-Validation)
- Volle Test-Suite grün
</success_criteria>

<output>
Nach Abschluss: erstelle `.planning/phases/11-dual-window-discharge/11-03-SUMMARY.md` mit:
- Save-Path-Erweiterungen (3 SolarEdge-Branches + Race-Validation)
- Panel-Erweiterungen (Wizard + Settings, Master-Toggle + Slot-A/B-Sub-Bereiche, SolarEdge-XOR-Radio)
- _parse_hhmm-Helper-Stelle
- Tests-Status (alle grün)
- Manual-UAT-Hinweis: SolarEdge-Setup-Visual-Check ist Teil der 7-Tage-UAT (siehe 11-VALIDATION.md)
- Hinweis: 11-04 ergänzt Markdown-Renderer + Activity-Log + CHANGELOG (parallel zu diesem Plan ausgeführt)
</output>
