---
phase: 11
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - custom_components/eeg_energy_optimizer/optimizer.py
  - custom_components/eeg_energy_optimizer/peakshare.py
  - tests/test_dual_window.py
autonomous: true
requirements: [SPEC-2, SPEC-3, SPEC-4, SPEC-5, SPEC-6, SPEC-7, SPEC-8]
tags: [dual-window, optimizer-refactor, peakshare-cache, hysteresis]

must_haves:
  truths:
    - "_should_discharge ist ein Dispatcher: er ruft _check_common_guards einmal vorab und wählt Legacy- oder Slot-Pfad"
    - "_evaluate_legacy_window ist 1:1 der alte _should_discharge-Body — bestehende Legacy-Tests bleiben grün"
    - "_evaluate_slot_a verwendet erhöhte SOC-Schwelle min_soc + reserve_pct, wenn Slot B aktiv; sonst nur min_soc"
    - "_evaluate_slot_b verwendet compute_b_window_end für das adaptive Ende vor Sonnenaufgang"
    - "Pro-Slot-Hysterese: _slot_a_activated_date und _slot_b_activated_date sind unabhängig, mit eigenen Reset-Triggern in _evaluate"
    - "SolarEdge: _is_solaredge → _should_discharge zwingt auf Legacy-Pfad (Defense-in-depth Runtime)"
    - "Decision.discharge_active_slot wird in _evaluate auf 'A' oder 'B' gesetzt, wenn ein Slot aktiv ist"
    - "PeakShare-Cache _discharge_plan ist dict[Literal['a','b'], tuple|None] statt single tuple"
    - "Slot A endet automatisch 5min vor Slot-B-Start, wenn Slot B aktiv ist"
    - "Slot B endet strikt vor Beginn der Morgen-Einspeisung (Mutex-Garantie)"
    - "A-only / B-only / Dual liefern erwartete Decision-Sequenz im 24h-Simulationstest"
  artifacts:
    - path: custom_components/eeg_energy_optimizer/optimizer.py
      provides: "Dispatcher _should_discharge + _check_common_guards + _evaluate_slot_a + _evaluate_slot_b + _evaluate_legacy_window + Pro-Slot-Hysterese-Felder + Reset-Logik in _evaluate"
      contains: "def _evaluate_slot_a"
    - path: custom_components/eeg_energy_optimizer/peakshare.py
      provides: "Cache-Schema dict[a/b] + slot-Parameter in get_discharge_plan + async_load-Migration"
      contains: "self._discharge_plan: dict"
    - path: tests/test_dual_window.py
      provides: "TestSlotAReserveLogic + TestSlotBLogic + TestProSlotHysteresis + TestSlotBPreSunriseCutoff (parametrisiert) + TestMutualExclusion + TestSolarEdgeRuntimeForce + TestEnableDualDischargeFalseLegacyPath"
      contains: "class TestSlotAReserveLogic"
  key_links:
    - from: "optimizer.py:_should_discharge"
      to: "optimizer.py:_check_common_guards + _evaluate_slot_a + _evaluate_slot_b + _evaluate_legacy_window"
      via: "Dispatcher-Aufruf basierend auf enable_dual_discharge + _is_solaredge"
      pattern: "self\\._evaluate_slot_a|self\\._evaluate_slot_b|self\\._evaluate_legacy_window"
    - from: "optimizer.py:_evaluate_slot_b"
      to: "optimizer.py:compute_b_window_end (aus Plan 11-01)"
      via: "module-level Funktionsaufruf"
      pattern: "compute_b_window_end\\("
    - from: "optimizer.py:_evaluate"
      to: "optimizer.py:Decision.discharge_active_slot"
      via: "Slot-Marker wird gesetzt, wenn reasons enthalten slot_a_active oder slot_b_active"
      pattern: "decision\\.discharge_active_slot\\s*=\\s*\"A\"|\"B\""
    - from: "peakshare.py:get_discharge_plan"
      to: "peakshare.py:_discharge_plan dict"
      via: "slot-Parameter steuert Cache-Lookup und Plan-Lock"
      pattern: "self\\._discharge_plan\\[slot\\]"
---

<objective>
Phase 11 Wave 2: Optimizer-Decision-Engine-Refactor und PeakShare-Cache-Schema-Migration.

Dieser Plan implementiert die zentrale Verhaltens-Änderung von Phase 11. Er macht den `_should_discharge`-Body refaktorierbar in einen Dispatcher mit drei Slot-Pfaden (Legacy / Slot-A / Slot-B), führt Pro-Slot-Hysterese ein, migriert den PeakShare-Cache von tuple auf slot-indiziertes dict, und stellt sicher, dass Decision.discharge_active_slot korrekt gesetzt wird.

Adressiert SPEC-Requirements:
- §2 Slot A — Abend-Entladung mit Energie-Reserve (effective_min_soc = min_soc + reserve_pct wenn Slot B aktiv)
- §3 Slot B — Morgen-Entladung mit adaptivem Ende (Aufruf von compute_b_window_end aus 11-01)
- §4 Pro-Slot-Hysterese (separate _activated_date-Felder + Reset-Trigger pro Slot)
- §5 Mutual Exclusion zur Morgen-Einspeisung (Slot B endet via compute_b_window_end strikt vor Sunrise und vor Morgen-Einspeisung)
- §6 SolarEdge-Sperre (Runtime-Erzwingung in _should_discharge: SolarEdge → Legacy-Pfad)
- §7 Telemetry-Reasons pro Slot (reasons-Liste enthält slot_a_active/slot_b_active im Pass-Fall)
- §8 Independent Slot-Aktivierung (A-only / B-only / Dual via _enable_slot_a + _enable_slot_b)

Adressiert User-Decisions:
- D-01 Drei Methoden: _evaluate_slot_a, _evaluate_slot_b, _evaluate_legacy_window + _check_common_guards
- D-02 Slot-State als _slot_a_activated_date / _slot_b_activated_date / _last_active_slot Instanz-Felder
- D-05 Single-Window-Pfad bleibt 1:1 erhalten als _evaluate_legacy_window
- D-10 Decision.discharge_active_slot wird in _evaluate gesetzt

Purpose: Decision-Engine ist nun Dual-Window-fähig. Bestehende Legacy-Pfad-Tests bleiben grün (D-05-Garantie); 24h-Simulationstests bestätigen Slot-A/B-Aktivierungs-Sequenzen.
Output: Erweiterte optimizer.py mit ~250 neuen Zeilen Decision-Code, migrierte peakshare.py, +5 neue Test-Klassen mit ~25 Test-Methoden in test_dual_window.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/milestones/v1.2-phases/11-dual-window-discharge/11-SPEC.md
@.planning/milestones/v1.2-phases/11-dual-window-discharge/11-CONTEXT.md
@.planning/milestones/v1.2-phases/11-dual-window-discharge/11-RESEARCH.md
@.planning/milestones/v1.2-phases/11-dual-window-discharge/11-PATTERNS.md
@.planning/milestones/v1.2-phases/11-dual-window-discharge/11-01-PLAN.md
@.planning/phases/11-dual-window-discharge/11-01-SUMMARY.md

<interfaces>
<!-- Bestehende Patterns/Signaturen aus dem Code, die der Refactor exakt erhält -->

Schnittstelle bleibt erhalten (aus optimizer.py:905-916):
```python
def _should_discharge(self, snap: Snapshot) -> tuple[bool, float, list[str], list[str], bool]:
    """Returns (should_discharge, min_soc, reasons, blocked_by, hysteresis_active) per D-11."""
```

Aus optimizer.py:366-371 (Hysterese-Felder-Pattern):
```python
self._morning_activated_date: str | None = None
self._discharge_activated_date: str | None = None
self._last_eval_zustand: str = STATE_NORMAL
```

Aus optimizer.py:382-384 (SolarEdge-Flag):
```python
self._discharge_aborted_date: str | None = None
self._is_solaredge = inv_type_cfg == "solaredge_storedge"
```

Aus optimizer.py:1081-1093 (Reset-Pattern in _evaluate):
```python
today_str = snap.now.strftime("%Y-%m-%d")
if self._morning_activated_date is not None and self._morning_activated_date < today_str:
    self._morning_activated_date = None
if (self._discharge_activated_date is not None
        and self._discharge_activated_date < today_str
        and snap.sunrise_today is not None
        and snap.now >= snap.sunrise_today):
    self._discharge_activated_date = None
```

Aus peakshare.py:171-172 (Cache-Init):
```python
self._discharge_plan: tuple[datetime, datetime] | None = None
self._discharge_plan_date: str | None = None
```

Aus peakshare.py:320-325 (Cache-Lookup):
```python
today_str = now.strftime("%Y-%m-%d")
if self._discharge_plan_date == today_str:
    return self._discharge_plan
```

Neue Konstanten (aus Plan 11-01) zur Verwendung:
- compute_b_window_end (optimizer.py)
- REASON_BEFORE_SLOT_A, REASON_SLOT_A_ACTIVE, REASON_SLOT_A_RESERVE_REACHED
- REASON_BETWEEN_SLOTS, REASON_BEFORE_SLOT_B, REASON_SLOT_B_ACTIVE
- REASON_SLOT_B_WINDOW_EXPIRED, REASON_SLOT_B_PRE_SUNRISE_CUTOFF
- CONF_ENABLE_DUAL_DISCHARGE, CONF_ENABLE_SLOT_A, CONF_ENABLE_SLOT_B
- CONF_DISCHARGE_A_START_TIME, CONF_DISCHARGE_B_START_TIME
- CONF_DISCHARGE_B_END_CAP, CONF_DISCHARGE_A_RESERVE_PCT
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Optimizer-Refactor in 3 Pfade + Pro-Slot-Hysterese + Decision-Slot-Setting</name>
  <files>
    custom_components/eeg_energy_optimizer/optimizer.py,
    tests/test_dual_window.py
  </files>
  <read_first>
    custom_components/eeg_energy_optimizer/optimizer.py (Zeilen 1-300 für Imports/Konstanten/Reasons + 350-410 für __init__-Felder + 600-650 für _get_sun_times + 850-880 für _should_block_charging + 905-1230 KOMPLETT für _should_discharge UND _evaluate),
    .planning/milestones/v1.2-phases/11-dual-window-discharge/11-PATTERNS.md (Sektionen "_should_discharge Refactor", "Slot-State-Felder + Reset", "_build_markdown" — letzteres als Read-only-Kontext, Markdown-Update kommt in Plan 11-04),
    .planning/milestones/v1.2-phases/11-dual-window-discharge/11-RESEARCH.md (Sektionen "Refactor-Strategie", "Pro-Slot-Hysterese", "Code Examples Slot-A/Slot-B", "Pitfalls 1-6"),
    .planning/milestones/v1.2-phases/11-dual-window-discharge/11-01-PLAN.md (must_haves + neue Konstanten),
    .planning/phases/11-dual-window-discharge/11-01-SUMMARY.md (concrete API der neuen Konstanten/Funktion)
  </read_first>
  <behavior>
    - _check_common_guards(snap) liefert (blocked_by_list, min_soc) — wird einmal vorab aufgerufen
    - _evaluate_legacy_window(snap, min_soc) verhält sich byte-identisch zum heutigen _should_discharge-Body (vor Refactor)
    - _evaluate_slot_a(snap, min_soc) liefert (passed, reasons, blocked_by, hysteresis_active) — wie _should_discharge ohne min_soc
    - _evaluate_slot_b(snap, min_soc) liefert dieselbe Tuple-Form
    - Slot A SOC-Schwelle: a_min_soc = min_soc + reserve_pct (wenn Slot B aktiv UND _enable_slot_b True), sonst min_soc
    - Slot A Hysterese-Aufschlag: +5% wenn _slot_a_activated_date is not None und _last_active_slot != "A"
    - Slot A endet 5min vor Slot-B-Start: bei snap.now >= b_start - 5min → REASON_SLOT_A_RESERVE_REACHED
    - Slot B SOC-Schwelle: min_soc + (5 if reactivation else 0) — kein Reserve-Aufschlag
    - Slot B Window-Ende: compute_b_window_end(snap.now, snap.sunrise, b_end_cap, morning_offset_h)
    - Slot B vor b_start → REASON_BEFORE_SLOT_B; nach b_end → REASON_SLOT_B_WINDOW_EXPIRED
    - Slot B mit b_start >= b_end_effective → REASON_SLOT_B_PRE_SUNRISE_CUTOFF (Sommer-Edge-Case)
    - _should_discharge ist Dispatcher: SolarEdge oder enable_dual_discharge=False → Legacy-Pfad
    - SolarEdge erzwingt zur Laufzeit _enable_dual_discharge=False im __init__ (Defense-in-depth)
    - _evaluate setzt decision.discharge_active_slot = "A" wenn REASON_SLOT_A_ACTIVE in reasons; "B" wenn REASON_SLOT_B_ACTIVE
    - _evaluate setzt _slot_a_activated_date = today_str beim erstmaligen Aktivieren von Slot A heute (analog Slot B)
    - Reset von _slot_a/b_activated_date: nur nach today's Sunrise (analog _discharge_activated_date)
    - _last_active_slot wird gesetzt wenn Slot aktiv ist (für Hysterese-Reaktivierungs-Check)
  </behavior>
  <action>
    **A. optimizer.py — neue Felder in EEGOptimizer.__init__ (nach Zeile 371, neben den bestehenden Hysterese-Feldern):**

    Lies zuerst Zeilen 350-400 um exakte Position zu sehen. Füge ein:
    ```python
    # Phase 11: Pro-Slot-Hysterese-Felder (analog _discharge_activated_date)
    self._slot_a_activated_date: str | None = None  # ISO "YYYY-MM-DD"
    self._slot_b_activated_date: str | None = None
    self._last_active_slot: str | None = None  # "A" | "B" | None — für Reaktivierungs-Check
    ```

    **B. optimizer.py — neue Config-Reads in EEGOptimizer.__init__ (in der Sektion wo bereits `self._discharge_start_h`, `self._enable_night_discharge` etc. gelesen werden, suche nach "discharge_start_time" + "enable_night_discharge" Reads):**

    ```python
    # Phase 11: Dual-Window Config-Reads
    self._enable_dual_discharge: bool = bool(
        config.get(CONF_ENABLE_DUAL_DISCHARGE, True)
    )
    self._enable_slot_a: bool = bool(config.get(CONF_ENABLE_SLOT_A, True))
    self._enable_slot_b: bool = bool(config.get(CONF_ENABLE_SLOT_B, True))
    self._discharge_a_reserve_pct: int = int(
        config.get(CONF_DISCHARGE_A_RESERVE_PCT, 15)
    )
    a_start_str = str(config.get(CONF_DISCHARGE_A_START_TIME, "20:00"))
    b_start_str = str(config.get(CONF_DISCHARGE_B_START_TIME, "03:00"))
    b_end_cap_str = str(config.get(CONF_DISCHARGE_B_END_CAP, "07:00"))
    self._discharge_a_start_h, self._discharge_a_start_m = (
        int(p) for p in a_start_str.split(":")
    )
    self._discharge_b_start_h, self._discharge_b_start_m = (
        int(p) for p in b_start_str.split(":")
    )
    self._discharge_b_end_cap: str = b_end_cap_str
    # Defense-in-depth: SolarEdge erzwingt Legacy-Pfad zur Laufzeit
    if self._is_solaredge and self._enable_dual_discharge:
        _LOGGER.warning(
            "SolarEdge: enable_dual_discharge=True nicht erlaubt — "
            "auf False gesetzt (NVRAM-Verschleiß)"
        )
        self._enable_dual_discharge = False
    ```

    Wichtig: `self._is_solaredge` wird bereits in der bestehenden `__init__` gesetzt (Zeile ~384). Stelle sicher, dass die Phase-11-Reads NACH dem `_is_solaredge`-Set kommen.

    Imports oben in optimizer.py erweitern:
    ```python
    from .const import (
        ... bestehende Imports ...,
        CONF_ENABLE_DUAL_DISCHARGE,
        CONF_ENABLE_SLOT_A,
        CONF_ENABLE_SLOT_B,
        CONF_DISCHARGE_A_START_TIME,
        CONF_DISCHARGE_B_START_TIME,
        CONF_DISCHARGE_B_END_CAP,
        CONF_DISCHARGE_A_RESERVE_PCT,
    )
    ```

    **C. optimizer.py — Refactor _should_discharge: Dispatcher + 4 neue Methoden:**

    Schritt 1: Aktuellen Body von `_should_discharge` (Zeilen 905-1055) komplett kopieren.
    Schritt 2: Methode umbenennen: aus `_should_discharge(self, snap: Snapshot)` wird `_evaluate_legacy_window(self, snap: Snapshot)`. Body 1:1 erhalten — D-05 garantiert byte-identisches Legacy-Verhalten. Docstring anpassen: "Legacy single-window discharge evaluation. Aktiv wenn enable_dual_discharge=False oder bei SolarEdge."

    Schritt 3: Drei neue Methoden ergänzen direkt vor `_evaluate_legacy_window`:

    ```python
    def _check_common_guards(
        self, snap: Snapshot
    ) -> tuple[list[str], float]:
        """Slot-übergreifende Guards.

        Liefert (blocked_by, min_soc). Wenn blocked_by leer ist, dürfen Slot-A/B
        evaluiert werden. Reihenfolge der Guards entspricht dem heutigen
        _should_discharge-Body-Anfang (D-01).
        """
        if not self._enable_night_discharge:
            return ([REASON_NIGHT_DISCHARGE_DISABLED], float(self._min_soc))
        min_soc = self._calc_min_soc(snap)
        if min_soc >= 100.0:
            return ([REASON_OVERNIGHT_DEMAND_TOO_HIGH], min_soc)
        # SOC-Sensor-Verfügbarkeit (analog _evaluate-SOC-None-Guard,
        # aber hier explizit: snap.battery_soc kann None sein)
        if snap.battery_soc is None:
            return ([REASON_BATTERY_SOC_UNAVAILABLE], min_soc)
        # Tomorrow-PV-Surplus (gleiche Berechnung wie heute in _should_discharge)
        cwb = snap.consumption_tomorrow_daylight_kwh * (
            1 + self._safety_buffer_pct / 100
        )
        bcn = (
            (100 - self._min_soc) / 100
            * snap.battery_capacity_kwh
            * snap.sim_factor
        )
        pv_tomorrow = snap.pv_tomorrow_kwh if snap.pv_tomorrow_kwh is not None else 0.0
        if pv_tomorrow < (cwb + bcn):
            return ([REASON_TOMORROW_PV_INSUFFICIENT], min_soc)
        # SolarEdge-Discharge-Aborted-Watchdog
        today_str = snap.now.strftime("%Y-%m-%d")
        if self._is_solaredge and self._discharge_aborted_date == today_str:
            return ([REASON_DISCHARGE_ABORTED_TODAY], min_soc)
        return ([], min_soc)

    def _evaluate_slot_a(
        self, snap: Snapshot, min_soc: float
    ) -> tuple[bool, list[str], list[str], bool]:
        """Slot A — Abend-Entladung mit Energie-Reserve.

        Returns (passed, reasons, blocked_by, hysteresis_active).
        SOC-Schwelle: min_soc + reserve_pct (wenn Slot B aktiv), sonst min_soc.
        Hysterese-Aufschlag +5% bei Reaktivierung innerhalb derselben Sitzung.
        """
        # Slot-A-Window: a_start bis 5min vor b_start (wenn B aktiv)
        a_start = snap.now.replace(
            hour=self._discharge_a_start_h,
            minute=self._discharge_a_start_m,
            second=0,
            microsecond=0,
        )
        if snap.now < a_start:
            return (False, [], [REASON_BEFORE_SLOT_A], False)

        # 5min-Pause vor Slot B (wenn B aktiv)
        if self._enable_slot_b:
            b_start = snap.now.replace(
                hour=self._discharge_b_start_h,
                minute=self._discharge_b_start_m,
                second=0,
                microsecond=0,
            )
            if self._discharge_b_start_h < 12:
                # Slot B liegt nach Mitternacht → +1 Tag
                b_start = b_start + timedelta(days=1)
            a_end_cap = b_start - timedelta(minutes=5)
            if snap.now >= a_end_cap:
                return (False, [], [REASON_SLOT_A_RESERVE_REACHED], False)

        # SOC-Schwelle
        a_reserve = self._discharge_a_reserve_pct if self._enable_slot_b else 0
        a_min_soc = min_soc + a_reserve

        is_reactivation = (
            self._slot_a_activated_date is not None
            and self._last_active_slot != "A"
        )
        effective_min_soc = a_min_soc + (5 if is_reactivation else 0)

        if snap.battery_soc <= effective_min_soc:
            blocked_by = []
            if is_reactivation:
                blocked_by.append(REASON_HYSTERESIS_STRICT)
            blocked_by.append(REASON_SLOT_A_RESERVE_REACHED)
            return (False, [], blocked_by, is_reactivation)

        passing = []
        if is_reactivation:
            passing.append(REASON_HYSTERESIS_STRICT)
        passing.append(REASON_SOC_ABOVE_MIN)
        passing.append(REASON_SLOT_A_ACTIVE)
        return (True, passing, [], is_reactivation)

    def _evaluate_slot_b(
        self, snap: Snapshot, min_soc: float
    ) -> tuple[bool, list[str], list[str], bool]:
        """Slot B — Morgen-Entladung mit adaptivem Ende vor Sonnenaufgang."""
        if snap.sunrise is None:
            return (False, [], [REASON_SUNRISE_UNKNOWN], False)

        # b_start auf morgigen Tag projizieren wenn now < 12:00 OK,
        # sonst auf morgen verschieben (analog discharge_start_resolved-Pattern
        # aus optimizer.py:953-962)
        b_start_today = snap.now.replace(
            hour=self._discharge_b_start_h,
            minute=self._discharge_b_start_m,
            second=0,
            microsecond=0,
        )
        if snap.now.hour >= 12 and self._discharge_b_start_h < 12:
            # Vorabend → b_start auf morgen
            b_start = b_start_today + timedelta(days=1)
        else:
            b_start = b_start_today

        b_end = compute_b_window_end(
            snap.now,
            snap.sunrise,
            self._discharge_b_end_cap,
            float(self._morning_start_offset),
        )
        if b_end is None or b_start >= b_end:
            return (False, [], [REASON_SLOT_B_PRE_SUNRISE_CUTOFF], False)
        if snap.now < b_start:
            return (False, [], [REASON_BEFORE_SLOT_B], False)
        if snap.now >= b_end:
            return (False, [], [REASON_SLOT_B_WINDOW_EXPIRED], False)

        is_reactivation = (
            self._slot_b_activated_date is not None
            and self._last_active_slot != "B"
        )
        effective_min_soc = min_soc + (5 if is_reactivation else 0)

        if snap.battery_soc <= effective_min_soc:
            blocked_by = []
            if is_reactivation:
                blocked_by.append(REASON_HYSTERESIS_STRICT)
            blocked_by.append(REASON_SOC_BELOW_MIN)
            return (False, [], blocked_by, is_reactivation)

        passing = []
        if is_reactivation:
            passing.append(REASON_HYSTERESIS_STRICT)
        passing.append(REASON_SOC_ABOVE_MIN)
        passing.append(REASON_SLOT_B_ACTIVE)
        return (True, passing, [], is_reactivation)

    def _should_discharge(
        self, snap: Snapshot
    ) -> tuple[bool, float, list[str], list[str], bool]:
        """Determine if evening discharge should be active.

        Phase 11 Dispatcher: ruft _check_common_guards einmal vorab und wählt
        anhand enable_dual_discharge + _is_solaredge den Pfad
        (Legacy/Slot-A/Slot-B). Schnittstelle bleibt erhalten (D-11):
        (should_discharge, min_soc, reasons, blocked_by, hysteresis_active).
        """
        common_blocked, min_soc = self._check_common_guards(snap)
        if common_blocked:
            return (False, min_soc, [], common_blocked, False)

        # Pfadwahl: SolarEdge oder Legacy-Konfig → Single-Slot-Pfad
        use_legacy = (not self._enable_dual_discharge) or self._is_solaredge
        if use_legacy:
            return self._evaluate_legacy_window(snap, min_soc)

        # Dual-Mode: Slot A bevorzugt (zeitlich früher), Fallback Slot B
        passed_a = blocked_a = []
        hyst_a = False
        passed_b = blocked_b = []
        hyst_b = False
        if self._enable_slot_a:
            passed_a_flag, passed_a, blocked_a, hyst_a = self._evaluate_slot_a(
                snap, min_soc
            )
            if passed_a_flag:
                return (True, min_soc, passed_a, [], hyst_a)
        if self._enable_slot_b:
            passed_b_flag, passed_b, blocked_b, hyst_b = self._evaluate_slot_b(
                snap, min_soc
            )
            if passed_b_flag:
                return (True, min_soc, passed_b, [], hyst_b)

        # Beide Slots blockiert/aus → kombinierte blocked_by-Liste
        combined_blocked = []
        if self._enable_slot_a:
            combined_blocked.extend(blocked_a)
        if self._enable_slot_b:
            combined_blocked.extend(blocked_b)
        if not combined_blocked:
            # Beide Slots disabled — synthetischer Reason
            combined_blocked = [REASON_NIGHT_DISCHARGE_DISABLED]
        return (False, min_soc, [], combined_blocked, hyst_a or hyst_b)
    ```

    **WICHTIG:** Die Signatur des bisherigen `_should_discharge` musste auf `_evaluate_legacy_window(self, snap, min_soc)` umgestellt werden. Da der Legacy-Body bisher selbst `min_soc = self._calc_min_soc(snap)` aufruft (Zeile 920), MUSS dieser Aufruf im umbenannten Body ENTFERNT werden — `min_soc` wird jetzt vom Dispatcher injiziert. Auch die `if not self._enable_night_discharge` und `if min_soc >= 100.0` Guards (Zeilen 918-927) MÜSSEN aus _evaluate_legacy_window entfernt werden, weil _check_common_guards sie bereits abdeckt. Tests in TestEnableDualDischargeFalseLegacyPath verifizieren das Ergebnis bleibt funktional gleich.

    **Alternative (sicherer für D-05):** Lasse die Guards und das `_calc_min_soc` im umbenannten `_evaluate_legacy_window` STEHEN, und passe `_should_discharge` so an, dass bei `use_legacy=True` direkt `_evaluate_legacy_window(snap)` ohne `min_soc`-Parameter aufgerufen wird. In dem Fall hat _evaluate_legacy_window die Original-Signatur `(snap)` und gibt das volle 5-Tuple zurück. Wähle diese Alternative — sie ist kompatibler mit der byte-identischen Legacy-Garantie.

    **Final-Variante:** _evaluate_legacy_window behält die Original-Signatur und Original-Body 1:1 (KEINE Guard-Entfernung). Dispatcher ruft entweder _evaluate_legacy_window (wenn use_legacy) oder den Common-Guards-Pfad (wenn dual). Das bedeutet: bei use_legacy=True werden die Common-Guards _nicht_ vorab aufgerufen — die Legacy-Methode hat ihre eigenen Guards.

    **D. optimizer.py — _evaluate-Erweiterung: Reset-Logik + Slot-Marker + Aktivierungsdatum:**

    Lies optimizer.py:1057-1230 (komplettes _evaluate). Erweiterungen:

    1) Reset-Block (nach Zeile 1093 — nach dem _discharge_activated_date-Reset):
    ```python
    # Phase 11: Pro-Slot-Hysterese-Reset (analog _discharge_activated_date)
    if (
        self._slot_a_activated_date is not None
        and self._slot_a_activated_date < today_str
        and snap.sunrise_today is not None
        and snap.now >= snap.sunrise_today
    ):
        self._slot_a_activated_date = None
    if (
        self._slot_b_activated_date is not None
        and self._slot_b_activated_date < today_str
        and snap.sunrise_today is not None
        and snap.now >= snap.sunrise_today
    ):
        self._slot_b_activated_date = None
    ```

    2) Slot-Marker setzen — direkt nachdem `should_discharge`-Ergebnis verarbeitet wird und Decision befüllt ist. Suche im _evaluate-Body nach der Stelle wo Decision.zustand = STATE_ABEND_ENTLADUNG gesetzt wird (~Zeile 1102-1107):

    ```python
    # Phase 11: Slot-Marker an Decision hängen
    if zustand == STATE_ABEND_ENTLADUNG:
        if REASON_SLOT_A_ACTIVE in (decision.reasons or []):
            decision.discharge_active_slot = "A"
        elif REASON_SLOT_B_ACTIVE in (decision.reasons or []):
            decision.discharge_active_slot = "B"
        # Sonst (Legacy-Pfad): bleibt None — D-10 explicit
    ```

    3) Aktivierungsdatum setzen (nach dem bestehenden _discharge_activated_date-Set in Z. 1116-1118, oder die Logik ersetzen wenn sie zwingend pro-Slot werden soll). **Empfehlung:** Bestehenden _discharge_activated_date-Block beibehalten (für Legacy-Pfad und Compat) und ergänzen:

    ```python
    elif zustand == STATE_ABEND_ENTLADUNG:
        if self._discharge_activated_date is None:
            self._discharge_activated_date = today_str
        # Phase 11: Pro-Slot-Aktivierungsdatum
        if decision.discharge_active_slot == "A":
            if self._slot_a_activated_date is None:
                self._slot_a_activated_date = today_str
            self._last_active_slot = "A"
        elif decision.discharge_active_slot == "B":
            if self._slot_b_activated_date is None:
                self._slot_b_activated_date = today_str
            self._last_active_slot = "B"
    ```

    **E. tests/test_dual_window.py — neue Test-Klassen ergänzen:**

    Imports oben erweitern um `_make_config`, `_make_snapshot`, `_make_optimizer` aus conftest:
    ```python
    from tests.conftest import _make_config, _make_snapshot, _make_optimizer
    ```

    Klassen (Test-Code parameterisiert + minimalstes Fixture-Setup):

    ```python
    class TestSlotAReserveLogic:
        def test_a_only_uses_min_soc_no_reserve(self, mock_hass, mock_inverter, mock_coordinator, mock_provider):
            cfg = _make_config(
                enable_dual_discharge=True,
                enable_slot_a=True, enable_slot_b=False,
                discharge_a_start_time="20:00",
                discharge_a_reserve_pct=15,
                min_soc=20,
            )
            opt = _make_optimizer(mock_hass, mock_inverter, mock_coordinator, mock_provider, config=cfg)
            snap = _make_snapshot(
                now=datetime(2026, 6, 15, 21, 0, tzinfo=timezone.utc),
                battery_soc=25.0,  # > min_soc=20, kein reserve_aufschlag
                sunrise=datetime(2026, 6, 16, 4, 52, tzinfo=timezone.utc),
                sunrise_today=datetime(2026, 6, 15, 4, 52, tzinfo=timezone.utc),
            )
            passed, reasons, blocked, hyst = opt._evaluate_slot_a(snap, 20.0)
            assert passed is True
            assert REASON_SLOT_A_ACTIVE in reasons

        def test_dual_a_uses_min_soc_plus_reserve(self, mock_hass, mock_inverter, mock_coordinator, mock_provider):
            cfg = _make_config(
                enable_dual_discharge=True,
                enable_slot_a=True, enable_slot_b=True,
                discharge_a_start_time="20:00",
                discharge_b_start_time="03:00",
                discharge_a_reserve_pct=15,
                min_soc=20,
            )
            opt = _make_optimizer(mock_hass, mock_inverter, mock_coordinator, mock_provider, config=cfg)
            snap = _make_snapshot(
                now=datetime(2026, 6, 15, 21, 0, tzinfo=timezone.utc),
                battery_soc=30.0,  # < min_soc(20) + reserve(15) = 35
                sunrise=datetime(2026, 6, 16, 4, 52, tzinfo=timezone.utc),
                sunrise_today=datetime(2026, 6, 15, 4, 52, tzinfo=timezone.utc),
            )
            passed, reasons, blocked, hyst = opt._evaluate_slot_a(snap, 20.0)
            assert passed is False
            assert REASON_SLOT_A_RESERVE_REACHED in blocked

        def test_a_ends_5min_before_b_start(self, mock_hass, mock_inverter, mock_coordinator, mock_provider):
            cfg = _make_config(
                enable_dual_discharge=True,
                enable_slot_a=True, enable_slot_b=True,
                discharge_a_start_time="20:00",
                discharge_b_start_time="03:00",
            )
            opt = _make_optimizer(mock_hass, mock_inverter, mock_coordinator, mock_provider, config=cfg)
            # snap.now = 02:56 → b_start = morgen 03:00, a_end_cap = 02:55 → now >= a_end_cap
            snap = _make_snapshot(
                now=datetime(2026, 6, 16, 2, 56, tzinfo=timezone.utc),
                battery_soc=80.0,
                sunrise=datetime(2026, 6, 16, 4, 52, tzinfo=timezone.utc),
                sunrise_today=datetime(2026, 6, 16, 4, 52, tzinfo=timezone.utc),
            )
            passed, reasons, blocked, hyst = opt._evaluate_slot_a(snap, 20.0)
            assert passed is False
            assert REASON_SLOT_A_RESERVE_REACHED in blocked


    class TestSlotBLogic:
        def test_b_only_uses_min_soc_threshold(self, mock_hass, mock_inverter, mock_coordinator, mock_provider):
            cfg = _make_config(
                enable_dual_discharge=True,
                enable_slot_a=False, enable_slot_b=True,
                discharge_b_start_time="03:00",
                discharge_b_end_cap="07:00",
                min_soc=20,
            )
            opt = _make_optimizer(mock_hass, mock_inverter, mock_coordinator, mock_provider, config=cfg)
            snap = _make_snapshot(
                now=datetime(2026, 12, 21, 4, 0, tzinfo=timezone.utc),
                battery_soc=25.0,
                sunrise=datetime(2026, 12, 21, 7, 30, tzinfo=timezone.utc),
                sunrise_today=datetime(2026, 12, 20, 7, 30, tzinfo=timezone.utc),  # gestern's sunrise
            )
            passed, reasons, blocked, hyst = opt._evaluate_slot_b(snap, 20.0)
            assert passed is True
            assert REASON_SLOT_B_ACTIVE in reasons

        def test_b_window_expired_marks_correct_reason(self, mock_hass, mock_inverter, mock_coordinator, mock_provider):
            cfg = _make_config(
                enable_dual_discharge=True,
                enable_slot_a=False, enable_slot_b=True,
                discharge_b_start_time="03:00",
                discharge_b_end_cap="07:00",
            )
            opt = _make_optimizer(mock_hass, mock_inverter, mock_coordinator, mock_provider, config=cfg)
            snap = _make_snapshot(
                now=datetime(2026, 12, 21, 7, 5, tzinfo=timezone.utc),  # nach b_end=07:00
                battery_soc=80.0,
                sunrise=datetime(2026, 12, 21, 7, 30, tzinfo=timezone.utc),
                sunrise_today=datetime(2026, 12, 21, 7, 30, tzinfo=timezone.utc),
            )
            passed, reasons, blocked, hyst = opt._evaluate_slot_b(snap, 20.0)
            assert passed is False
            assert REASON_SLOT_B_WINDOW_EXPIRED in blocked

        def test_b_pre_sunrise_cutoff_summer_edge_case(self, mock_hass, mock_inverter, mock_coordinator, mock_provider):
            # Sommer: SA 04:52, b_start=05:00 → b_end=04:47 < b_start → SLOT_B_PRE_SUNRISE_CUTOFF
            cfg = _make_config(
                enable_dual_discharge=True,
                enable_slot_a=False, enable_slot_b=True,
                discharge_b_start_time="05:00",
                discharge_b_end_cap="07:00",
            )
            opt = _make_optimizer(mock_hass, mock_inverter, mock_coordinator, mock_provider, config=cfg)
            snap = _make_snapshot(
                now=datetime(2026, 6, 21, 3, 0, tzinfo=timezone.utc),
                battery_soc=80.0,
                sunrise=datetime(2026, 6, 21, 4, 52, tzinfo=timezone.utc),
                sunrise_today=datetime(2026, 6, 20, 4, 52, tzinfo=timezone.utc),
            )
            passed, reasons, blocked, hyst = opt._evaluate_slot_b(snap, 20.0)
            assert passed is False
            assert REASON_SLOT_B_PRE_SUNRISE_CUTOFF in blocked


    class TestProSlotHysteresis:
        def test_a_reactivation_requires_min_soc_plus_5(self, mock_hass, mock_inverter, mock_coordinator, mock_provider):
            cfg = _make_config(
                enable_dual_discharge=True, enable_slot_a=True, enable_slot_b=True,
                discharge_a_start_time="20:00", discharge_b_start_time="03:00",
                discharge_a_reserve_pct=15, min_soc=20,
            )
            opt = _make_optimizer(mock_hass, mock_inverter, mock_coordinator, mock_provider, config=cfg)
            opt._slot_a_activated_date = "2026-06-15"  # bereits aktiv heute
            opt._last_active_slot = "B"  # zwischenzeitlich war B aktiv → Reaktivierung
            snap = _make_snapshot(
                now=datetime(2026, 6, 15, 21, 0, tzinfo=timezone.utc),
                battery_soc=39.0,  # > 35 (min+reserve) aber <= 40 (min+reserve+5)
                sunrise=datetime(2026, 6, 16, 4, 52, tzinfo=timezone.utc),
                sunrise_today=datetime(2026, 6, 15, 4, 52, tzinfo=timezone.utc),
            )
            passed, reasons, blocked, hyst = opt._evaluate_slot_a(snap, 20.0)
            assert passed is False
            assert REASON_HYSTERESIS_STRICT in blocked
            assert hyst is True

        def test_b_starts_without_a_reactivation_aufschlag(self, mock_hass, mock_inverter, mock_coordinator, mock_provider):
            cfg = _make_config(
                enable_dual_discharge=True, enable_slot_a=True, enable_slot_b=True,
                discharge_b_start_time="03:00", discharge_b_end_cap="07:00",
                min_soc=20,
            )
            opt = _make_optimizer(mock_hass, mock_inverter, mock_coordinator, mock_provider, config=cfg)
            opt._slot_a_activated_date = "2026-06-15"  # A heute aktiv gewesen
            opt._slot_b_activated_date = None  # B noch nicht aktiv heute
            opt._last_active_slot = "A"
            snap = _make_snapshot(
                now=datetime(2026, 6, 16, 4, 0, tzinfo=timezone.utc),
                battery_soc=22.0,  # > min_soc=20, ohne Aufschlag passes
                sunrise=datetime(2026, 6, 16, 4, 52, tzinfo=timezone.utc),
                sunrise_today=datetime(2026, 6, 15, 4, 52, tzinfo=timezone.utc),
            )
            passed, reasons, blocked, hyst = opt._evaluate_slot_b(snap, 20.0)
            # B nutzt min_soc=20 ohne reserve und ohne Hysterese-Aufschlag (B war noch nicht aktiv)
            assert passed is True
            assert hyst is False


    @pytest.mark.parametrize("sunrise_hour,morning_offset", [
        (5, 0), (5, 1), (6, 0), (7, 0), (7, 1), (8, 0), (8, 1),
    ])
    class TestSlotBPreSunriseCutoff:
        def test_slot_b_ends_min_5min_before_morning_einspeisung(
            self, sunrise_hour, morning_offset,
            mock_hass, mock_inverter, mock_coordinator, mock_provider,
        ):
            cfg = _make_config(
                enable_dual_discharge=True, enable_slot_a=False, enable_slot_b=True,
                discharge_b_start_time="03:00", discharge_b_end_cap="07:00",
                morning_start_offset=morning_offset,
            )
            opt = _make_optimizer(mock_hass, mock_inverter, mock_coordinator, mock_provider, config=cfg)
            sunrise = datetime(2026, 12, 21, sunrise_hour, 30, tzinfo=timezone.utc)
            b_end = compute_b_window_end(
                datetime(2026, 12, 21, 3, 0, tzinfo=timezone.utc),
                sunrise, "07:00", float(morning_offset),
            )
            morning_einspeisung_start = sunrise - timedelta(hours=morning_offset)
            assert (morning_einspeisung_start - b_end) >= timedelta(minutes=5)


    class TestMutualExclusion:
        def test_slot_b_does_not_run_when_morning_einspeisung_starts(
            self, mock_hass, mock_inverter, mock_coordinator, mock_provider,
        ):
            # Slot B aktiv, sunrise=07:30, morning_offset=0 → b_end=07:00
            # Morgen-Einspeisung beginnt 07:30 → mind. 30min Pause
            cfg = _make_config(
                enable_dual_discharge=True, enable_slot_a=False, enable_slot_b=True,
                discharge_b_start_time="03:00", discharge_b_end_cap="07:00",
                morning_start_offset=0,
            )
            opt = _make_optimizer(mock_hass, mock_inverter, mock_coordinator, mock_provider, config=cfg)
            # Snap zur Morgen-Einspeisungs-Zeit (07:30) — Slot B muss enden
            snap = _make_snapshot(
                now=datetime(2026, 12, 21, 7, 30, tzinfo=timezone.utc),
                battery_soc=80.0,
                sunrise=datetime(2026, 12, 21, 7, 30, tzinfo=timezone.utc),
                sunrise_today=datetime(2026, 12, 21, 7, 30, tzinfo=timezone.utc),
            )
            passed, reasons, blocked, hyst = opt._evaluate_slot_b(snap, 20.0)
            assert passed is False
            assert REASON_SLOT_B_WINDOW_EXPIRED in blocked


    class TestSolarEdgeRuntimeForce:
        def test_solaredge_init_forces_dual_to_false(
            self, mock_hass, mock_inverter, mock_coordinator, mock_provider,
        ):
            cfg = _make_config(
                inverter_type="solaredge_storedge",
                enable_dual_discharge=True,  # User-Setting (würde im Save-Path schon korrigiert)
                enable_slot_a=True, enable_slot_b=False,
            )
            opt = _make_optimizer(mock_hass, mock_inverter, mock_coordinator, mock_provider, config=cfg)
            assert opt._enable_dual_discharge is False  # Runtime-Erzwingung greift


    class TestEnableDualDischargeFalseLegacyPath:
        """Ensures byte-identical legacy behavior with enable_dual_discharge=False."""
        def test_legacy_path_taken_when_dual_disabled(
            self, mock_hass, mock_inverter, mock_coordinator, mock_provider,
        ):
            cfg = _make_config(enable_dual_discharge=False)
            opt = _make_optimizer(mock_hass, mock_inverter, mock_coordinator, mock_provider, config=cfg)
            snap = _make_snapshot(
                now=datetime(2026, 6, 15, 1, 30, tzinfo=timezone.utc),
                battery_soc=80.0,
                sunrise=datetime(2026, 6, 15, 4, 52, tzinfo=timezone.utc),
                sunrise_today=datetime(2026, 6, 14, 4, 52, tzinfo=timezone.utc),
                pv_tomorrow_kwh=20.0,
                consumption_tomorrow_daylight_kwh=10.0,
            )
            should, min_soc, reasons, blocked, hyst = opt._should_discharge(snap)
            # Dispatcher hat _evaluate_legacy_window aufgerufen — Legacy-Reasons-Set
            # KEIN slot_a_active oder slot_b_active darf erscheinen
            assert REASON_SLOT_A_ACTIVE not in reasons
            assert REASON_SLOT_B_ACTIVE not in reasons
    ```

    **Legacy-Pfad-Tests in tests/test_optimizer.py:** Bestehende `TestShouldDischarge*`-Klassen MÜSSEN unverändert grün bleiben. Wenn ein bestehender Test fehlschlägt, ist es Hinweis auf Refactor-Fehler — nicht der Test ist falsch.
  </action>
  <verify>
    <automated>pytest tests/test_dual_window.py::TestSlotAReserveLogic -x -q</automated>
    <automated>pytest tests/test_dual_window.py::TestSlotBLogic -x -q</automated>
    <automated>pytest tests/test_dual_window.py::TestProSlotHysteresis -x -q</automated>
    <automated>pytest tests/test_dual_window.py::TestSlotBPreSunriseCutoff -x -q</automated>
    <automated>pytest tests/test_dual_window.py::TestMutualExclusion -x -q</automated>
    <automated>pytest tests/test_dual_window.py::TestSolarEdgeRuntimeForce -x -q</automated>
    <automated>pytest tests/test_dual_window.py::TestEnableDualDischargeFalseLegacyPath -x -q</automated>
    <automated>pytest tests/test_optimizer.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def _check_common_guards" custom_components/eeg_energy_optimizer/optimizer.py` liefert genau einen Treffer
    - `grep -n "def _evaluate_slot_a" custom_components/eeg_energy_optimizer/optimizer.py` liefert genau einen Treffer
    - `grep -n "def _evaluate_slot_b" custom_components/eeg_energy_optimizer/optimizer.py` liefert genau einen Treffer
    - `grep -n "def _evaluate_legacy_window" custom_components/eeg_energy_optimizer/optimizer.py` liefert genau einen Treffer
    - `grep -n "def _should_discharge" custom_components/eeg_energy_optimizer/optimizer.py` liefert genau einen Treffer (nun Dispatcher)
    - `grep -n "self._slot_a_activated_date" custom_components/eeg_energy_optimizer/optimizer.py` liefert mindestens 4 Treffer (Init + 2× Reset + 2× Set)
    - `grep -n "self._slot_b_activated_date" custom_components/eeg_energy_optimizer/optimizer.py` liefert mindestens 4 Treffer
    - `grep -n "self._last_active_slot" custom_components/eeg_energy_optimizer/optimizer.py` liefert mindestens 3 Treffer
    - `grep -n "compute_b_window_end(" custom_components/eeg_energy_optimizer/optimizer.py` liefert mindestens einen Treffer (Aufruf in _evaluate_slot_b)
    - `grep -n "decision.discharge_active_slot = \"A\"" custom_components/eeg_energy_optimizer/optimizer.py` liefert genau einen Treffer
    - `grep -n "decision.discharge_active_slot = \"B\"" custom_components/eeg_energy_optimizer/optimizer.py` liefert genau einen Treffer
    - `grep -n "self._is_solaredge and self._enable_dual_discharge" custom_components/eeg_energy_optimizer/optimizer.py` liefert mindestens einen Treffer (Runtime-Force in __init__)
    - `pytest tests/test_dual_window.py::TestSlotAReserveLogic -x -q` exit-code 0
    - `pytest tests/test_dual_window.py::TestSlotBLogic -x -q` exit-code 0
    - `pytest tests/test_dual_window.py::TestProSlotHysteresis -x -q` exit-code 0
    - `pytest tests/test_dual_window.py::TestSlotBPreSunriseCutoff -x -q` exit-code 0 (7 parametrized Tests)
    - `pytest tests/test_dual_window.py::TestMutualExclusion -x -q` exit-code 0
    - `pytest tests/test_dual_window.py::TestSolarEdgeRuntimeForce -x -q` exit-code 0
    - `pytest tests/test_dual_window.py::TestEnableDualDischargeFalseLegacyPath -x -q` exit-code 0
    - `pytest tests/test_optimizer.py -x -q` exit-code 0 (KEINE Regression — D-05 byte-identische Legacy-Garantie)
  </acceptance_criteria>
  <done>
    Optimizer-Refactor abgeschlossen. _should_discharge ist Dispatcher; 3 neue Slot-Methoden + _check_common_guards. Pro-Slot-Hysterese-Felder + Reset-Logik im _evaluate. Decision.discharge_active_slot wird gesetzt. SolarEdge-Runtime-Force greift. Alle Slot-A/B-Tests grün, alle Hysterese-Tests grün, Mutex-Tests grün, Legacy-Pfad-Tests grün ohne Regression.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PeakShare-Cache-Schema-Migration zu dict[a/b] + 24h-Decision-Sequenz-Integration-Test</name>
  <files>
    custom_components/eeg_energy_optimizer/peakshare.py,
    tests/test_dual_window.py
  </files>
  <read_first>
    custom_components/eeg_energy_optimizer/peakshare.py (Zeilen 1-50 für Imports, Zeilen 130-200 für __init__ + async_load + async_fetch, Zeilen 295-410 KOMPLETT für get_discharge_plan),
    custom_components/eeg_energy_optimizer/optimizer.py (Zeilen 740-770 für _discharge_detail_status mit _discharge_plan-Zugriff, Zeilen 1130-1145 für _evaluate-_discharge_plan-Lookup),
    .planning/milestones/v1.2-phases/11-dual-window-discharge/11-PATTERNS.md (Sektion "peakshare.py Cache-Schema dict[a/b]"),
    .planning/milestones/v1.2-phases/11-dual-window-discharge/11-RESEARCH.md (Sektion "PeakShare-Cache-Schema-Migration"),
    tests/conftest.py (Helper-Imports prüfen)
  </read_first>
  <behavior>
    - PeakShareProvider.__init__ initialisiert _discharge_plan als dict {"a": None, "b": None}
    - get_discharge_plan akzeptiert slot-Parameter (default "a") + optionale window_start/window_end Overrides
    - Cache-Lookup nutzt self._discharge_plan.get(slot)
    - Plan-Lock setzt self._discharge_plan[slot] = plan
    - async_load: alte tuple-Form wird verworfen (force-recompute beim nächsten Cycle); neue dict-Form wird geladen falls vorhanden
    - async_fetch invalidiert Cache: self._discharge_plan = {"a": None, "b": None}
    - Optimizer-Code-Pfade die _discharge_plan lesen (optimizer.py:_discharge_detail_status, optimizer.py:_evaluate) lesen via dict.get(slot) statt direkt
    - 24h-Simulationstest: A-only / B-only / Dual produzieren erwartete Decision-Sequenzen mit korrekten Slot-Markern
  </behavior>
  <action>
    **A. peakshare.py — _discharge_plan-Schema-Migration:**

    1) `__init__` (Zeile ~171):
    ```python
    # Phase 11: Dual-Window — slot-indizierter Cache
    self._discharge_plan: dict[str, tuple[datetime, datetime] | None] = {
        "a": None, "b": None,
    }
    self._discharge_plan_date: str | None = None  # Lock pro Tag, gemeinsam für a/b
    ```

    2) `async_load` (Zeile ~174-195) — alte tuple-Form verwerfen:
    Direkt nach dem `try:`-Block, wo `stored = await self._store.async_load()` gelesen wird, am Ende des `if stored and isinstance(stored, dict):`-Blocks ergänzen:
    ```python
    # Phase 11: Cache-Schema-Migration — alte tuple-Form (single discharge_plan)
    # wird verworfen, nächster Cycle berechnet neu.
    self._discharge_plan = {"a": None, "b": None}
    self._discharge_plan_date = None
    ```

    3) `async_fetch` Cache-Invalidate (suche nach `self._discharge_plan = None`-Zuweisung, ~Zeile 236-237):
    ```python
    # Phase 11: dict-Schema
    self._discharge_plan = {"a": None, "b": None}
    self._discharge_plan_date = None
    ```

    4) `get_discharge_plan` Signatur erweitern + Lookup/Lock anpassen:

    Aktuelle Signatur (gemäß peakshare.py:295+):
    ```python
    def get_discharge_plan(
        self, community, available_kwh, discharge_power_kw, sunset_time, now,
        discharge_start_lower_bound=None, next_sunrise=None,
    ) -> tuple[datetime, datetime] | None:
    ```

    Erweitern:
    ```python
    def get_discharge_plan(
        self,
        community,
        available_kwh,
        discharge_power_kw,
        sunset_time,
        now,
        discharge_start_lower_bound=None,
        next_sunrise=None,
        *,
        slot: str = "a",  # Phase 11: "a" oder "b"
        window_start: datetime | None = None,  # Phase 11: optional override
        window_end: datetime | None = None,    # Phase 11: optional override
    ) -> tuple[datetime, datetime] | None:
    ```

    Cache-Lookup-Block (Zeile ~320-325) anpassen:
    ```python
    today_str = now.strftime("%Y-%m-%d")
    if self._discharge_plan_date == today_str:
        return self._discharge_plan.get(slot)
    ```

    Window-Resolution (Zeile ~349-379) — wenn window_start/window_end übergeben sind, nutze sie statt sunset/hard_cutoff:
    ```python
    # Phase 11: Slot-spezifische Window-Overrides
    if window_start is None:
        window_start = sunset_time
        if discharge_start_lower_bound is not None:
            window_start = max(window_start, discharge_start_lower_bound)
    if window_end is None:
        from .optimizer import compute_hard_cutoff
        next_day = now.replace(
            hour=0, minute=0, second=0, microsecond=0
        ) + timedelta(days=1)
        window_end = compute_hard_cutoff(next_day, next_sunrise)
    ```

    Plan-Lock-Block (Zeile ~382-393) anpassen:
    ```python
    # Phase 11: Slot-spezifischer Lock
    self._discharge_plan[slot] = plan
    self._discharge_plan_date = today_str
    ```

    **WICHTIG:** Edge-Case-Pfade (window_start >= window_end → return None) müssen den Slot-spezifischen Lock auch setzen:
    ```python
    if window_start >= window_end:
        _LOGGER.info(
            "PeakShare: Untergrenze %s liegt nach Cutoff %s — kein Fenster",
            window_start.strftime("%H:%M"), window_end.strftime("%H:%M"),
        )
        self._discharge_plan[slot] = None
        self._discharge_plan_date = today_str
        return None
    ```

    **B. optimizer.py — _discharge_plan-Zugriffe anpassen:**

    Suche `getattr(self._peakshare, "_discharge_plan", None)` (es gibt mehrere Stellen, ~Zeile 743 in _discharge_detail_status und ~Zeile 1135 in _evaluate). An JEDER Stelle anpassen auf:
    ```python
    plan_dict = getattr(self._peakshare, "_discharge_plan", None) or {}
    plan_a = plan_dict.get("a") if isinstance(plan_dict, dict) else None
    plan_b = plan_dict.get("b") if isinstance(plan_dict, dict) else None
    ```

    Im Legacy-Pfad (`_evaluate_legacy_window`) wird `plan_a` (Slot-A-Default) genutzt. In Dual-Mode-Pfaden (`_evaluate_slot_a` / `_evaluate_slot_b`) gibt es heute KEINEN Aufruf zu PeakShare — Plan 11-02 verzichtet darauf, PeakShare-Slot-Aufrufe in Slot-A/B einzuführen (das ist eine Optimierung für später; statisches Window aus Konfig genügt für Slot-A/B Phase 11).

    **WICHTIG:** Legacy-Pfad nutzt heute eine `_discharge_plan` als single tuple via `peakshare.get_discharge_plan(...)`. Mit dem neuen dict-Schema ruft Legacy weiterhin ohne `slot=`-Parameter auf → default `slot="a"` → Cache liegt unter Key "a". Das ist konsistent.

    **C. tests/test_dual_window.py — neue Test-Klassen:**

    ```python
    class TestPeakShareCacheSchema:
        def test_init_creates_dict_schema(self):
            from custom_components.eeg_energy_optimizer.peakshare import (
                PeakShareProvider,
            )
            ps = PeakShareProvider(MagicMock(), entry_id="test", community="BEG")
            assert ps._discharge_plan == {"a": None, "b": None}

        def test_get_discharge_plan_default_slot_is_a(self):
            from custom_components.eeg_energy_optimizer.peakshare import (
                PeakShareProvider,
            )
            ps = PeakShareProvider(MagicMock(), entry_id="test", community="BEG")
            ps._discharge_plan_date = "2026-06-15"
            ps._discharge_plan = {
                "a": (datetime(2026, 6, 15, 22, 0, tzinfo=timezone.utc),
                      datetime(2026, 6, 15, 23, 0, tzinfo=timezone.utc)),
                "b": None,
            }
            now = datetime(2026, 6, 15, 23, 30, tzinfo=timezone.utc)
            # default slot "a"
            result = ps.get_discharge_plan(
                community="BEG", available_kwh=5.0,
                discharge_power_kw=5.0, sunset_time=None, now=now,
            )
            assert result is not None
            assert result[0].hour == 22

        def test_get_discharge_plan_slot_b_returns_b_cache(self):
            from custom_components.eeg_energy_optimizer.peakshare import (
                PeakShareProvider,
            )
            ps = PeakShareProvider(MagicMock(), entry_id="test", community="BEG")
            ps._discharge_plan_date = "2026-06-15"
            ps._discharge_plan = {
                "a": None,
                "b": (datetime(2026, 6, 16, 5, 0, tzinfo=timezone.utc),
                      datetime(2026, 6, 16, 6, 30, tzinfo=timezone.utc)),
            }
            now = datetime(2026, 6, 15, 23, 30, tzinfo=timezone.utc)
            result = ps.get_discharge_plan(
                community="BEG", available_kwh=5.0,
                discharge_power_kw=5.0, sunset_time=None, now=now,
                slot="b",
            )
            assert result is not None
            assert result[0].hour == 5


    class TestDualWindow24hSimulation:
        """24h-Simulationslauf: Decision-Sequenz über einen vollen Tag.

        SPEC §8: A-only / B-only / Dual liefern erwartete Decision-Sequenzen.
        """
        def _simulate(self, opt, snap_factory, hours):
            """Run optimizer for sequence of hours, return slot markers."""
            results = []
            for hour in hours:
                snap = snap_factory(hour)
                # _evaluate ist async — wir nutzen direkt _should_discharge
                # für deterministische Pure-Logic-Tests
                should, min_soc, reasons, blocked, hyst = opt._should_discharge(snap)
                slot = None
                if REASON_SLOT_A_ACTIVE in reasons:
                    slot = "A"
                elif REASON_SLOT_B_ACTIVE in reasons:
                    slot = "B"
                results.append((hour, should, slot))
            return results

        def test_dual_a_and_b_both_activate(
            self, mock_hass, mock_inverter, mock_coordinator, mock_provider,
        ):
            cfg = _make_config(
                enable_dual_discharge=True,
                enable_slot_a=True, enable_slot_b=True,
                discharge_a_start_time="20:00",
                discharge_b_start_time="03:00",
                discharge_b_end_cap="07:00",
                discharge_a_reserve_pct=15,
                min_soc=20,
            )
            opt = _make_optimizer(mock_hass, mock_inverter, mock_coordinator, mock_provider, config=cfg)
            sunrise_today = datetime(2026, 12, 21, 7, 30, tzinfo=timezone.utc)
            sunrise_tomorrow = datetime(2026, 12, 22, 7, 30, tzinfo=timezone.utc)
            def snap_factory(hour):
                # Vereinfacht: hour mod 24, Tag 21 vor Mitternacht, Tag 22 ab 00:00
                day = 21 if hour < 24 else 22
                hour_norm = hour % 24
                return _make_snapshot(
                    now=datetime(2026, 12, day, hour_norm, 0, tzinfo=timezone.utc),
                    battery_soc=80.0,
                    sunrise=sunrise_tomorrow if hour_norm >= 8 else sunrise_today,
                    sunrise_today=sunrise_today,
                    pv_tomorrow_kwh=20.0,
                    consumption_tomorrow_daylight_kwh=10.0,
                )
            # Stunden 18..30 (=06:00 next day)
            seq = self._simulate(opt, snap_factory, range(18, 30))
            slots = [s for _, _, s in seq if s is not None]
            assert "A" in slots, f"Slot A erwartet aktiv zwischen 20:00-02:55: {seq}"
            assert "B" in slots, f"Slot B erwartet aktiv zwischen 03:00-07:00: {seq}"

        def test_a_only_pure_evening_discharge(
            self, mock_hass, mock_inverter, mock_coordinator, mock_provider,
        ):
            cfg = _make_config(
                enable_dual_discharge=True,
                enable_slot_a=True, enable_slot_b=False,
                discharge_a_start_time="20:00",
                discharge_a_reserve_pct=15,
                min_soc=20,
            )
            opt = _make_optimizer(mock_hass, mock_inverter, mock_coordinator, mock_provider, config=cfg)
            sunrise_today = datetime(2026, 12, 21, 7, 30, tzinfo=timezone.utc)
            sunrise_tomorrow = datetime(2026, 12, 22, 7, 30, tzinfo=timezone.utc)
            def snap_factory(hour):
                day = 21 if hour < 24 else 22
                hour_norm = hour % 24
                return _make_snapshot(
                    now=datetime(2026, 12, day, hour_norm, 0, tzinfo=timezone.utc),
                    battery_soc=80.0,
                    sunrise=sunrise_tomorrow if hour_norm >= 8 else sunrise_today,
                    sunrise_today=sunrise_today,
                    pv_tomorrow_kwh=20.0,
                    consumption_tomorrow_daylight_kwh=10.0,
                )
            seq = self._simulate(opt, snap_factory, range(18, 30))
            slots = [s for _, _, s in seq if s is not None]
            assert "A" in slots
            assert "B" not in slots

        def test_b_only_pure_morning_discharge(
            self, mock_hass, mock_inverter, mock_coordinator, mock_provider,
        ):
            cfg = _make_config(
                enable_dual_discharge=True,
                enable_slot_a=False, enable_slot_b=True,
                discharge_b_start_time="03:00",
                discharge_b_end_cap="07:00",
                min_soc=20,
            )
            opt = _make_optimizer(mock_hass, mock_inverter, mock_coordinator, mock_provider, config=cfg)
            sunrise_today = datetime(2026, 12, 21, 7, 30, tzinfo=timezone.utc)
            sunrise_tomorrow = datetime(2026, 12, 22, 7, 30, tzinfo=timezone.utc)
            def snap_factory(hour):
                day = 21 if hour < 24 else 22
                hour_norm = hour % 24
                return _make_snapshot(
                    now=datetime(2026, 12, day, hour_norm, 0, tzinfo=timezone.utc),
                    battery_soc=80.0,
                    sunrise=sunrise_tomorrow if hour_norm >= 8 else sunrise_today,
                    sunrise_today=sunrise_today,
                    pv_tomorrow_kwh=20.0,
                    consumption_tomorrow_daylight_kwh=10.0,
                )
            seq = self._simulate(opt, snap_factory, range(18, 30))
            slots = [s for _, _, s in seq if s is not None]
            assert "B" in slots
            assert "A" not in slots
    ```
  </action>
  <verify>
    <automated>pytest tests/test_dual_window.py::TestPeakShareCacheSchema -x -q</automated>
    <automated>pytest tests/test_dual_window.py::TestDualWindow24hSimulation -x -q</automated>
    <automated>pytest tests/test_dual_window.py -x -q</automated>
    <automated>pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'self._discharge_plan: dict' custom_components/eeg_energy_optimizer/peakshare.py` liefert genau einen Treffer
    - `grep -n '"a": None, "b": None' custom_components/eeg_energy_optimizer/peakshare.py` liefert mindestens 2 Treffer (Init + async_fetch)
    - `grep -n 'slot: str = "a"' custom_components/eeg_energy_optimizer/peakshare.py` liefert genau einen Treffer (get_discharge_plan-Signatur)
    - `grep -n 'self._discharge_plan\[slot\]' custom_components/eeg_energy_optimizer/peakshare.py` liefert mindestens 2 Treffer (Lock-Stellen)
    - `grep -n 'self._discharge_plan.get(slot)' custom_components/eeg_energy_optimizer/peakshare.py` liefert genau einen Treffer (Cache-Lookup)
    - `grep -n 'plan_dict.get("a")' custom_components/eeg_energy_optimizer/optimizer.py` liefert mindestens einen Treffer (Optimizer-Zugriff)
    - `pytest tests/test_dual_window.py::TestPeakShareCacheSchema -x -q` exit-code 0
    - `pytest tests/test_dual_window.py::TestDualWindow24hSimulation -x -q` exit-code 0
    - `pytest tests/test_dual_window.py -x -q` exit-code 0 (alle 11+ Klassen grün)
    - `pytest tests/ -q` exit-code 0 (volle Suite grün, keine Regression in test_optimizer.py / test_peakshare.py / test_telemetry_*.py)
  </acceptance_criteria>
  <done>
    PeakShare-Cache ist dict[a/b]-Schema, get_discharge_plan akzeptiert slot-Parameter, Optimizer-Zugriffe sind angepasst, alte tuple-Form wird sauber migriert (verworfen). 24h-Simulationstest deckt A-only / B-only / Dual-Szenarien ab. Volle Test-Suite grün.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Snapshot → Slot-Decision | Snapshot ist trusted Input; Slot-Decision-Funktionen sind pure-fns ohne Side-Effects |
| Decision-Engine → Hysterese-State | _slot_*_activated_date / _last_active_slot sind in-memory; Reset-Trigger müssen idempotent sein |
| PeakShare-Cache → Storage | Persistente Cache-Form muss schema-stabil sein, Migration verwirft alte Form |
| Decision → Inverter | Mutex-Garantie: nur ein State pro Cycle (block ODER discharge ODER normal) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-01 | Tampering | Hysterese-State (Slot-Reaktivierungs-Aufschlag wird umgangen) | mitigate | Reset-Pattern strikt am _discharge_activated_date orientiert: Reset NUR nach today's Sunrise — verhindert Mitternachts-Reset, der Hysterese aushebeln würde. Test `test_a_reactivation_requires_min_soc_plus_5` validiert. |
| T-11-02-02 | Tampering | SolarEdge-Bypass (Config sagt dual=True, Runtime nicht synchronisiert) | mitigate | Defense-in-depth: __init__-Force `if self._is_solaredge: self._enable_dual_discharge = False` — Dispatcher fällt auf Legacy-Pfad. Test `test_solaredge_init_forces_dual_to_false`. Ergänzend in 11-03 Save-Path. |
| T-11-02-03 | Tampering | PeakShare-Cache-Schema-Drift (alte tuple-Form trifft auf neuen dict-Code) | mitigate | async_load verwirft alte Form explizit; nächster Cycle berechnet neu. Test `test_init_creates_dict_schema`. |
| T-11-02-04 | DoS | Slot-A/B blockiert sich gegenseitig durch fehlerhafte 5min-Pause-Math | mitigate | _evaluate_slot_a setzt explizit a_end_cap = b_start - 5min; compute_b_window_end zieht 5min für Slot B ab. Test `test_a_ends_5min_before_b_start`. |
| T-11-02-05 | Tampering | Decision.discharge_active_slot wird nicht gesetzt trotz aktivem Slot | mitigate | _evaluate setzt das Feld basierend auf REASON_SLOT_A/B_ACTIVE in reasons-Liste. Test `test_dual_a_and_b_both_activate` prüft korrekten Slot-Marker. |
| T-11-02-06 | Information Disclosure | reasons-Liste leakt interne Slot-State-Info | accept | reasons-Keys sind designed für Telemetrie und werden in Backend-Repo bereits explizit gesammelt (Phase 8). Slot-State ist nicht-sensitiv. |

Severity: alle LOW-MEDIUM. Größtes Risiko ist T-11-02-01 (Hysterese-Bypass) — durch Reset-Pattern abgefangen.
</threat_model>

<verification>
**Automated:**
```bash
pytest tests/test_dual_window.py -x -q
pytest tests/test_optimizer.py -x -q  # KEINE Regression
pytest tests/ -q  # Volle Suite
```

**Pattern checks:**
```bash
grep -c "def _evaluate_slot_" custom_components/eeg_energy_optimizer/optimizer.py  # 2
grep -c "def _check_common_guards" custom_components/eeg_energy_optimizer/optimizer.py  # 1
grep -c "def _evaluate_legacy_window" custom_components/eeg_energy_optimizer/optimizer.py  # 1
grep -c "self._slot_a_activated_date\|self._slot_b_activated_date" custom_components/eeg_energy_optimizer/optimizer.py  # mind. 8
```
</verification>

<success_criteria>
- _should_discharge ist Dispatcher (3 Pfade: Legacy / Slot-A / Slot-B)
- _evaluate_legacy_window ist 1:1 alter _should_discharge-Body — D-05-Garantie hält
- 4 neue Methoden in optimizer.py: _check_common_guards, _evaluate_slot_a, _evaluate_slot_b, _evaluate_legacy_window
- Pro-Slot-Hysterese-Felder + Reset-Logik im _evaluate
- Decision.discharge_active_slot wird korrekt gesetzt
- SolarEdge-Runtime-Force in __init__ greift
- PeakShare-Cache ist dict[a/b]-Schema
- Optimizer-Zugriffe auf _discharge_plan sind angepasst
- 24h-Simulationstest grün für A-only / B-only / Dual
- Volle Test-Suite grün ohne Regression
</success_criteria>

<output>
Nach Abschluss: erstelle `.planning/phases/11-dual-window-discharge/11-02-SUMMARY.md` mit:
- Refactor-Struktur: Dispatcher + 4 Methoden, Body-Größen
- Pro-Slot-Hysterese-Felder + Reset-Trigger
- SolarEdge-Runtime-Force-Stelle
- PeakShare-Cache-Schema-Migration (Init/async_load/async_fetch/get_discharge_plan)
- 24h-Simulationstest-Ergebnisse (A/B/Dual Slot-Sequenzen)
- Test-Status (alle grün, keine Regression in test_optimizer.py)
- Hinweis: 11-03 baut auf der Decision-Engine auf (Panel + Save-Path); 11-04 ergänzt Markdown-Renderer
</output>
