---
phase: 11
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - custom_components/eeg_energy_optimizer/const.py
  - custom_components/eeg_energy_optimizer/optimizer.py
  - custom_components/eeg_energy_optimizer/__init__.py
  - custom_components/eeg_energy_optimizer/config_flow.py
  - tests/conftest.py
  - tests/test_dual_window.py
  - tests/test_config_flow.py
autonomous: true
requirements: [SPEC-1, SPEC-3, SPEC-5, SPEC-7]
tags: [dual-window, migration, reasons-catalog, config-schema]

must_haves:
  truths:
    - "const.py exportiert die 7 neuen CONF_*-Keys und 6 neuen DEFAULT_*-Werte für Dual-Window"
    - "compute_b_window_end liefert die 4 SPEC-Test-Cases korrekt (Sommer/Winter/Übergang/tiefer Winter)"
    - "Migration v14→v15 setzt nicht-SolarEdge-Bestands-Entries auf enable_dual_discharge=True"
    - "Migration v14→v15 setzt SolarEdge-Bestands-Entries auf enable_dual_discharge=False, enable_slot_a=True, enable_slot_b=False"
    - "config_flow.py deklariert VERSION = 15"
    - "Decision-Dataclass besitzt das Feld discharge_active_slot mit Default None"
    - "ALL_REASONS enthält die 8 neuen Slot-Reasons; REASON_LABELS_DE übersetzt alle 8 ins Deutsche"
    - "tests/conftest.py exportiert die Helper _make_config / _make_snapshot / _make_optimizer als Modul-Level-Funktionen"
  artifacts:
    - path: custom_components/eeg_energy_optimizer/const.py
      provides: "7 CONF_*-Konstanten + 6 DEFAULT_*-Werte für Dual-Window + erweiterte TELEMETRY_SETTINGS_KEYS"
      contains: "CONF_ENABLE_DUAL_DISCHARGE"
    - path: custom_components/eeg_energy_optimizer/optimizer.py
      provides: "compute_b_window_end-Funktion + 8 neue REASON_*-Konstanten + REASON_LABELS_DE-Erweiterung + Decision.discharge_active_slot"
      contains: "def compute_b_window_end("
    - path: custom_components/eeg_energy_optimizer/__init__.py
      provides: "Migration v14→v15 mit slot-spezifischen Defaults und SolarEdge-Sonderfall"
      contains: "if entry.version < 15:"
    - path: custom_components/eeg_energy_optimizer/config_flow.py
      provides: "VERSION = 15"
      contains: "VERSION = 15"
    - path: tests/conftest.py
      provides: "Modul-Level-Helpers _make_config, _make_snapshot, _make_optimizer (extrahiert aus tests/test_optimizer.py)"
      contains: "def _make_config"
    - path: tests/test_dual_window.py
      provides: "TestComputeBWindowEnd-Klasse mit 7 Test-Methoden + TestMigrationV14ToV15-Klasse mit 4 Tests + TestReasonsCatalog-Klasse"
      contains: "class TestComputeBWindowEnd"
    - path: tests/test_config_flow.py
      provides: "TestMigrationV14ToV15-Tests (4 Tests) für Defaults, SolarEdge-XOR, idempotenz"
      contains: "test_migration_v14_to_v15"
  key_links:
    - from: "custom_components/eeg_energy_optimizer/__init__.py:async_migrate_entry"
      to: "config_flow.py:VERSION = 15"
      via: "version-bump im async_update_entry-Aufruf muss synchron sein"
      pattern: "version=15"
    - from: "custom_components/eeg_energy_optimizer/optimizer.py:ALL_REASONS"
      to: "Decision.reasons + Decision.blocked_by"
      via: "frozenset-Subset-Check im Test"
      pattern: "set\\(reasons\\)\\.issubset\\(ALL_REASONS\\)"
    - from: "custom_components/eeg_energy_optimizer/optimizer.py:compute_b_window_end"
      to: "Plan 11-02 _evaluate_slot_b"
      via: "module-level Funktion, importierbar wie compute_hard_cutoff"
      pattern: "from .optimizer import compute_b_window_end"
---

<objective>
Phase 11 Wave 1: Datenstruktur, Migration und gemeinsame Hilfsfunktionen.

Dieser Plan legt das fundamentale Skelett, an das alle nachfolgenden Plans (11-02 Optimizer-Refactor, 11-03 Panel-UI, 11-04 Markdown/Activity-Log) andocken. Es werden ausschließlich additive Änderungen durchgeführt — keine bestehenden Code-Pfade werden umgebaut. Der Plan setzt zudem die Test-Infrastruktur (Helper-Extraktion in conftest.py + neue tests/test_dual_window.py) für alle Folge-Plans bereit.

Adressiert SPEC-Requirements:
- §1 Konfigurations-Schema für Dual-Window (const.py + Migration v14→v15)
- §3 Slot B — Adaptives Ende vor Sonnenaufgang (compute_b_window_end mit 4 Test-Cases)
- §5 Mutual Exclusion zur Morgen-Einspeisung (compute_b_window_end zieht 5min ab)
- §7 Telemetry-Reasons pro Slot (8 neue Keys in ALL_REASONS + REASON_LABELS_DE)

Adressiert User-Decisions:
- D-03 Migration v14→v15 (KORRIGIERT von 12→13 in SPEC; Ist-Version laut config_flow.py:24 ist 14)
- D-04 Default-Wechsel intendiert (CHANGELOG-Eintrag in 11-04)
- D-09 Reasons additiv, snake_case, ALL_REASONS-Closed-Set bleibt deterministisch
- D-10 Decision.discharge_active_slot als Literal["A", "B"] | None (mit None-Default)

Purpose: Stelle alle Datenstruktur- und Helper-Voraussetzungen bereit, sodass Plan 11-02 sofort gegen sie programmieren kann (Imports + Tests existieren).
Output: 5 modifizierte Module, 1 neue Test-Datei, 1 erweiterte conftest.py, 4 grüne Migrations-Tests, 7+ grüne compute_b_window_end-Tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/milestones/v1.2-phases/11-dual-window-discharge/11-SPEC.md
@.planning/milestones/v1.2-phases/11-dual-window-discharge/11-CONTEXT.md
@.planning/milestones/v1.2-phases/11-dual-window-discharge/11-RESEARCH.md
@.planning/milestones/v1.2-phases/11-dual-window-discharge/11-PATTERNS.md
@.planning/milestones/v1.2-phases/11-dual-window-discharge/11-VALIDATION.md

<interfaces>
<!-- Bestehende Patterns/Signaturen, die dieser Plan exakt nachbildet (extrahiert aus Code) -->

Aus custom_components/eeg_energy_optimizer/optimizer.py:175-208 (compute_hard_cutoff — Vorlage für compute_b_window_end):
```python
def compute_hard_cutoff(now: datetime, next_sunrise: datetime | None) -> datetime:
    """Berechne den dynamischen Hard-Cutoff für die Abend-Entladung."""
    if next_sunrise is None:
        anchor = now if now.hour < 12 else now + timedelta(days=1)
        return anchor.replace(hour=4, minute=0, second=0, microsecond=0)
    fixed_at_sunrise_day = next_sunrise.replace(hour=4, minute=0, second=0, microsecond=0)
    pre_sunrise = next_sunrise - timedelta(hours=1)
    return min(fixed_at_sunrise_day, pre_sunrise)
```

Aus custom_components/eeg_energy_optimizer/optimizer.py:79-92 (Reasons-Block-Pattern):
```python
# Abend-Entladung
REASON_NIGHT_DISCHARGE_DISABLED = "night_discharge_disabled"
REASON_OVERNIGHT_DEMAND_TOO_HIGH = "overnight_demand_too_high"
... weitere snake_case Konstanten ...
```

Aus custom_components/eeg_energy_optimizer/__init__.py:831-844 (Migration-Pattern v13→v14):
```python
if entry.version < 14:
    # v14 — Abend-Entladestart auf 01:00 vereinheitlichen.
    # [Multiline-Begründung]
    new_data = {**entry.data}
    new_data["discharge_start_time"] = "01:00"
    hass.config_entries.async_update_entry(entry, data=new_data, version=14)
```

Aus custom_components/eeg_energy_optimizer/optimizer.py:247-280 (Decision-Dataclass):
```python
@dataclass
class Decision:
    timestamp: str = ""
    zustand: str = "Normal"
    ...
    reasons: list[str] = field(default_factory=list)
    blocked_by: list[str] = field(default_factory=list)
    snapshot: dict = field(default_factory=dict)
```

Aus tests/test_optimizer.py:63-106 (Helper-Pattern, zu extrahieren nach conftest.py):
```python
def _make_config(**overrides):
    base = {
        CONF_BATTERY_SOC_SENSOR: "sensor.battery_soc",
        CONF_BATTERY_CAPACITY_SENSOR: "",
        CONF_BATTERY_CAPACITY_KWH: 10.0,
        CONF_DISCHARGE_START_TIME: "20:00",
    }
    base.update(overrides)
    return base
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: const.py + compute_b_window_end + Reasons-Catalog + Decision-Field</name>
  <files>
    custom_components/eeg_energy_optimizer/const.py,
    custom_components/eeg_energy_optimizer/optimizer.py
  </files>
  <read_first>
    custom_components/eeg_energy_optimizer/const.py (komplett, um Konstanten-Block-Pattern und TELEMETRY_SETTINGS_KEYS-Position zu sehen),
    custom_components/eeg_energy_optimizer/optimizer.py (Zeilen 1-300 für Imports, Reasons-Block 63-142, compute_hard_cutoff 175-208, Decision-Dataclass 247-298),
    .planning/milestones/v1.2-phases/11-dual-window-discharge/11-PATTERNS.md (Sektionen "const.py", "compute_b_window_end", "Reasons-Catalog", "Decision.discharge_active_slot"),
    .planning/milestones/v1.2-phases/11-dual-window-discharge/11-RESEARCH.md (Sektion "Migration & Versioning" + "Slot-Window-Mathematik" + "Telemetry-Reasons")
  </read_first>
  <behavior>
    - compute_b_window_end(now, sunrise, b_end_cap, morning_offset_h) gibt None zurück wenn sunrise None ist
    - Sommer-Test: sunrise=2026-06-21 04:52, cap="07:00", offset=0 → ergibt sunrise−5min = 04:47
    - Winter-Test: sunrise=2026-12-21 07:30, cap="07:00", offset=0 → ergibt cap = 07:00 (cap am Sunrise-Tag)
    - Übergang-Test: sunrise=2026-04-15 06:00, cap="07:00", offset=0 → ergibt sunrise−5min = 05:55
    - Tiefer-Winter-Test: sunrise=2026-01-15 08:30, cap="07:00", offset=0 → ergibt cap = 07:00
    - morning_offset=1 Test: sunrise=2026-12-21 07:30, cap="07:00", offset=1 → ergibt sunrise−1h−5min = 06:25
    - ALL_REASONS-Frozenset enthält alle 8 neuen Slot-Reason-Keys nach Erweiterung
    - REASON_LABELS_DE.get(REASON_SLOT_A_ACTIVE) liefert deutschen Text mit echten Umlauten falls relevant
    - Decision()-Konstruktor ohne Argumente liefert decision.discharge_active_slot is None
  </behavior>
  <action>
    **A. const.py — additiv direkt nach Phase-3-Block (nach Zeile 105 DEFAULT_SAFETY_BUFFER_PCT):**

    Füge ein:
    ```python
    # Phase 11: Dual-Window-Entladung
    CONF_ENABLE_DUAL_DISCHARGE = "enable_dual_discharge"
    CONF_ENABLE_SLOT_A = "enable_slot_a"
    CONF_ENABLE_SLOT_B = "enable_slot_b"
    CONF_DISCHARGE_A_START_TIME = "discharge_a_start_time"
    CONF_DISCHARGE_B_START_TIME = "discharge_b_start_time"
    CONF_DISCHARGE_B_END_CAP = "discharge_b_end_cap"
    CONF_DISCHARGE_A_RESERVE_PCT = "discharge_a_reserve_pct"

    DEFAULT_ENABLE_DUAL_DISCHARGE_NON_SOLAREDGE = True
    DEFAULT_ENABLE_DUAL_DISCHARGE_SOLAREDGE = False
    DEFAULT_DISCHARGE_A_START_TIME = "20:00"
    DEFAULT_DISCHARGE_B_START_TIME = "03:00"
    DEFAULT_DISCHARGE_B_END_CAP = "07:00"
    DEFAULT_DISCHARGE_A_RESERVE_PCT = 15
    ```

    **CONF_DISCHARGE_START_TIME und DEFAULT_DISCHARGE_START_TIME = "01:00" NICHT entfernen** — sie sind Legacy-Anker für `enable_dual_discharge=False` (D-05).

    **TELEMETRY_SETTINGS_KEYS in const.py:166-178** erweitern (neue Keys am Ende anhängen, bestehende NICHT umsortieren):
    ```python
    TELEMETRY_SETTINGS_KEYS = (
        "enable_morning_delay",
        "enable_night_discharge",
        "enable_peakshare",
        "morning_start_offset",
        "morning_end_time",
        "discharge_start_time",
        "discharge_power_kw",
        "min_soc",
        "safety_buffer_pct",
        "peakshare_community",
        "forecast_source",
        # Phase 11: Dual-Window
        "enable_dual_discharge",
        "enable_slot_a",
        "enable_slot_b",
        "discharge_a_start_time",
        "discharge_b_start_time",
        "discharge_b_end_cap",
        "discharge_a_reserve_pct",
    )
    ```

    **B. optimizer.py — compute_b_window_end direkt nach compute_hard_cutoff (nach Zeile 208):**

    ```python
    def compute_b_window_end(
        now: datetime,
        sunrise: datetime | None,
        b_end_cap: str,
        morning_offset_h: float,
    ) -> datetime | None:
        """Berechne das effektive Slot-B-Ende.

        Liefert das strikteste Minimum aus:
          - b_end_cap an Sunrise-Tag verankert (z.B. "07:00")
          - sunrise − morning_offset_h − 5min (Pause vor Morgen-Einspeisung)
          - sunrise − 5min (Pause vor Sunrise selbst)

        Garantiert ≥5min Pause vor Beginn der Morgen-Einspeisung
        (sunrise − morning_offset_h) — Slot B und Morgen-Einspeisung
        laufen niemals parallel (SPEC §5).

        Wenn sunrise unbekannt: None — Slot B kann ohne Sunrise nicht laufen.

        Beispiele:
          Sommer SA 04:52, cap 07:00, offset 0 → 04:47 (sunrise−5min dominiert)
          Winter SA 07:30, cap 07:00, offset 0 → 07:00 (cap dominiert)
          Winter SA 07:30, cap 07:00, offset 1 → 06:25 (pre-morning dominiert)
        """
        if sunrise is None:
            return None
        cap_h, cap_m = (int(p) for p in b_end_cap.split(":"))
        cap_at_sunrise_day = sunrise.replace(
            hour=cap_h, minute=cap_m, second=0, microsecond=0
        )
        pre_morning_einspeisung = sunrise - timedelta(
            hours=morning_offset_h, minutes=5
        )
        pre_sunrise = sunrise - timedelta(minutes=5)
        return min(cap_at_sunrise_day, pre_morning_einspeisung, pre_sunrise)
    ```

    **C. optimizer.py — 8 neue REASON_*-Konstanten direkt nach Zeile 92 (REASON_BATTERY_SOC_UNAVAILABLE), VOR ALL_REASONS:**

    ```python
    # Phase 11: Dual-Window-Entladung
    REASON_BEFORE_SLOT_A = "before_slot_a"
    REASON_SLOT_A_ACTIVE = "slot_a_active"
    REASON_SLOT_A_RESERVE_REACHED = "slot_a_reserve_reached"
    REASON_BETWEEN_SLOTS = "between_slots"
    REASON_BEFORE_SLOT_B = "before_slot_b"
    REASON_SLOT_B_ACTIVE = "slot_b_active"
    REASON_SLOT_B_WINDOW_EXPIRED = "slot_b_window_expired"
    REASON_SLOT_B_PRE_SUNRISE_CUTOFF = "slot_b_pre_sunrise_cutoff"
    ```

    **D. optimizer.py — ALL_REASONS-Frozenset (Zeile 95-117) am Ende erweitern:**

    Füge die 8 neuen Konstanten am Ende des Frozenset-Literals ein, vor der schließenden Klammer `})`:
    ```python
        REASON_BATTERY_SOC_UNAVAILABLE,
        # Phase 11: Dual-Window
        REASON_BEFORE_SLOT_A,
        REASON_SLOT_A_ACTIVE,
        REASON_SLOT_A_RESERVE_REACHED,
        REASON_BETWEEN_SLOTS,
        REASON_BEFORE_SLOT_B,
        REASON_SLOT_B_ACTIVE,
        REASON_SLOT_B_WINDOW_EXPIRED,
        REASON_SLOT_B_PRE_SUNRISE_CUTOFF,
    })
    ```

    **E. optimizer.py — REASON_LABELS_DE-Dict (Zeile 120-142) am Ende erweitern (echte Umlaute):**

    ```python
        REASON_BATTERY_SOC_UNAVAILABLE: "Batterie-SOC-Sensor nicht verfügbar",
        # Phase 11: Dual-Window
        REASON_BEFORE_SLOT_A: "Vor Slot-A-Start (Abend)",
        REASON_SLOT_A_ACTIVE: "Slot A aktiv (Abend-Entladung)",
        REASON_SLOT_A_RESERVE_REACHED: "Slot-A-Reserve erreicht",
        REASON_BETWEEN_SLOTS: "Pause zwischen Slot A und Slot B",
        REASON_BEFORE_SLOT_B: "Vor Slot-B-Start (Morgen)",
        REASON_SLOT_B_ACTIVE: "Slot B aktiv (Morgen-Entladung)",
        REASON_SLOT_B_WINDOW_EXPIRED: "Slot-B-Fenster abgelaufen",
        REASON_SLOT_B_PRE_SUNRISE_CUTOFF: "Slot B beendet vor Sonnenaufgang",
    }
    ```

    **F. optimizer.py — Decision-Dataclass (Zeile 247-280) erweitern:**

    Füge `discharge_active_slot` am Ende des `discharge_*`-Feld-Blocks ein, vor den `reasons`/`blocked_by`-Feldern (oder direkt davor — wichtig: VOR den `field(default_factory=...)`-Feldern, mit Default `None`):
    ```python
        # Phase 11: aktiver Slot ("A" | "B" | None für Legacy/Pause)
        discharge_active_slot: str | None = None
    ```
    Wenn der Decision-Block bereits andere Optional-Felder mit `None`-Default hat, neue Zeile direkt darunter; sonst direkt vor dem `reasons`-Feld einfügen.

    **G. Tests SCHREIBEN VOR Implementierung verifizieren** — siehe Task 2 für Test-Code. Diese Aufgabe (Task 1) implementiert die Konstanten/Funktion; Task 2 schreibt+lässt die Tests gegen sie laufen.
  </action>
  <verify>
    <automated>pytest tests/test_dual_window.py::TestComputeBWindowEnd -x -q</automated>
    <automated>pytest tests/test_dual_window.py::TestReasonsCatalog -x -q</automated>
    <automated>pytest tests/test_optimizer.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "CONF_ENABLE_DUAL_DISCHARGE = \"enable_dual_discharge\"" custom_components/eeg_energy_optimizer/const.py` liefert genau einen Treffer
    - `grep -n "CONF_DISCHARGE_A_START_TIME = \"discharge_a_start_time\"" custom_components/eeg_energy_optimizer/const.py` liefert genau einen Treffer
    - `grep -n "DEFAULT_DISCHARGE_A_RESERVE_PCT = 15" custom_components/eeg_energy_optimizer/const.py` liefert genau einen Treffer
    - `grep -n "\"enable_dual_discharge\"," custom_components/eeg_energy_optimizer/const.py` liefert mindestens einen Treffer (in TELEMETRY_SETTINGS_KEYS)
    - `grep -n "def compute_b_window_end(" custom_components/eeg_energy_optimizer/optimizer.py` liefert genau einen Treffer
    - `grep -n "REASON_SLOT_A_ACTIVE = \"slot_a_active\"" custom_components/eeg_energy_optimizer/optimizer.py` liefert genau einen Treffer
    - `grep -n "REASON_SLOT_B_PRE_SUNRISE_CUTOFF = \"slot_b_pre_sunrise_cutoff\"" custom_components/eeg_energy_optimizer/optimizer.py` liefert genau einen Treffer
    - `grep -n "discharge_active_slot: str | None = None" custom_components/eeg_energy_optimizer/optimizer.py` liefert genau einen Treffer
    - `grep -n "Slot A aktiv (Abend-Entladung)" custom_components/eeg_energy_optimizer/optimizer.py` liefert genau einen Treffer (Umlaut: KEIN "Abend-Entladung" mit ASCII-Substitution)
    - `python -c "from custom_components.eeg_energy_optimizer.optimizer import ALL_REASONS, REASON_SLOT_A_ACTIVE; assert REASON_SLOT_A_ACTIVE in ALL_REASONS"` exit-code 0
    - `python -c "from custom_components.eeg_energy_optimizer.optimizer import Decision; assert Decision().discharge_active_slot is None"` exit-code 0
    - Bestehende Test-Suite `pytest tests/test_optimizer.py -x` bleibt grün (keine Regression durch Decision-Erweiterung)
  </acceptance_criteria>
  <done>
    const.py exportiert 7 CONF_* + 6 DEFAULT_* + erweiterte TELEMETRY_SETTINGS_KEYS. optimizer.py exportiert compute_b_window_end + 8 neue REASON_* in ALL_REASONS + REASON_LABELS_DE-Erweiterung + Decision.discharge_active_slot. Bestehende Tests grün, neue Tests aus Task 2 grün.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migration v14→v15 + config_flow.VERSION-Bump + Test-Infrastruktur</name>
  <files>
    custom_components/eeg_energy_optimizer/__init__.py,
    custom_components/eeg_energy_optimizer/config_flow.py,
    tests/conftest.py,
    tests/test_dual_window.py,
    tests/test_config_flow.py
  </files>
  <read_first>
    custom_components/eeg_energy_optimizer/__init__.py (Zeilen 1-50 für Imports, Zeilen 750-846 für komplette _async_migrate_entry-Funktion mit allen Migrations-Blöcken bis v14),
    custom_components/eeg_energy_optimizer/config_flow.py (Zeilen 1-50 für VERSION-Position),
    tests/conftest.py (komplett, um bestehende Fixtures zu sehen),
    tests/test_optimizer.py (Zeilen 1-110 für Helper-Definitionen _make_config / _make_snapshot / _make_optimizer und Imports),
    tests/test_config_flow.py (komplett, um bestehende Migrations-Tests-Pattern zu sehen),
    .planning/milestones/v1.2-phases/11-dual-window-discharge/11-PATTERNS.md (Sektionen "Migration v14→v15", "tests/test_dual_window.py NEU", "tests/conftest.py")
  </read_first>
  <behavior>
    - Migration v14→v15 für nicht-SolarEdge-Bestands-Entry ohne explizite Slot-Keys: setzt enable_dual_discharge=True, enable_slot_a=True, enable_slot_b=True
    - Migration v14→v15 für SolarEdge-Bestands-Entry: setzt enable_dual_discharge=False, enable_slot_a=True, enable_slot_b=False
    - Migration ist idempotent: zweiter Aufruf mit version=15 macht keine Änderungen
    - Migration respektiert vorhandene Werte: setdefault statt Hard-Set für die 4 Time-/Reserve-Keys
    - config_flow.py:24 deklariert VERSION = 15
    - tests/conftest.py exportiert _make_config / _make_snapshot / _make_optimizer als Modul-Level-Funktionen (importierbar via `from tests.conftest import _make_config`)
    - tests/test_dual_window.py importiert die Helpers und definiert TestComputeBWindowEnd / TestReasonsCatalog / TestMigrationV14ToV15 Klassen
  </behavior>
  <action>
    **A. tests/conftest.py — Helper-Extraktion (Modul-Level-Funktionen, NICHT als Fixtures, weil sie parametrisierbar sein müssen):**

    Lies `tests/test_optimizer.py:63-106` und kopiere die drei Funktionen `_make_config`, `_make_snapshot`, `_make_optimizer` 1:1 in `tests/conftest.py` als Modul-Level-Funktionen. Imports in conftest.py entsprechend ergänzen (alle `CONF_*`-Konstanten, `Snapshot`, `EEGOptimizer`, `datetime`, `timezone` etc. — siehe Imports-Block in `tests/test_optimizer.py:1-60`).

    **WICHTIG:** Belasse die Funktionen ZUSÄTZLICH in `tests/test_optimizer.py` (Re-Export oder Duplikat) — bestehende Tests in `test_optimizer.py` importieren sie lokal. Lösung: In `tests/test_optimizer.py` die lokalen Definitionen durch `from .conftest import _make_config, _make_snapshot, _make_optimizer` ersetzen (oder Tests laufen weiter über lokale Def — beides akzeptabel solange beide Test-Dateien grün sind).

    Bestehende Fixtures `mock_hass`, `mock_inverter`, `mock_coordinator`, `mock_provider` in conftest.py bleiben unverändert.

    **B. config_flow.py — VERSION-Bump:**

    Zeile 24 ändern von `VERSION = 14` auf `VERSION = 15`. Keine weiteren Änderungen.

    **C. __init__.py — Migrations-Block v14→v15 direkt nach dem v14-Block (nach Zeile 844, vor `return True` Zeile 846):**

    Importe oben in `__init__.py` prüfen — `INVERTER_TYPE_SOLAREDGE` muss verfügbar sein. Falls nicht importiert: ergänze in der Imports-Region oben `from .const import INVERTER_TYPE_SOLAREDGE` (oder verwende den Stringliteral `"solaredge_storedge"` direkt, konsistent mit `optimizer.py:349` und `:384`). **Empfehlung:** importieren der Konstante; falls die Konstante in const.py noch nicht existiert, Stringliteral verwenden und in einer Bemerkung im Code dokumentieren.

    Migrationsblock:
    ```python
    if entry.version < 15:
        # v15 — Phase 11: Dual-Window-Entladung
        # Additive Migration: setzt neue Slot-Konfigurations-Keys mit
        # Defaults. Default-Wechsel (D-04, intendiert) — Bestands-Anlagen
        # erhalten Dual-Window automatisch beim Update. Mitigation:
        # Pro-Slot-Hysterese und PV-Tomorrow-Garantie verhindern aggressive
        # Erstaktivierung; CHANGELOG dokumentiert die Verhaltensänderung.
        # SolarEdge-Sonderfall (D-03): NVRAM-Verschleiß erlaubt nur einen
        # Slot pro Tag → enable_dual_discharge=False, enable_slot_a=True,
        # enable_slot_b=False. Defense-in-depth in 11-03 (Save-Path) und
        # 11-02 (Runtime-Erzwingung).
        new_data = {**entry.data}
        inverter_type = new_data.get("inverter_type", "")
        is_solaredge = inverter_type == "solaredge_storedge"
        if is_solaredge:
            new_data.setdefault("enable_dual_discharge", False)
            new_data.setdefault("enable_slot_a", True)
            new_data.setdefault("enable_slot_b", False)
        else:
            new_data.setdefault("enable_dual_discharge", True)
            new_data.setdefault("enable_slot_a", True)
            new_data.setdefault("enable_slot_b", True)
        new_data.setdefault("discharge_a_start_time", "20:00")
        new_data.setdefault("discharge_b_start_time", "03:00")
        new_data.setdefault("discharge_b_end_cap", "07:00")
        new_data.setdefault("discharge_a_reserve_pct", 15)
        hass.config_entries.async_update_entry(entry, data=new_data, version=15)
    ```

    **D. tests/test_dual_window.py — neue Datei mit drei Test-Klassen:**

    Datei-Anfang:
    ```python
    """Phase 11: Dual-Window-Entladung — Tests for compute_b_window_end,
    Reasons-Catalog, Migration v14→v15."""
    from __future__ import annotations

    from datetime import datetime, timedelta, timezone
    from unittest.mock import AsyncMock, MagicMock

    import pytest

    from custom_components.eeg_energy_optimizer.optimizer import (
        ALL_REASONS,
        Decision,
        REASON_BEFORE_SLOT_A,
        REASON_BEFORE_SLOT_B,
        REASON_BETWEEN_SLOTS,
        REASON_LABELS_DE,
        REASON_SLOT_A_ACTIVE,
        REASON_SLOT_A_RESERVE_REACHED,
        REASON_SLOT_B_ACTIVE,
        REASON_SLOT_B_PRE_SUNRISE_CUTOFF,
        REASON_SLOT_B_WINDOW_EXPIRED,
        compute_b_window_end,
    )
    ```

    **TestComputeBWindowEnd-Klasse mit 7 Tests** (jeweils tz-aware UTC, exakt die Werte aus 11-RESEARCH.md):
    ```python
    class TestComputeBWindowEnd:
        def test_summer_sunrise_5min_dominant(self):
            sunrise = datetime(2026, 6, 21, 4, 52, tzinfo=timezone.utc)
            now = datetime(2026, 6, 21, 3, 0, tzinfo=timezone.utc)
            result = compute_b_window_end(now, sunrise, "07:00", 0)
            assert result == datetime(2026, 6, 21, 4, 47, tzinfo=timezone.utc)

        def test_winter_sunrise_cap_dominant(self):
            sunrise = datetime(2026, 12, 21, 7, 30, tzinfo=timezone.utc)
            now = datetime(2026, 12, 21, 3, 0, tzinfo=timezone.utc)
            result = compute_b_window_end(now, sunrise, "07:00", 0)
            assert result == datetime(2026, 12, 21, 7, 0, tzinfo=timezone.utc)

        def test_transition_sunrise_5min_dominant(self):
            sunrise = datetime(2026, 4, 15, 6, 0, tzinfo=timezone.utc)
            now = datetime(2026, 4, 15, 3, 0, tzinfo=timezone.utc)
            result = compute_b_window_end(now, sunrise, "07:00", 0)
            assert result == datetime(2026, 4, 15, 5, 55, tzinfo=timezone.utc)

        def test_deep_winter_cap_dominant(self):
            sunrise = datetime(2026, 1, 15, 8, 30, tzinfo=timezone.utc)
            now = datetime(2026, 1, 15, 3, 0, tzinfo=timezone.utc)
            result = compute_b_window_end(now, sunrise, "07:00", 0)
            assert result == datetime(2026, 1, 15, 7, 0, tzinfo=timezone.utc)

        def test_morning_offset_one_hour_pause(self):
            sunrise = datetime(2026, 12, 21, 7, 30, tzinfo=timezone.utc)
            now = datetime(2026, 12, 21, 3, 0, tzinfo=timezone.utc)
            result = compute_b_window_end(now, sunrise, "07:00", 1)
            assert result == datetime(2026, 12, 21, 6, 25, tzinfo=timezone.utc)

        def test_sunrise_none_returns_none(self):
            now = datetime(2026, 6, 21, 3, 0, tzinfo=timezone.utc)
            assert compute_b_window_end(now, None, "07:00", 0) is None

        def test_b_end_cap_late_evening_still_clamped_by_sunrise(self):
            sunrise = datetime(2026, 6, 21, 4, 52, tzinfo=timezone.utc)
            now = datetime(2026, 6, 21, 3, 0, tzinfo=timezone.utc)
            result = compute_b_window_end(now, sunrise, "23:59", 0)
            # cap weit nach sunrise → sunrise−5min muss gewinnen
            assert result == datetime(2026, 6, 21, 4, 47, tzinfo=timezone.utc)
    ```

    **TestReasonsCatalog-Klasse:**
    ```python
    class TestReasonsCatalog:
        def test_all_8_new_reasons_in_all_reasons(self):
            new = {
                REASON_BEFORE_SLOT_A,
                REASON_SLOT_A_ACTIVE,
                REASON_SLOT_A_RESERVE_REACHED,
                REASON_BETWEEN_SLOTS,
                REASON_BEFORE_SLOT_B,
                REASON_SLOT_B_ACTIVE,
                REASON_SLOT_B_WINDOW_EXPIRED,
                REASON_SLOT_B_PRE_SUNRISE_CUTOFF,
            }
            assert new.issubset(ALL_REASONS)

        def test_all_new_reasons_have_german_labels(self):
            keys = [
                REASON_BEFORE_SLOT_A,
                REASON_SLOT_A_ACTIVE,
                REASON_SLOT_A_RESERVE_REACHED,
                REASON_BETWEEN_SLOTS,
                REASON_BEFORE_SLOT_B,
                REASON_SLOT_B_ACTIVE,
                REASON_SLOT_B_WINDOW_EXPIRED,
                REASON_SLOT_B_PRE_SUNRISE_CUTOFF,
            ]
            for key in keys:
                assert key in REASON_LABELS_DE
                assert REASON_LABELS_DE[key]  # nicht leer

        def test_decision_default_active_slot_is_none(self):
            d = Decision()
            assert d.discharge_active_slot is None
    ```

    **TestMigrationV14ToV15-Klasse mit 4 Tests:**
    ```python
    class TestMigrationV14ToV15:
        @pytest.mark.asyncio
        async def test_non_solaredge_gets_dual_true(self):
            from custom_components.eeg_energy_optimizer import async_migrate_entry
            hass = MagicMock()
            hass.config_entries.async_update_entry = MagicMock()
            entry = MagicMock()
            entry.version = 14
            entry.data = {"inverter_type": "huawei_sun2000"}
            await async_migrate_entry(hass, entry)
            args, kwargs = hass.config_entries.async_update_entry.call_args
            new_data = kwargs.get("data") or args[1]
            assert new_data["enable_dual_discharge"] is True
            assert new_data["enable_slot_a"] is True
            assert new_data["enable_slot_b"] is True
            assert new_data["discharge_a_start_time"] == "20:00"
            assert new_data["discharge_b_start_time"] == "03:00"
            assert new_data["discharge_b_end_cap"] == "07:00"
            assert new_data["discharge_a_reserve_pct"] == 15
            assert kwargs.get("version") == 15

        @pytest.mark.asyncio
        async def test_solaredge_gets_xor_config(self):
            from custom_components.eeg_energy_optimizer import async_migrate_entry
            hass = MagicMock()
            hass.config_entries.async_update_entry = MagicMock()
            entry = MagicMock()
            entry.version = 14
            entry.data = {"inverter_type": "solaredge_storedge"}
            await async_migrate_entry(hass, entry)
            args, kwargs = hass.config_entries.async_update_entry.call_args
            new_data = kwargs.get("data") or args[1]
            assert new_data["enable_dual_discharge"] is False
            assert new_data["enable_slot_a"] is True
            assert new_data["enable_slot_b"] is False

        @pytest.mark.asyncio
        async def test_existing_user_values_preserved(self):
            from custom_components.eeg_energy_optimizer import async_migrate_entry
            hass = MagicMock()
            hass.config_entries.async_update_entry = MagicMock()
            entry = MagicMock()
            entry.version = 14
            entry.data = {
                "inverter_type": "huawei_sun2000",
                "enable_dual_discharge": False,  # User hat explizit deaktiviert
                "discharge_a_start_time": "21:00",  # User-Override
            }
            await async_migrate_entry(hass, entry)
            args, kwargs = hass.config_entries.async_update_entry.call_args
            new_data = kwargs.get("data") or args[1]
            # setdefault muss vorhandene Werte respektieren
            assert new_data["enable_dual_discharge"] is False
            assert new_data["discharge_a_start_time"] == "21:00"
            # Defaults für nicht-gesetzte Keys
            assert new_data["enable_slot_a"] is True
            assert new_data["discharge_b_start_time"] == "03:00"

        @pytest.mark.asyncio
        async def test_migration_idempotent_when_already_v15(self):
            from custom_components.eeg_energy_optimizer import async_migrate_entry
            hass = MagicMock()
            hass.config_entries.async_update_entry = MagicMock()
            entry = MagicMock()
            entry.version = 15
            entry.data = {"inverter_type": "huawei_sun2000"}
            await async_migrate_entry(hass, entry)
            # version<15 ist False → Update-Aufruf für v15-Block darf nicht passieren.
            # Frühere Blöcke (v11/v12/v13/v14) feuern auch nicht weil version=15.
            assert hass.config_entries.async_update_entry.call_count == 0
    ```

    **E. tests/test_config_flow.py — VERSION-Test ergänzen (am Ende der Datei oder in passender Klasse):**

    ```python
    def test_config_flow_version_is_15():
        from custom_components.eeg_energy_optimizer.config_flow import (
            EegEnergyOptimizerConfigFlow,
        )
        assert EegEnergyOptimizerConfigFlow.VERSION == 15
    ```

    Falls der genaue Klassen-Name in config_flow.py abweicht (Read der Datei prüft das), Klassennamen anpassen.
  </action>
  <verify>
    <automated>pytest tests/test_dual_window.py::TestMigrationV14ToV15 -x -q</automated>
    <automated>pytest tests/test_config_flow.py -k version -x -q</automated>
    <automated>pytest tests/test_dual_window.py -x -q</automated>
    <automated>pytest tests/test_optimizer.py -x -q</automated>
    <automated>pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "VERSION = 15" custom_components/eeg_energy_optimizer/config_flow.py` liefert genau einen Treffer
    - `grep -n "if entry.version < 15:" custom_components/eeg_energy_optimizer/__init__.py` liefert genau einen Treffer
    - `grep -n "version=15" custom_components/eeg_energy_optimizer/__init__.py` liefert genau einen Treffer (im async_update_entry-Aufruf)
    - `grep -n "enable_dual_discharge" custom_components/eeg_energy_optimizer/__init__.py` liefert mindestens 2 Treffer (SolarEdge-Branch + non-SolarEdge-Branch)
    - `grep -n "discharge_a_start_time" custom_components/eeg_energy_optimizer/__init__.py` liefert mindestens einen Treffer mit `setdefault`
    - `grep -n "def _make_config" tests/conftest.py` liefert genau einen Treffer
    - `grep -n "def _make_snapshot" tests/conftest.py` liefert genau einen Treffer
    - `grep -n "def _make_optimizer" tests/conftest.py` liefert genau einen Treffer
    - `grep -n "class TestComputeBWindowEnd" tests/test_dual_window.py` liefert genau einen Treffer
    - `grep -n "class TestMigrationV14ToV15" tests/test_dual_window.py` liefert genau einen Treffer
    - `pytest tests/test_dual_window.py::TestComputeBWindowEnd -x -q` exit-code 0
    - `pytest tests/test_dual_window.py::TestMigrationV14ToV15 -x -q` exit-code 0
    - `pytest tests/test_dual_window.py::TestReasonsCatalog -x -q` exit-code 0
    - `pytest tests/test_optimizer.py -x -q` exit-code 0 (keine Regression)
    - `pytest tests/ -q` exit-code 0 (volle Suite grün)
  </acceptance_criteria>
  <done>
    Migration v14→v15 läuft durch, alle 4 Migrations-Tests grün, conftest.py-Helpers extrahiert, tests/test_dual_window.py existiert mit 3 Test-Klassen, config_flow.VERSION = 15, volle Test-Suite grün.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HA Boot → Migration | entry.data wird mutiert, version-Bump persistiert sich in HA-Storage |
| Constants → Optimizer | const.py-Konstanten werden importiert; falsche Werte führen zu Default-Drift |
| compute_b_window_end → Slot-B-Decision | Pure-Funktion, kein State; falsche Math führt zu Mutex-Verletzung mit Morgen-Einspeisung |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-01 | Tampering | __init__.py:async_migrate_entry | mitigate | `setdefault` statt `=` für nicht-Hard-Migration-Keys; existierende User-Konfiguration darf nicht überschrieben werden — Test `test_existing_user_values_preserved` |
| T-11-01-02 | Tampering | Migration läuft mehrfach (Idempotenz) | mitigate | `if entry.version < 15:`-Guard ist HA-Standard und idempotent — Test `test_migration_idempotent_when_already_v15` |
| T-11-01-03 | Tampering | compute_b_window_end mit malformed b_end_cap | accept | b_end_cap kommt aus dem Save-Path mit Voluptuous-Validierung in 11-03; Phase 11-01 vertraut diesem Vertrag. Bei manueller direkter Aufruf-Tampering: Crash via ValueError im split() ist akzeptabel (Defense-in-depth in Save-Path) |
| T-11-01-04 | Information Disclosure | TELEMETRY_SETTINGS_KEYS-Erweiterung | mitigate | Whitelist-Pattern bleibt — neue Keys sind alle Bool/Int/Time-String, keine PII. Reviewed: enable_dual_discharge, enable_slot_a/b, discharge_a/b_start_time, discharge_b_end_cap, discharge_a_reserve_pct sind allesamt nicht-personenbezogen |

Severity: alle LOW. Phase 11-01 ist additive Daten-Schicht ohne neue Auth-/Endpoint-/Krypto-Operation.
</threat_model>

<verification>
**Automated:**
```bash
pytest tests/test_dual_window.py::TestComputeBWindowEnd -x -q
pytest tests/test_dual_window.py::TestReasonsCatalog -x -q
pytest tests/test_dual_window.py::TestMigrationV14ToV15 -x -q
pytest tests/test_config_flow.py -k version -x -q
pytest tests/ -q
```

**Pattern checks (no regression):**
```bash
grep -c "VERSION = " custom_components/eeg_energy_optimizer/config_flow.py  # genau 1
grep -c "if entry.version <" custom_components/eeg_energy_optimizer/__init__.py  # mindestens 5 (v11..v15)
```
</verification>

<success_criteria>
- const.py exportiert 7 CONF_* + 6 DEFAULT_* + erweiterte TELEMETRY_SETTINGS_KEYS
- optimizer.py exportiert compute_b_window_end + 8 neue REASON_* in ALL_REASONS + REASON_LABELS_DE-Erweiterung + Decision.discharge_active_slot
- config_flow.py:VERSION = 15
- __init__.py enthält Migrations-Block v14→v15 mit SolarEdge-Sonderfall
- tests/conftest.py exportiert die drei Helper-Funktionen als Modul-Level
- tests/test_dual_window.py existiert mit TestComputeBWindowEnd (7 Tests), TestReasonsCatalog (3 Tests), TestMigrationV14ToV15 (4 Tests)
- Volle Test-Suite `pytest tests/ -q` grün
- Keine Regression in tests/test_optimizer.py
</success_criteria>

<output>
Nach Abschluss: erstelle `.planning/phases/11-dual-window-discharge/11-01-SUMMARY.md` mit:
- Aufgelisteten Konstanten und Defaults
- compute_b_window_end-Signatur und Test-Coverage
- Migrations-Block mit Branch-Logik (SolarEdge vs Standard)
- Decision-Field-Erweiterung
- Helper-Extraktion-Status (conftest.py)
- Test-Status (alle grün)
- Hinweis: Phase 11-02 baut auf compute_b_window_end + neuen REASON_*-Konstanten + Decision.discharge_active_slot auf
</output>
