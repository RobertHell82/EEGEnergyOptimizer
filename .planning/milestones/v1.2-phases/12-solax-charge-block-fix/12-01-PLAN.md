---
phase: 12-solax-charge-block-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - custom_components/eeg_energy_optimizer/inverter/solax.py
  - custom_components/eeg_energy_optimizer/__init__.py
  - custom_components/eeg_energy_optimizer/config_flow.py
  - tests/test_solax_inverter.py
  - CHANGELOG.md
autonomous: true
requirements:
  - 12-R1
  - 12-R2
  - 12-R3
  - 12-R4
tags:
  - solax
  - inverter
  - morgen-einspeisung
  - phase-12

must_haves:
  truths:
    - "Bei SolaX-Wechselrichtern setzt `async_set_charge_limit(0)` das Entity `battery_charge_max_current` auf 0 und stellt sicher, dass `remotecontrol_power_control = \"Disabled\"` — Mode 1 wird NICHT mehr aktiviert."
    - "Beim ersten Aufruf von `async_set_charge_limit` mit einem aktuellen `battery_charge_max_current` > 0 wird dieser Wert über `homeassistant.helpers.storage.Store` persistiert."
    - "Wenn der aktuelle `battery_charge_max_current` 0 ist (Reboot mitten im Block), wird der Cache nicht überschrieben — der vorhandene gespeicherte Wert bleibt erhalten."
    - "`async_stop_forcible` deaktiviert Mode 1 explizit (Disabled + active_power=0 + autorepeat_duration=0 + trigger) UND restoriert `battery_charge_max_current` aus dem Store; Fallback ist `attributes.max` des Entities (typisch 30 A)."
    - "`async_set_discharge` bleibt unverändert — nutzt weiter Mode 1 mit negativer Power für Slot A/B/Manual-Discharge."
    - "Config-Migration v17 → v18 fügt den Key `solax_battery_charge_max_current` mit Default `number.solax_inverter_battery_charge_max_current` hinzu, falls nicht vorhanden."
    - "Bestehende SolaX-Anlagen erhalten beim ersten Optimizer-Cycle nach dem Update automatisch den Original-Wert ihres `battery_charge_max_current` in den Store gecached."
  artifacts:
    - path: "custom_components/eeg_energy_optimizer/inverter/solax.py"
      provides: "battery_charge_max_current-basierter Charge-Block, Storage-persistierter Original-Wert, async_stop_forcible mit Restore"
      contains: "battery_charge_max_current"
    - path: "custom_components/eeg_energy_optimizer/inverter/solax.py"
      provides: "SOLAX_ENTITY_DEFAULTS um battery_charge_max_current erweitert"
      contains: "\"battery_charge_max_current\": \"number.solax_inverter_battery_charge_max_current\""
    - path: "custom_components/eeg_energy_optimizer/inverter/solax.py"
      provides: "Storage-Helper-Klasse SolaXStateStore"
      contains: "STORAGE_KEY = \"eeg_energy_optimizer.solax_state\""
    - path: "custom_components/eeg_energy_optimizer/__init__.py"
      provides: "Migration v17→v18 mit solax_battery_charge_max_current Default"
      contains: "version=18"
    - path: "custom_components/eeg_energy_optimizer/config_flow.py"
      provides: "VERSION = 18"
      contains: "VERSION = 18"
    - path: "tests/test_solax_inverter.py"
      provides: "8 neue/aktualisierte Tests für charge-limit + stop-forcible Verhalten"
      contains: "battery_charge_max_current"
    - path: "CHANGELOG.md"
      provides: "Eintrag unter Unreleased mit Verhaltensänderung beim Update"
      contains: "SolaX: Morgen-Einspeisung blockiert"
  key_links:
    - from: "solax.py:async_set_charge_limit"
      to: "_set_number('battery_charge_max_current', 0)"
      via: "Block Laden über Max-Current statt Mode-1-Idle"
      pattern: "battery_charge_max_current.*=.*0|\"battery_charge_max_current\""
    - from: "solax.py:async_set_charge_limit"
      to: "SolaXStateStore.async_save_original_current"
      via: "Persistierung des Original-Werts beim ersten Eingriff"
      pattern: "async_save_original_current"
    - from: "solax.py:async_stop_forcible"
      to: "SolaXStateStore.original_current"
      via: "Restore aus Store nach Morgen-Einspeisung"
      pattern: "original_current"
    - from: "__init__.py:async_migrate_entry"
      to: "version=18 + solax_battery_charge_max_current"
      via: "Config-Migration für neuen Entity-Override-Key"
      pattern: "version=18"
---

<objective>
SolaX-Driver so umbauen, dass `async_set_charge_limit(0)` nur das Laden blockiert (`battery_charge_max_current = 0`) statt die Batterie via Mode 1 komplett auf Idle zu setzen. Self-Use-Mode der SolaX läuft im Hintergrund weiter — Hausverbrauch wird wieder aus der Batterie gedeckt bis `selfuse_discharge_min_soc`, statt aus dem Netz. Damit wird die SolaX-Implementierung auf das gleiche Verhalten wie Huawei (`batterien_maximale_ladeleistung=0`) und Fronius (`StorCtl_Mod Bit 0 + InWRte=0`) gebracht. Original-Wert von `battery_charge_max_current` wird über `homeassistant.helpers.storage.Store` persistiert und beim Beenden von Morgen-Einspeisung wiederhergestellt.

Purpose: Fixt den in der Nacht 14./15. Mai 2026 beobachteten SOC-Einfrierungs-Bug bei ~19 % während Morgen-Einspeisung. Bringt SolaX-Verhalten in Einklang mit den anderen Inverter-Drivern.

Output: Refactored `solax.py` mit Storage-Persistierung, Config-Migration v17 → v18, 8 aktualisierte/neue Tests, CHANGELOG-Eintrag mit Verhaltensänderung-Hinweis.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v1.2-ROADMAP.md
@.planning/milestones/v1.2-phases/12-solax-charge-block-fix/12-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Existierende Verträge, die der Executor unverändert nutzt. -->

From custom_components/eeg_energy_optimizer/inverter/base.py:
```python
class InverterBase(ABC):
    @abstractmethod
    async def async_set_charge_limit(self, power_kw: float) -> bool: ...
    @abstractmethod
    async def async_set_discharge(self, power_kw: float, target_soc: float | None = None) -> bool: ...
    @abstractmethod
    async def async_stop_forcible(self) -> bool: ...
    @property
    @abstractmethod
    def is_available(self) -> bool: ...
```

From custom_components/eeg_energy_optimizer/optimizer.py:1939-1948:
```python
if decision.zustand == STATE_MORGEN_EINSPEISUNG:
    await self._inverter.async_set_charge_limit(0)
elif decision.zustand == STATE_ABEND_ENTLADUNG:
    await self._inverter.async_set_discharge(
        decision.entladeleistung_kw,
        target_soc=decision.min_soc_berechnet,
    )
else:
    await self._inverter.async_stop_forcible()
```

From custom_components/eeg_energy_optimizer/inverter/solax.py (PRE-PHASE-12 — wird ersetzt):
```python
SOLAX_ENTITY_DEFAULTS = {
    "remotecontrol_power_control": "select.solax_inverter_remotecontrol_power_control",
    "remotecontrol_active_power": "number.solax_inverter_remotecontrol_active_power",
    "remotecontrol_autorepeat_duration": "number.solax_inverter_remotecontrol_autorepeat_duration",
    "remotecontrol_duration": "number.solax_inverter_remotecontrol_duration",
    "remotecontrol_trigger": "button.solax_inverter_remotecontrol_trigger",
    "battery_charge_max_current": "number.solax_inverter_battery_charge_max_current",
}
```

From homeassistant.helpers.storage (used by existing __init__.py for activity log):
```python
from homeassistant.helpers.storage import Store
store = Store(hass, version, key)
await store.async_load()  # returns dict or None
await store.async_save(data: dict)
```
</interfaces>
</context>

<tasks>

<task id="1">
<action>
Erweitere `SOLAX_ENTITY_DEFAULTS` in `custom_components/eeg_energy_optimizer/inverter/solax.py` um den Schlüssel `battery_charge_max_current` (Default-Entity: `number.solax_inverter_battery_charge_max_current`). Falls der Key bereits existiert: belasse ihn unverändert.

Konkret in `solax.py` Zeile 26-33 (`SOLAX_ENTITY_DEFAULTS`-Dict) ergänzen oder verifizieren:
```python
SOLAX_ENTITY_DEFAULTS = {
    "remotecontrol_power_control": "select.solax_inverter_remotecontrol_power_control",
    "remotecontrol_active_power": "number.solax_inverter_remotecontrol_active_power",
    "remotecontrol_autorepeat_duration": "number.solax_inverter_remotecontrol_autorepeat_duration",
    "remotecontrol_duration": "number.solax_inverter_remotecontrol_duration",
    "remotecontrol_trigger": "button.solax_inverter_remotecontrol_trigger",
    "battery_charge_max_current": "number.solax_inverter_battery_charge_max_current",
}
```
</action>
<read_first>
- custom_components/eeg_energy_optimizer/inverter/solax.py
</read_first>
<acceptance_criteria>
- `grep -n "battery_charge_max_current" custom_components/eeg_energy_optimizer/inverter/solax.py` zeigt mindestens 3 Treffer (Default-Dict + 2 Verwendungen in den umgebauten Methoden)
- `grep -c "SOLAX_ENTITY_DEFAULTS\[" custom_components/eeg_energy_optimizer/inverter/solax.py` ≥ 1
- `python -c "from custom_components.eeg_energy_optimizer.inverter.solax import SOLAX_ENTITY_DEFAULTS; assert 'battery_charge_max_current' in SOLAX_ENTITY_DEFAULTS"` exit code 0
</acceptance_criteria>
</task>

<task id="2">
<action>
Füge in `custom_components/eeg_energy_optimizer/inverter/solax.py` eine neue Klasse `SolaXStateStore` direkt nach den Konstanten und vor `class SolaXInverter` ein. Diese Klasse kapselt die Persistierung des Original-Werts von `battery_charge_max_current`.

```python
from homeassistant.helpers.storage import Store

class SolaXStateStore:
    """Persistiert den Original-Wert von battery_charge_max_current über HA-Reboots.

    Wird beim ersten async_set_charge_limit-Aufruf mit aktuellem State > 0 befüllt.
    async_stop_forcible liest den Wert beim Beenden von Morgen-Einspeisung wieder aus.
    """

    STORAGE_KEY = "eeg_energy_optimizer.solax_state"
    STORAGE_VERSION = 1

    def __init__(self, hass: Any) -> None:
        self._store = Store(hass, self.STORAGE_VERSION, self.STORAGE_KEY)
        self._data: dict = {}
        self._loaded = False

    async def async_load(self) -> None:
        if self._loaded:
            return
        data = await self._store.async_load()
        self._data = data if isinstance(data, dict) else {}
        self._loaded = True

    async def async_save_original_current(self, amps: float) -> None:
        self._data["battery_charge_max_current_original"] = float(amps)
        await self._store.async_save(self._data)

    @property
    def original_current(self) -> float | None:
        val = self._data.get("battery_charge_max_current_original")
        return float(val) if val is not None else None
```

Den Import von `Store` aus `homeassistant.helpers.storage` an den Top-Imports unter den TYPE_CHECKING-Block einfügen — ABER guarded für Test-Environment (Pattern wie in `__init__.py` für `ActivityLog`):

```python
try:
    from homeassistant.helpers.storage import Store
except ImportError:  # pragma: no cover — test environment
    Store = None  # type: ignore
```
</action>
<read_first>
- custom_components/eeg_energy_optimizer/inverter/solax.py
- custom_components/eeg_energy_optimizer/__init__.py
</read_first>
<acceptance_criteria>
- `grep -n "class SolaXStateStore" custom_components/eeg_energy_optimizer/inverter/solax.py` exit 0
- `grep -n "STORAGE_KEY = \"eeg_energy_optimizer.solax_state\"" custom_components/eeg_energy_optimizer/inverter/solax.py` exit 0
- `grep -n "async_save_original_current" custom_components/eeg_energy_optimizer/inverter/solax.py` exit 0
- `grep -n "original_current" custom_components/eeg_energy_optimizer/inverter/solax.py` exit 0
- `grep -n "from homeassistant.helpers.storage import Store" custom_components/eeg_energy_optimizer/inverter/solax.py` exit 0
</acceptance_criteria>
</task>

<task id="3">
<action>
Refactore `SolaXInverter.__init__` und `async_set_charge_limit` in `custom_components/eeg_energy_optimizer/inverter/solax.py`.

`__init__`: Instanziere einen `SolaXStateStore`, speichere ihn in `self._state_store`. Nicht laden — das passiert lazy beim ersten Eingriff.

```python
def __init__(self, hass: Any, config: dict) -> None:
    super().__init__(hass, config)
    self._state_store = SolaXStateStore(hass) if Store is not None else None
```

`async_set_charge_limit(power_kw)` komplett neu schreiben:

```python
async def async_set_charge_limit(self, power_kw: float) -> bool:
    """Set battery charge limit. power_kw=0 blocks charging only (NOT discharge).

    Uses battery_charge_max_current entity (Ampere) — analog zu Huawei
    batterien_maximale_ladeleistung und Fronius StorCtl_Mod Bit 0 + InWRte=0.
    Self-Use-Mode der SolaX läuft im Hintergrund weiter; Discharge bleibt
    möglich bis selfuse_discharge_min_soc.

    Der Originalwert wird beim ersten Eingriff in einen Store persistiert
    (siehe SolaXStateStore). Bei Reboot mitten im Block (aktueller State = 0)
    wird der Cache NICHT überschrieben.
    """
    try:
        await self._ensure_original_cached()

        if power_kw == 0:
            await self._set_number("battery_charge_max_current", 0)
        else:
            # Partial limit — kW → A über Batteriespannung (default 400 V Fallback)
            voltage = self._read_battery_voltage_or_default()
            max_a = self._read_max_charge_current_attribute() or 30.0
            amps = max(0.0, min(power_kw * 1000.0 / voltage, max_a))
            await self._set_number("battery_charge_max_current", amps)

        # Sicherstellen, dass Mode 1 NICHT aktiv ist (Migration für
        # Bestands-Setups, die noch im alten Mode-1-Idle stehen).
        await self._set_select("remotecontrol_power_control", "Disabled")
        return True
    except Exception:
        _LOGGER.exception("SolaX: Failed to set charge limit")
        return False

async def _ensure_original_cached(self) -> None:
    """Cache aktuellen battery_charge_max_current in Store, falls > 0 und noch nicht gespeichert."""
    if self._state_store is None:
        return
    await self._state_store.async_load()
    if self._state_store.original_current is not None:
        return  # Bereits gespeichert
    current = self._read_current_charge_max_current()
    if current is not None and current > 0:
        await self._state_store.async_save_original_current(current)

def _read_current_charge_max_current(self) -> float | None:
    """Liest aktuellen State von battery_charge_max_current Entity. Liefert None wenn nicht verfügbar."""
    entity_id = self._config.get(
        "solax_battery_charge_max_current",
        SOLAX_ENTITY_DEFAULTS["battery_charge_max_current"],
    )
    state = self._hass.states.get(entity_id)
    if state is None:
        return None
    try:
        return float(state.state)
    except (TypeError, ValueError):
        return None

def _read_max_charge_current_attribute(self) -> float | None:
    """Liest attributes.max des battery_charge_max_current Entities (Hardware-Maximum)."""
    entity_id = self._config.get(
        "solax_battery_charge_max_current",
        SOLAX_ENTITY_DEFAULTS["battery_charge_max_current"],
    )
    state = self._hass.states.get(entity_id)
    if state is None:
        return None
    max_val = state.attributes.get("max")
    try:
        return float(max_val) if max_val is not None else None
    except (TypeError, ValueError):
        return None

def _read_battery_voltage_or_default(self) -> float:
    """Liest aktuelle Batteriespannung. Fallback: 400 V (typischer SolaX-Hochvolt-Bereich)."""
    state = self._hass.states.get("sensor.solax_inverter_battery_voltage_charge")
    if state is not None:
        try:
            v = float(state.state)
            if v > 50:  # Sanity-Check (HV-System)
                return v
        except (TypeError, ValueError):
            pass
    return 400.0
```

Den Anpassungen in `_set_number` ist NICHT nötig — der Helper resolved Entity-IDs bereits via `self._config.get(f"solax_{config_key}", SOLAX_ENTITY_DEFAULTS[config_key])` (Zeile 42-46). Mit dem neuen `battery_charge_max_current`-Key funktioniert das out-of-the-box.
</action>
<read_first>
- custom_components/eeg_energy_optimizer/inverter/solax.py
</read_first>
<acceptance_criteria>
- `grep -n "self._state_store" custom_components/eeg_energy_optimizer/inverter/solax.py` zeigt mindestens 4 Treffer (Init + 3 Methoden-Verwendungen)
- `grep -n "_ensure_original_cached" custom_components/eeg_energy_optimizer/inverter/solax.py` exit 0
- `grep -n "_read_current_charge_max_current" custom_components/eeg_energy_optimizer/inverter/solax.py` exit 0
- `grep -n "_read_battery_voltage_or_default" custom_components/eeg_energy_optimizer/inverter/solax.py` exit 0
- Im Code-Body von `async_set_charge_limit`: KEIN Aufruf von `_set_select("remotecontrol_power_control", "Enabled Battery Control")` mit anschließendem `active_power=0`. Stattdessen: `_set_select(..., "Disabled")` UND `_set_number("battery_charge_max_current", ...)`.
- `grep -n "Enabled Battery Control" custom_components/eeg_energy_optimizer/inverter/solax.py | grep -v async_set_discharge` darf maximal 0 Treffer haben (Mode 1 nur noch in `async_set_discharge` für negativen Power-Wert verwenden — verifiziere mit `grep -B5 -A20 'async def async_set_discharge'`).
</acceptance_criteria>
</task>

<task id="4">
<action>
Refactore `async_stop_forcible` in `custom_components/eeg_energy_optimizer/inverter/solax.py`. Die Methode muss zusätzlich zum existierenden Mode-1-Disable den `battery_charge_max_current` aus dem Store wiederherstellen.

```python
async def async_stop_forcible(self) -> bool:
    """Stop forced charge/discharge, return to automatic mode.

    Beendet Mode 1 (Disabled + active_power=0 + autorepeat=0 + trigger)
    UND restoriert battery_charge_max_current aus dem Store. Fallback wenn
    Store leer: attributes.max des Entities (typisch 30 A).
    """
    try:
        # Mode-1-Discharge sauber beenden (autorepeat=0 VOR trigger)
        await self._set_select("remotecontrol_power_control", "Disabled")
        await self._set_number("remotecontrol_active_power", 0)
        await self._set_number("remotecontrol_duration", 20)
        await self._set_number("remotecontrol_autorepeat_duration", 0)
        await self._press_trigger()

        # battery_charge_max_current restorieren
        original = await self._resolve_original_charge_current()
        if original is not None:
            await self._set_number("battery_charge_max_current", original)
        return True
    except Exception:
        _LOGGER.exception("SolaX: Failed to stop forcible mode")
        return False

async def _resolve_original_charge_current(self) -> float | None:
    """Liefert gecachten Originalwert; Fallback ist attributes.max des Entities."""
    if self._state_store is not None:
        await self._state_store.async_load()
        original = self._state_store.original_current
        if original is not None:
            return original
    # Fallback
    return self._read_max_charge_current_attribute()
```
</action>
<read_first>
- custom_components/eeg_energy_optimizer/inverter/solax.py
</read_first>
<acceptance_criteria>
- `grep -n "_resolve_original_charge_current" custom_components/eeg_energy_optimizer/inverter/solax.py` zeigt mindestens 2 Treffer (Definition + Aufruf in stop_forcible)
- In `async_stop_forcible`: nach dem `_press_trigger()` folgt ein Aufruf von `_resolve_original_charge_current` und ein `_set_number("battery_charge_max_current", ...)`
- `grep -B2 -A20 "async def async_stop_forcible" custom_components/eeg_energy_optimizer/inverter/solax.py | grep "battery_charge_max_current"` exit 0
</acceptance_criteria>
</task>

<task id="5">
<action>
Migration v17 → v18 in `custom_components/eeg_energy_optimizer/__init__.py` einfügen, direkt nach dem v17-Block (aktuell Zeile 947-953).

```python
    if entry.version < 18:
        # v18 — Phase 12: SolaX Charge-Block via battery_charge_max_current.
        # Neuer Entity-Override-Key für das Lade-Limit (analog zu den
        # existierenden solax_remotecontrol_*-Keys). Bestehende Anlagen
        # bekommen den Default-Entity-Pfad.
        new_data = {**entry.data}
        new_data.setdefault(
            "solax_battery_charge_max_current",
            "number.solax_inverter_battery_charge_max_current",
        )
        hass.config_entries.async_update_entry(entry, data=new_data, version=18)
```

Und in `custom_components/eeg_energy_optimizer/config_flow.py` `VERSION = 17` auf `VERSION = 18` setzen.
</action>
<read_first>
- custom_components/eeg_energy_optimizer/__init__.py
- custom_components/eeg_energy_optimizer/config_flow.py
</read_first>
<acceptance_criteria>
- `grep -n "version=18" custom_components/eeg_energy_optimizer/__init__.py` exit 0
- `grep -n "entry.version < 18" custom_components/eeg_energy_optimizer/__init__.py` exit 0
- `grep -n "solax_battery_charge_max_current" custom_components/eeg_energy_optimizer/__init__.py` exit 0
- `grep -n "^    VERSION = 18$" custom_components/eeg_energy_optimizer/config_flow.py` exit 0
- Migration-Block enthält den Default-Wert `"number.solax_inverter_battery_charge_max_current"` (verifizierbar via `grep -A6 "entry.version < 18" custom_components/eeg_energy_optimizer/__init__.py | grep "number.solax_inverter_battery_charge_max_current"`)
</acceptance_criteria>
</task>

<task id="6">
<action>
Update `tests/test_solax_inverter.py`. Die existierenden Tests in `TestAsyncSetChargeLimit` müssen angepasst werden, weil sich das Verhalten von `async_set_charge_limit(0)` geändert hat (kein Mode-1-Idle mehr).

Auch `TestAsyncStopForcible` braucht einen neuen Test für den Restore-Pfad. `TestAsyncSetDischarge` bleibt unverändert (regression guard).

Ersetze die existierende `test_block_charging_writes_zero_active_power` durch:

```python
async def test_block_charging_sets_max_current_to_zero(self, inverter, mock_hass):
    """power_kw=0 → battery_charge_max_current=0 + remotecontrol_power_control=Disabled.

    NEU in Phase 12: KEIN Mode-1-Idle mehr — nur Charge blockieren, Self-Use
    läuft weiter und entlädt bei Bedarf.
    """
    # Mock-State für Entity-Reads (Original-Caching)
    mock_state = MagicMock()
    mock_state.state = "30.0"
    mock_state.attributes = {"max": 30}
    mock_hass.states.get = MagicMock(return_value=mock_state)

    result = await inverter.async_set_charge_limit(0)
    assert result is True

    payloads = _calls_by_entity(mock_hass)
    # battery_charge_max_current wurde auf 0 gesetzt
    assert payloads[SOLAX_ENTITY_DEFAULTS["battery_charge_max_current"]]["value"] == 0
    # remotecontrol_power_control explizit auf Disabled
    select_calls = [c for c in mock_hass.services.async_call.call_args_list
                    if c.args[0] == "select" and c.args[1] == "select_option"]
    assert any(
        c.args[2].get("option") == "Disabled" for c in select_calls
    ), "remotecontrol_power_control must be set to Disabled"
    # KEIN Aufruf mit "Enabled Battery Control" mehr
    assert not any(
        c.args[2].get("option") == "Enabled Battery Control" for c in select_calls
    ), "Mode 1 must NOT be activated for charge block"
```

Und passe `test_partial_charge_kw_to_w` an (jetzt: kW → A, nicht mehr kW → W):

```python
async def test_partial_charge_writes_amps_to_max_current(self, inverter, mock_hass):
    """power_kw=3.0 → battery_charge_max_current in Ampere (kW über Spannung → A).

    Bei 400V Default-Fallback: 3000W / 400V = 7.5 A.
    """
    voltage_state = MagicMock()
    voltage_state.state = "400.0"
    voltage_state.attributes = {}

    max_current_state = MagicMock()
    max_current_state.state = "30.0"
    max_current_state.attributes = {"max": 30}

    def states_get(entity_id):
        if "voltage" in entity_id:
            return voltage_state
        return max_current_state

    mock_hass.states.get = MagicMock(side_effect=states_get)

    result = await inverter.async_set_charge_limit(3.0)
    assert result is True

    payloads = _calls_by_entity(mock_hass)
    written = payloads[SOLAX_ENTITY_DEFAULTS["battery_charge_max_current"]]["value"]
    assert written == pytest.approx(7.5, rel=0.01)
```

Füge neue Tests hinzu (in `TestAsyncSetChargeLimit`):

```python
async def test_caches_original_on_first_call(self, inverter, mock_hass, tmp_path, monkeypatch):
    """Erster Eingriff mit State > 0 cached den Wert im Store."""
    # Mock Store
    save_calls = []

    class FakeStore:
        async def async_load(self): return {}
        async def async_save(self, data): save_calls.append(data)

    inverter._state_store._store = FakeStore()
    inverter._state_store._loaded = False
    inverter._state_store._data = {}

    mock_state = MagicMock()
    mock_state.state = "25.0"
    mock_state.attributes = {"max": 30}
    mock_hass.states.get = MagicMock(return_value=mock_state)

    await inverter.async_set_charge_limit(0)
    assert len(save_calls) == 1
    assert save_calls[0]["battery_charge_max_current_original"] == 25.0

async def test_skips_cache_when_state_is_zero(self, inverter, mock_hass):
    """Reboot-Schutz: aktueller State 0 darf bestehenden Cache nicht überschreiben."""
    save_calls = []

    class FakeStore:
        async def async_load(self): return {"battery_charge_max_current_original": 30.0}
        async def async_save(self, data): save_calls.append(data)

    inverter._state_store._store = FakeStore()
    inverter._state_store._loaded = False
    inverter._state_store._data = {}

    mock_state = MagicMock()
    mock_state.state = "0.0"
    mock_state.attributes = {"max": 30}
    mock_hass.states.get = MagicMock(return_value=mock_state)

    # Cache hat bereits 30 — async_set_charge_limit darf NICHT überschreiben
    # (weil aktueller State=0 wäre, das ist nicht das Original)
    await inverter._state_store.async_load()
    assert inverter._state_store.original_current == 30.0

    await inverter.async_set_charge_limit(0)
    # Kein neuer save_call mit 0
    assert all(c.get("battery_charge_max_current_original") != 0 for c in save_calls)
```

Und in `TestAsyncStopForcible`:

```python
async def test_stop_forcible_restores_original_from_store(self, inverter, mock_hass):
    """stop_forcible restoriert gecachten battery_charge_max_current."""
    class FakeStore:
        async def async_load(self): return {"battery_charge_max_current_original": 25.0}
        async def async_save(self, data): pass

    inverter._state_store._store = FakeStore()
    inverter._state_store._loaded = False
    inverter._state_store._data = {}

    await inverter.async_stop_forcible()

    payloads = _calls_by_entity(mock_hass)
    assert payloads[SOLAX_ENTITY_DEFAULTS["battery_charge_max_current"]]["value"] == 25.0

async def test_stop_forcible_uses_max_fallback_when_no_cache(self, inverter, mock_hass):
    """Wenn Store leer, nutzt stop_forcible attributes.max."""
    class FakeStore:
        async def async_load(self): return {}
        async def async_save(self, data): pass

    inverter._state_store._store = FakeStore()
    inverter._state_store._loaded = False
    inverter._state_store._data = {}

    mock_state = MagicMock()
    mock_state.state = "0.0"
    mock_state.attributes = {"max": 30}
    mock_hass.states.get = MagicMock(return_value=mock_state)

    await inverter.async_stop_forcible()
    payloads = _calls_by_entity(mock_hass)
    assert payloads[SOLAX_ENTITY_DEFAULTS["battery_charge_max_current"]]["value"] == 30.0
```

`TestAsyncSetDischarge`-Tests bleiben unverändert (Mode 1 mit negativer Power für Discharge — regression guard).

Stelle sicher, dass `from unittest.mock import MagicMock` importiert ist (sollte schon).
</action>
<read_first>
- tests/test_solax_inverter.py
- custom_components/eeg_energy_optimizer/inverter/solax.py
</read_first>
<acceptance_criteria>
- `cd "C:\\Data\\source\\HA_EEG_Energy_Optimizier" && python -m pytest tests/test_solax_inverter.py -v` exit 0
- `grep -n "test_block_charging_sets_max_current_to_zero" tests/test_solax_inverter.py` exit 0
- `grep -n "test_caches_original_on_first_call" tests/test_solax_inverter.py` exit 0
- `grep -n "test_skips_cache_when_state_is_zero" tests/test_solax_inverter.py` exit 0
- `grep -n "test_stop_forcible_restores_original_from_store" tests/test_solax_inverter.py` exit 0
- `grep -n "test_stop_forcible_uses_max_fallback_when_no_cache" tests/test_solax_inverter.py` exit 0
- `grep -n "test_discharge_uses_negative_active_power" tests/test_solax_inverter.py` exit 0 (regression guard — Mode 1 weiter aktiv für Discharge)
- Alle SolaX-Tests in `tests/test_solax_inverter.py` grün
- Gesamttest-Suite läuft: `python -m pytest tests/ -q --tb=line` exit 0
</acceptance_criteria>
</task>

<task id="7">
<action>
Update `CHANGELOG.md`. Füge unter dem aktuellsten "## Unreleased"-Block (falls vorhanden, sonst neu anlegen ÜBER der letzten Release-Sektion) folgenden Eintrag ein:

```markdown
## Unreleased

### Geändert
- **SolaX:** Morgen-Einspeisung blockiert jetzt nur noch das Laden statt die Batterie via Mode 1 komplett auf Idle zu setzen. Hausverbrauch wird wieder aus der Batterie gedeckt (bis `selfuse_discharge_min_soc`, typisch 10 %), nicht mehr aus dem Netz. Bringt das SolaX-Verhalten auf gleiches Niveau wie Huawei und Fronius. (Phase 12)
- **SolaX:** `async_stop_forcible` setzt das Lade-Limit jetzt automatisch auf den vor dem Eingriff gespeicherten Originalwert zurück. Persistierung via `homeassistant.helpers.storage.Store` — überlebt HA-Reboots. (Phase 12)

### Verhaltensänderung beim Update
Auf SolaX-Anlagen: Während Morgen-Einspeisung wird die Batterie nicht mehr eingefroren. Wenn vorher der SOC bei ~19 % stehengeblieben ist, weil Mode 1 mit `active_power=0` die Batterie komplett stillgelegt hat, entlädt sie jetzt weiter bis zum konfigurierten `selfuse_discharge_min_soc` des Wechselrichters (Default 10 %). Der Original-Wert von `battery_charge_max_current` wird beim ersten Optimizer-Cycle nach dem Update automatisch erkannt und gespeichert.

### Migration
- Config-Schema v17 → v18: Neuer interner Entity-Override-Key `solax_battery_charge_max_current` (Default: `number.solax_inverter_battery_charge_max_current`). Automatisch via `async_migrate_entry` gesetzt — keine User-Aktion nötig.
```
</action>
<read_first>
- CHANGELOG.md
</read_first>
<acceptance_criteria>
- `grep -n "## Unreleased" CHANGELOG.md` exit 0
- `grep -A1 "## Unreleased" CHANGELOG.md | grep -i "SolaX\|Morgen-Einspeisung"` zeigt mind. 1 Treffer
- `grep -n "Phase 12" CHANGELOG.md` exit 0
- `grep -n "selfuse_discharge_min_soc" CHANGELOG.md` exit 0
- `grep -n "v17 → v18\|v17 -> v18" CHANGELOG.md` exit 0
</acceptance_criteria>
</task>

</tasks>

<verification>
After all tasks complete, run the following verification sequence in order:

```bash
# 1. Lint: no leftover Mode-1 activation in async_set_charge_limit
grep -B5 -A25 "async def async_set_charge_limit" custom_components/eeg_energy_optimizer/inverter/solax.py | grep -q '"Enabled Battery Control"' && echo "FAIL: Mode 1 still in set_charge_limit" && exit 1
echo "PASS: Mode 1 removed from set_charge_limit"

# 2. Lint: Mode 1 STILL present in async_set_discharge (regression guard)
grep -B2 -A20 "async def async_set_discharge" custom_components/eeg_energy_optimizer/inverter/solax.py | grep -q '"Enabled Battery Control"' || (echo "FAIL: Mode 1 removed from discharge (regression)" && exit 1)
echo "PASS: Mode 1 preserved in set_discharge"

# 3. Config-Version
grep -q "VERSION = 18" custom_components/eeg_energy_optimizer/config_flow.py && echo "PASS: VERSION = 18"
grep -q "entry.version < 18" custom_components/eeg_energy_optimizer/__init__.py && echo "PASS: Migration v17->v18 present"

# 4. Test-Suite
python -m pytest tests/test_solax_inverter.py -v --tb=short
python -m pytest tests/ -q --tb=line

# 5. Schnell-Audit für die geänderten Verhaltens-Kernpunkte
grep -c "battery_charge_max_current" custom_components/eeg_energy_optimizer/inverter/solax.py  # >= 5
grep -c "SolaXStateStore" custom_components/eeg_energy_optimizer/inverter/solax.py             # >= 3
```

Expected: alle Echo-PASS, alle pytest exit 0, alle grep-Counts ≥ Schwelle.
</verification>

<must_haves>
**Goal-backward verification — what defines done:**

1. **Verhaltensänderung verifizierbar:** Auf der Live-SolaX-Anlage (oder im Test) wird beim Übergang in Morgen-Einspeisung KEIN `remotecontrol_power_control = "Enabled Battery Control"` mehr geschrieben. Stattdessen geht `battery_charge_max_current` auf 0, und `remotecontrol_power_control` wird explizit auf "Disabled" gesetzt.
2. **Self-Use läuft weiter:** Während Morgen-Einspeisung kann die Batterie SOC abbauen durch Hausverbrauch (Self-Use-Mode-Verhalten der SolaX), wenn aktueller SOC > `selfuse_discharge_min_soc`. Der SOC bleibt NICHT bei einem Wert stehen, der über `selfuse_discharge_min_soc` liegt.
3. **Persistierung funktioniert:** Der Originalwert von `battery_charge_max_current` (typisch 30 A) wird beim ersten Eingriff in den Storage-Helper geschrieben und überlebt einen HA-Reboot.
4. **Restore funktioniert:** Beim Verlassen von Morgen-Einspeisung wird `battery_charge_max_current` wieder auf den gecachten Wert gesetzt — nicht auf einen Default.
5. **Reboot-Schutz greift:** Wenn HA mitten in einer Morgen-Einspeisung neu startet (aktueller State = 0), wird der existierende Store-Wert NICHT mit 0 überschrieben.
6. **Discharge unbeeinflusst:** Slot-A/B/Manual-Discharge funktioniert weiterhin via Mode 1 mit negativer `active_power` — keine Regression durch den Refactor.
7. **Migration ist idempotent:** Bestehende Anlagen werden auf Config-Version 18 migriert ohne User-sichtbaren Konfigurations-Verlust. Power-User mit eigenem Entity-Override für `battery_charge_max_current` werden über den Key `solax_battery_charge_max_current` respektiert.
8. **Tests grün:** Alle 8 neuen/aktualisierten SolaX-Inverter-Tests + die Gesamttest-Suite.
</must_haves>
