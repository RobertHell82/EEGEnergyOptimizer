---
phase: quick
plan: 260325-sh8
type: execute
wave: 1
depends_on: []
files_modified: [SOLAREDGE_RESEARCH.md]
autonomous: true
requirements: []
must_haves:
  truths:
    - "Research document covers all known SolarEdge HA integrations (official + custom)"
    - "Battery control capabilities are assessed for charge blocking, forced discharge, and SOC management"
    - "Feasibility for EEG Energy Optimizer InverterBase implementation is clearly stated"
    - "Comparison with existing Huawei pattern is provided"
  artifacts:
    - path: "SOLAREDGE_RESEARCH.md"
      provides: "Complete SolarEdge HA integration research document"
      min_lines: 150
  key_links: []
---

<objective>
Research SolarEdge inverter integrations available in Home Assistant and assess their suitability for the EEG Energy Optimizer project.

Purpose: Determine whether a SolarEdge inverter can be controlled (charge blocking, forced discharge, SOC management) via HA integrations, following the same pattern as the existing Huawei and planned SolaX implementations.

Output: A single research document `SOLAREDGE_RESEARCH.md` in the project root.
</objective>

<execution_context>
@C:/Users/RobertHell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/RobertHell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@custom_components/eeg_energy_optimizer/inverter/base.py
@custom_components/eeg_energy_optimizer/inverter/huawei.py
@.planning/quick/solax-research/RESEARCH.md

<interfaces>
<!-- The InverterBase ABC that any new inverter must implement -->
From custom_components/eeg_energy_optimizer/inverter/base.py:
```python
class InverterBase(ABC):
    def __init__(self, hass: Any, config: dict) -> None: ...
    async def async_set_charge_limit(self, power_kw: float) -> bool: ...
    async def async_set_discharge(self, power_kw: float, target_soc: float | None = None) -> bool: ...
    async def async_stop_forcible(self) -> bool: ...
    @property
    def is_available(self) -> bool: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Research SolarEdge HA integrations and write SOLAREDGE_RESEARCH.md</name>
  <files>SOLAREDGE_RESEARCH.md</files>
  <action>
Research SolarEdge inverter integrations for Home Assistant and produce a comprehensive markdown document at the project root. Use the SolaX research document (.planning/quick/solax-research/RESEARCH.md) as the template for structure and depth.

The research document MUST cover these sections in order:

1. **Summary** — One-paragraph executive summary with overall feasibility verdict
2. **Available Integrations** — For each integration found:
   - Integration name, type (core/HACS), connection method, control capability
   - URL/repository link
   - Supported SolarEdge models (SE3000H, SE5000H, SE10K, etc.)
   - Focus on these known integrations:
     a. Official HA SolarEdge integration (`solaredge`) — cloud-based monitoring API
     b. SolarEdge Modbus integration (`solaredge_modbus`) — local Modbus TCP
     c. Any HACS custom components for SolarEdge with battery control (e.g., `solaredge-modbus-multi`, `ha-solaredge-modbus`)
   - For each: explicitly state whether it supports WRITE operations (inverter control) or is read-only

3. **Sensor Entities** — Table of key entities provided by the most capable integration:
   - Battery SOC, battery power, PV power, grid power, house consumption
   - Map to what our optimizer needs (same format as SolaX research Section 2)

4. **Control Capabilities** — Critical section:
   - Can the battery charging be blocked/limited?
   - Can forced discharge be triggered with a target power?
   - Can a target SOC be set for discharge?
   - Can the inverter be returned to auto mode?
   - What HA services or entities are used for control?
   - StorEdge (SolarEdge's battery product line) specific considerations

5. **Mapping to InverterBase ABC** — Table mapping each ABC method to SolarEdge implementation:
   - `async_set_charge_limit(power_kw)` — how to block/limit charging
   - `async_set_discharge(power_kw, target_soc)` — how to force discharge
   - `async_stop_forcible()` — how to return to normal
   - `is_available` — how to check integration availability

6. **Comparison with Huawei Implementation** — Table comparing:
   - Integration type, control mechanism, charge blocking approach
   - Force discharge, stop forcible, target SOC handling
   - Key implementation differences

7. **Common Pitfalls** — Numbered list of things that can go wrong (same format as SolaX research Section 6)

8. **Feasibility Assessment** — Table with each requirement, feasibility (YES/NO/PARTIAL), approach, confidence level

9. **Scope Recommendation** — What to implement first vs later

10. **Sources** — Categorized by confidence (Primary/Secondary/Tertiary) with URLs

**Research approach:**
- Use WebFetch to check the official HA SolarEdge integration page (https://www.home-assistant.io/integrations/solaredge/)
- Use WebFetch to check SolarEdge Modbus integrations on GitHub (search for solaredge modbus home assistant)
- Use WebFetch to check the HA community forums and HACS default repositories for SolarEdge battery control
- Check the solaredge_modbus_multi integration which is commonly referenced for StorEdge battery control
- Look specifically for StorEdge / SolarEdge battery hybrid systems since standard SolarEdge inverters may not have batteries

**Key questions to answer:**
- Does SolarEdge even support battery storage natively? (StorEdge is their battery product)
- Which HA integration provides write access to StorEdge battery settings?
- Is Modbus TCP available on SolarEdge inverters for local control?
- Are there SetApp or cloud API methods for battery control?
- What is the SolarEdge Power Control (export limiting) capability and can it be leveraged?

**Document metadata:**
- Confidence: rate overall and per-section
- Research date: 2026-03-25
- Valid until: estimate based on integration maturity

Write the complete document to SOLAREDGE_RESEARCH.md in the project root.
  </action>
  <verify>
    <automated>test -f "SOLAREDGE_RESEARCH.md" && wc -l "SOLAREDGE_RESEARCH.md" | grep -E "^[1-9][0-9]{2,}" && echo "PASS: File exists with 100+ lines"</automated>
  </verify>
  <done>SOLAREDGE_RESEARCH.md exists in project root with all 10 sections populated, covering integrations, control capabilities, ABC mapping, comparison with Huawei, feasibility assessment, and sources</done>
</task>

</tasks>

<verification>
- SOLAREDGE_RESEARCH.md exists in project root
- Document has all 10 required sections
- Feasibility assessment clearly states YES/NO/PARTIAL for each InverterBase method
- Comparison with Huawei is provided
- Sources are cited with URLs
</verification>

<success_criteria>
- Complete research document covering SolarEdge HA integration landscape
- Clear feasibility verdict for implementing a SolarEdgeInverter class
- Actionable mapping of InverterBase ABC methods to SolarEdge control mechanisms
- Honest assessment of gaps or limitations compared to Huawei/SolaX
</success_criteria>

<output>
After completion, create `.planning/quick/260325-sh8-solaredge-ha-integration-research-m-glic/260325-sh8-SUMMARY.md`
</output>
