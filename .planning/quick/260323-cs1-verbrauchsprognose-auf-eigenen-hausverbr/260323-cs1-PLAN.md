---
phase: quick-260323-cs1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - custom_components/eeg_energy_optimizer/const.py
  - custom_components/eeg_energy_optimizer/__init__.py
  - custom_components/eeg_energy_optimizer/config_flow.py
  - custom_components/eeg_energy_optimizer/frontend/eeg-optimizer-panel.js
  - tests/test_config_flow.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "DEFAULT_CONSUMPTION_SENSOR points to sensor.eeg_energy_optimizer_hausverbrauch"
    - "Existing installations migrate to the new default via config migration v8"
    - "Panel wizard shows the new default sensor for new setups"
    - "All tests pass with the updated default"
  artifacts:
    - path: "custom_components/eeg_energy_optimizer/const.py"
      provides: "Updated DEFAULT_CONSUMPTION_SENSOR constant"
      contains: "sensor.eeg_energy_optimizer_hausverbrauch"
    - path: "custom_components/eeg_energy_optimizer/__init__.py"
      provides: "Migration v8 updating consumption_sensor"
    - path: "custom_components/eeg_energy_optimizer/config_flow.py"
      provides: "VERSION = 8"
  key_links:
    - from: "custom_components/eeg_energy_optimizer/sensor.py"
      to: "custom_components/eeg_energy_optimizer/const.py"
      via: "DEFAULT_CONSUMPTION_SENSOR import"
      pattern: "DEFAULT_CONSUMPTION_SENSOR"
    - from: "custom_components/eeg_energy_optimizer/__init__.py"
      to: "custom_components/eeg_energy_optimizer/const.py"
      via: "migration v8 uses new default value"
---

<objective>
Switch the default consumption sensor from the external Huawei sensor (sensor.power_meter_verbrauch) to the integration's own calculated Hausverbrauch sensor (sensor.eeg_energy_optimizer_hausverbrauch).

Purpose: The power_meter_verbrauch sensor only tracks grid import, missing self-consumption from PV/battery. The Hausverbrauch sensor calculates true house consumption (PV - Battery - Grid), making consumption forecasts accurate.

Output: Updated default constant, config migration v8, updated panel wizard default, passing tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@custom_components/eeg_energy_optimizer/const.py
@custom_components/eeg_energy_optimizer/__init__.py
@custom_components/eeg_energy_optimizer/config_flow.py
@custom_components/eeg_energy_optimizer/frontend/eeg-optimizer-panel.js
@tests/test_config_flow.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update default constant, config migration, and panel default</name>
  <files>
    custom_components/eeg_energy_optimizer/const.py,
    custom_components/eeg_energy_optimizer/__init__.py,
    custom_components/eeg_energy_optimizer/config_flow.py,
    custom_components/eeg_energy_optimizer/frontend/eeg-optimizer-panel.js
  </files>
  <action>
1. In const.py line 32, change:
   `DEFAULT_CONSUMPTION_SENSOR = "sensor.power_meter_verbrauch"`
   to:
   `DEFAULT_CONSUMPTION_SENSOR = "sensor.eeg_energy_optimizer_hausverbrauch"`

2. In __init__.py, add a new migration block after the version 7 block (after line 69):
   ```python
   if entry.version < 8:
       new_data = {**entry.data}
       # Switch default consumption sensor to own Hausverbrauch sensor
       if new_data.get("consumption_sensor") == "sensor.power_meter_verbrauch":
           new_data["consumption_sensor"] = "sensor.eeg_energy_optimizer_hausverbrauch"
       hass.config_entries.async_update_entry(entry, data=new_data, version=8)
   ```
   Note: Only migrate if the user had the OLD default. If they configured a custom sensor, leave it unchanged.

3. In config_flow.py line 24, change `VERSION = 7` to `VERSION = 8`.

4. In frontend/eeg-optimizer-panel.js line 63, change:
   `consumption_sensor: "sensor.power_meter_verbrauch",`
   to:
   `consumption_sensor: "sensor.eeg_energy_optimizer_hausverbrauch",`
  </action>
  <verify>
    <automated>grep -n "eeg_energy_optimizer_hausverbrauch" custom_components/eeg_energy_optimizer/const.py custom_components/eeg_energy_optimizer/__init__.py custom_components/eeg_energy_optimizer/frontend/eeg-optimizer-panel.js && grep -n "VERSION = 8" custom_components/eeg_energy_optimizer/config_flow.py</automated>
  </verify>
  <done>
    - const.py DEFAULT_CONSUMPTION_SENSOR = "sensor.eeg_energy_optimizer_hausverbrauch"
    - __init__.py has migration v8 that conditionally updates consumption_sensor
    - config_flow.py VERSION = 8
    - Panel wizard default is "sensor.eeg_energy_optimizer_hausverbrauch"
  </done>
</task>

<task type="auto">
  <name>Task 2: Update tests to use new default sensor</name>
  <files>tests/test_config_flow.py</files>
  <action>
In tests/test_config_flow.py, update all three references to the old default:

1. Line 263: Change `"sensor.power_meter_verbrauch"` to `"sensor.eeg_energy_optimizer_hausverbrauch"` in the async_step_consumption user_input.

2. Line 307: Same change in the full flow test's async_step_consumption user_input.

3. Line 330: Change the assertion:
   `assert data[CONF_CONSUMPTION_SENSOR] == "sensor.power_meter_verbrauch"`
   to:
   `assert data[CONF_CONSUMPTION_SENSOR] == "sensor.eeg_energy_optimizer_hausverbrauch"`
  </action>
  <verify>
    <automated>cd /c/Data/source/HA_EEG_Energy_Optimizier && python -m pytest tests/test_config_flow.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>All test_config_flow.py tests pass with the new default sensor value. No references to "sensor.power_meter_verbrauch" remain in test assertions for consumption_sensor.</done>
</task>

</tasks>

<verification>
- `grep -rn "power_meter_verbrauch" custom_components/eeg_energy_optimizer/` returns NO matches (only planning docs may still reference it)
- `grep -rn "eeg_energy_optimizer_hausverbrauch" custom_components/eeg_energy_optimizer/const.py` confirms new default
- `python -m pytest tests/ -x -q` passes
</verification>

<success_criteria>
- DEFAULT_CONSUMPTION_SENSOR constant changed to sensor.eeg_energy_optimizer_hausverbrauch
- Config migration v8 migrates existing installations (only if they had the old default)
- Panel wizard shows new default for new setups
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/quick/260323-cs1-verbrauchsprognose-auf-eigenen-hausverbr/260323-cs1-SUMMARY.md`
</output>
