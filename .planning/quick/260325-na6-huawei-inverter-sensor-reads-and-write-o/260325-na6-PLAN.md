---
phase: quick
plan: 260325-na6
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260325-na6-huawei-inverter-sensor-reads-and-write-o/HUAWEI-INVENTORY.md
autonomous: true
requirements: [INVENTORY-01]

must_haves:
  truths:
    - "Every inverter-dependent sensor read is listed with config key, default entity, data type, unit, and source location"
    - "Every inverter write operation is listed with abstract method, Huawei service call, parameters, and call sites"
    - "Every hardcoded Huawei reference is listed with file location, value, and porting impact"
    - "A porting checklist identifies all files and changes needed for a new inverter type"
    - "Generic (already portable) code is explicitly identified so porters know what NOT to touch"
  artifacts:
    - path: ".planning/quick/260325-na6-huawei-inverter-sensor-reads-and-write-o/HUAWEI-INVENTORY.md"
      provides: "Complete Huawei inverter dependency inventory for porting reference"
      min_lines: 150
  key_links:
    - from: "HUAWEI-INVENTORY.md section A (Reads)"
      to: "optimizer.py, sensor.py, __init__.py"
      via: "hass.states.get() calls with config-mapped entity IDs"
      pattern: "hass\\.states\\.get"
    - from: "HUAWEI-INVENTORY.md section B (Writes)"
      to: "inverter/huawei.py, inverter/base.py"
      via: "InverterBase ABC methods -> HuaweiInverter service calls"
      pattern: "async_set_charge_limit|async_set_discharge|async_stop_forcible"
    - from: "HUAWEI-INVENTORY.md section D (Hardcoded)"
      to: "inverter/huawei.py, websocket_api.py, const.py, __init__.py"
      via: "Huawei-specific entity IDs, device lookup, domain checks"
      pattern: "huawei_solar|HUAWEI_DEFAULTS"
---

<objective>
Create a structured inventory document cataloging all Huawei inverter dependencies (sensor reads, write operations, config parameters, hardcoded references) as a porting reference for adding new inverter types like SolaX.

Purpose: Enable systematic porting by providing a single reference document that maps every Huawei-specific touchpoint in the codebase.
Output: HUAWEI-INVENTORY.md — the definitive porting checklist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260325-na6-huawei-inverter-sensor-reads-and-write-o/260325-na6-RESEARCH.md

@custom_components/eeg_energy_optimizer/inverter/base.py
@custom_components/eeg_energy_optimizer/inverter/huawei.py
@custom_components/eeg_energy_optimizer/inverter/__init__.py
@custom_components/eeg_energy_optimizer/optimizer.py
@custom_components/eeg_energy_optimizer/sensor.py
@custom_components/eeg_energy_optimizer/websocket_api.py
@custom_components/eeg_energy_optimizer/const.py
@custom_components/eeg_energy_optimizer/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HUAWEI-INVENTORY.md from research findings with source verification</name>
  <files>.planning/quick/260325-na6-huawei-inverter-sensor-reads-and-write-o/HUAWEI-INVENTORY.md</files>
  <action>
Create the inventory document at the target path. Use the research (260325-na6-RESEARCH.md) as the primary source — it already contains the complete inventory. Transform it into a clean reference document with these sections:

1. **Header**: Title, date, purpose statement ("Porting reference for adding new inverter types")

2. **Section A — Sensor Reads (Entity State Reads)**: Table with columns: #, Config Key, Huawei Default Entity, Data Type, Unit, Source File:Line, Purpose. Split into:
   - A1: Battery sensors (SOC, capacity, power) — 3 entries
   - A2: PV and Grid sensors (pv_power, grid_power) — 2 entries
   - A3: Huawei-only reads (max charge power entity) — 1 entry
   - A4: Non-inverter reads (sun.sun, forecasts) — listed but marked as "no porting needed"
   - A5: Indirect reads (own Hausverbrauch sensor) — marked as "derived, no porting needed"

3. **Section B — Inverter Writes (Service Calls)**:
   - B1: Abstract interface listing (InverterBase ABC — 3 methods + 1 property)
   - B2: Huawei implementation table: #, Method, HA Service, Service Data, When Called
   - B3: Call sites table: Call Site (file:function), Method, Trigger

4. **Section C — Config Parameters**: Table: Config Key, Constant, Generic?, Porting Note

5. **Section D — Hardcoded Huawei References**: Table: File:Line, Hardcoded Value, What It Does, Porting Action Required. This is the critical section — verify each line reference against the actual source files.

6. **Section E — Data Flow Diagram**: ASCII diagram from research showing reads -> optimizer -> writes

7. **Section F — Porting Checklist**: Numbered steps to add a new inverter type. Include both "must implement" and "already generic (no changes)" lists.

IMPORTANT: Verify all line number references from the research against the actual source files. Update any that have drifted. The document must have accurate line references.

Format as clean Markdown. No frontmatter needed — this is a reference document, not a plan.
  </action>
  <verify>
    <automated>wc -l ".planning/quick/260325-na6-huawei-inverter-sensor-reads-and-write-o/HUAWEI-INVENTORY.md" | awk '{if ($1 >= 150) print "PASS: " $1 " lines"; else print "FAIL: only " $1 " lines"}'</automated>
  </verify>
  <done>HUAWEI-INVENTORY.md exists with all 7 sections (A-F + header), every sensor read and write operation documented with verified file:line references, and a complete porting checklist</done>
</task>

</tasks>

<verification>
- Document has all sections A through F
- Line count >= 150 (comprehensive coverage)
- All 5 inverter-dependent sensor reads documented (battery_soc, battery_capacity, battery_power, pv_power, grid_power)
- All 3 InverterBase write methods documented (set_charge_limit, set_discharge, stop_forcible)
- All hardcoded Huawei references from section D of research are included
- Porting checklist covers all 8 steps from research
</verification>

<success_criteria>
A single HUAWEI-INVENTORY.md file that a developer can use as the sole reference to port the integration to a new inverter type — without needing to grep the codebase themselves.
</success_criteria>

<output>
After completion, create `.planning/quick/260325-na6-huawei-inverter-sensor-reads-and-write-o/260325-na6-SUMMARY.md`
</output>
