---
phase: quick-260323-q66
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - custom_components/eeg_energy_optimizer/optimizer.py
  - tests/test_optimizer.py
autonomous: true
requirements: [daylight-consumption-sensor]

must_haves:
  truths:
    - "Morning delay decision uses daylight-only (SA->SU) consumption instead of full-day consumption"
    - "Before morning_end_time, today's SA->SU consumption is used; after, tomorrow's SA->SU"
    - "Evening discharge logic remains unchanged (still uses full-day consumption_tomorrow_kwh)"
    - "Dashboard 'Bedarf + Puffer' line automatically shows the corrected (lower) value"
  artifacts:
    - path: "custom_components/eeg_energy_optimizer/optimizer.py"
      provides: "Snapshot with daylight consumption fields, updated morning delay logic"
      contains: "consumption_today_daylight_kwh"
    - path: "tests/test_optimizer.py"
      provides: "Tests for daylight consumption calculation and morning delay switching"
      contains: "daylight"
  key_links:
    - from: "optimizer.py _gather_snapshot()"
      to: "coordinator.calculate_period()"
      via: "sunrise/sunset boundaries"
      pattern: "calculate_period.*sunrise.*sunset"
    - from: "optimizer.py _calc_energiebedarf()"
      to: "Snapshot.consumption_today_daylight_kwh"
      via: "field access"
      pattern: "consumption_today_daylight_kwh"
    - from: "optimizer.py _morning_delay_status()"
      to: "Snapshot.consumption_tomorrow_daylight_kwh"
      via: "tomorrow preview"
      pattern: "consumption_tomorrow_daylight_kwh"
---

<objective>
Add daylight-only (sunrise-to-sunset) consumption fields to the optimizer Snapshot and use them in the morning delay decision instead of full-day consumption.

Purpose: The current morning delay threshold uses 24h consumption (including nighttime WP/EV charging), making the threshold unrealistically high. Using only daylight hours gives a much more accurate picture of what PV actually needs to cover during production hours.

Output: Modified optimizer.py with daylight consumption fields and updated morning delay logic; tests covering the new behavior.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260323-q66-tagesverbrauch-sensor-sonnenaufgang-sonn/260323-q66-CONTEXT.md
@.planning/quick/260323-q66-tagesverbrauch-sensor-sonnenaufgang-sonn/260323-q66-RESEARCH.md
@custom_components/eeg_energy_optimizer/optimizer.py

<interfaces>
<!-- Key types and contracts the executor needs -->

From optimizer.py (Snapshot dataclass, lines 71-84):
```python
@dataclass
class Snapshot:
    now: datetime
    battery_soc: float = 0.0
    battery_capacity_kwh: float = 0.0
    pv_remaining_today_kwh: float | None = None
    pv_tomorrow_kwh: float | None = None
    consumption_today_kwh: float = 0.0
    consumption_to_sunset_kwh: float = 0.0
    consumption_tomorrow_kwh: float = 0.0
    consumption_overnight_kwh: float = 0.0
    sunrise: datetime | None = None
    sunset: datetime | None = None
```

From coordinator.py (calculate_period API):
```python
def calculate_period(self, start: datetime, end: datetime) -> dict:
    """Returns {"verbrauch_kwh": float, "stunden": float, "stundenprofil": list}"""
```

Key methods to modify:
- `_gather_snapshot()` (line 171) -- add daylight consumption calculation
- `_calc_energiebedarf()` (line 414) -- use daylight consumption
- `_morning_delay_status()` (line 296) -- use daylight consumption for tomorrow preview
- `_build_markdown()` (line 598) -- update label text from "Verbrauch bis SU" to reflect daylight scope
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add daylight consumption fields to Snapshot and update morning delay logic</name>
  <files>custom_components/eeg_energy_optimizer/optimizer.py, tests/test_optimizer.py</files>
  <behavior>
    - Test: Snapshot has `consumption_today_daylight_kwh` and `consumption_tomorrow_daylight_kwh` fields (default 0.0)
    - Test: `_gather_snapshot()` computes daylight consumption using `coordinator.calculate_period(sunrise, sunset)` for today and tomorrow
    - Test: Sun time derivation -- when `next_rising` is tomorrow (afternoon call), today's sunrise is derived by subtracting 1 day
    - Test: Sun time derivation -- when `next_setting` is tomorrow (after sunset), today's sunset is derived correctly
    - Test: Tomorrow sunrise/sunset approximated by shifting today's values by +1 day
    - Test: `_calc_energiebedarf()` uses `consumption_today_daylight_kwh` instead of `consumption_to_sunset_kwh`
    - Test: `_morning_delay_status()` outside-window path uses `consumption_tomorrow_daylight_kwh` instead of `consumption_tomorrow_kwh`
    - Test: Evening discharge `_should_discharge()` and `_discharge_detail_status()` still use `consumption_tomorrow_kwh` (unchanged)
    - Test: When sunrise/sunset is None, daylight consumption fields remain 0.0 (graceful fallback)
  </behavior>
  <action>
  **1. Add Snapshot fields** (after line 83):
  ```python
  consumption_today_daylight_kwh: float = 0.0    # SA -> SU today
  consumption_tomorrow_daylight_kwh: float = 0.0  # SA -> SU tomorrow
  ```

  **2. Add daylight consumption calculation in `_gather_snapshot()`** (after the overnight consumption block, before the return statement around line 233):

  Derive today's and tomorrow's sunrise/sunset from `sun.sun` attributes:
  - If `sunrise.date() == today_date`: today_sunrise = sunrise, else today_sunrise = sunrise - timedelta(days=1)
  - If `sunset.date() == today_date`: today_sunset = sunset, else need to handle -- after sunset, `next_setting` is tomorrow, so today_sunset = sunset - timedelta(days=1)
  - Tomorrow: shift today's values by +1 day (accurate enough for hourly resolution)

  Calculate:
  - `consumption_today_daylight`: `coordinator.calculate_period(max(today_sunrise, now), today_sunset)` -- only remaining daylight hours
  - `consumption_tomorrow_daylight`: `coordinator.calculate_period(tomorrow_sunrise, tomorrow_sunset)` -- full daylight day

  Guard: only compute if sunrise and sunset are both not None and today_sunset > now.

  Add both fields to the Snapshot return constructor.

  **3. Update `_calc_energiebedarf()`** (line 414-431):
  Change `consumption = snap.consumption_to_sunset_kwh` to `consumption = snap.consumption_today_daylight_kwh`.
  Update docstring to reflect "daylight consumption (SA->SU)" instead of "consumption to sunset".

  **4. Update `_morning_delay_status()`** (line 352):
  Change `tomorrow_demand = snap.consumption_tomorrow_kwh + missing_battery_est` to `tomorrow_demand = snap.consumption_tomorrow_daylight_kwh + missing_battery_est`.

  **5. Update `_build_markdown()`** (line 619):
  Change the label from `"Verbrauch bis SU"` to `"Verbrauch SA-SU"` to accurately reflect the daylight scope.

  **IMPORTANT -- Leave unchanged:**
  - `_should_discharge()` (line 520): keeps using `snap.consumption_tomorrow_kwh` (full 24h) per locked decision
  - `_discharge_detail_status()` (line 380): keeps using `snap.consumption_tomorrow_kwh` per locked decision
  - All existing `consumption_to_sunset_kwh` and `consumption_tomorrow_kwh` fields remain on Snapshot (used elsewhere)
  </action>
  <verify>
    <automated>cd C:\Data\source\HA_EEG_Energy_Optimizier && python -m pytest tests/test_optimizer.py -x -v -k "daylight or energiebedarf or morning" 2>&1 | tail -30</automated>
  </verify>
  <done>
    - Snapshot dataclass has two new daylight consumption fields
    - `_gather_snapshot()` computes them from coordinator using sunrise/sunset boundaries
    - `_calc_energiebedarf()` uses daylight consumption for morning delay threshold
    - `_morning_delay_status()` tomorrow preview uses daylight consumption
    - Evening discharge logic unchanged (still uses full-day consumption)
    - All existing and new tests pass
  </done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_optimizer.py -x -v` -- all tests pass
- Verify `_should_discharge()` still references `snap.consumption_tomorrow_kwh` (not daylight)
- Verify `_discharge_detail_status()` still references `snap.consumption_tomorrow_kwh` (not daylight)
</verification>

<success_criteria>
- Morning delay decision (both in-window and tomorrow-preview) uses SA->SU consumption
- Evening discharge decision uses full 24h consumption (unchanged)
- Daylight consumption correctly derived from sun times even when next_rising/next_setting point to different days
- Tests cover the sun time derivation edge cases and the morning/evening split
</success_criteria>

<output>
After completion, create `.planning/quick/260323-q66-tagesverbrauch-sensor-sonnenaufgang-sonn/260323-q66-SUMMARY.md`
</output>
